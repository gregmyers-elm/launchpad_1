import { type OrgExtract } from "../engine/pull/org.js";
export declare function writeOrgCatalogFromExtract(root: string, extract: OrgExtract): Promise<{
    catalogPath: string;
    orgPath: string;
}>;
/**
 * Rewrite generated/catalog.ts ALONE from the current file tree — org.ts is
 * untouched (it belongs to `pull org`). Called after a pull writes new
 * authoring (apps/agents) so `@catalog` resolves the fresh entries.
 */
export declare function writeCatalogBarrel(root: string): Promise<string>;
