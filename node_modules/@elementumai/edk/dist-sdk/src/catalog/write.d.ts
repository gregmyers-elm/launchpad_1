import type { CatalogRef } from "./types.js";
/** Where a catalog is written, and how its file is named. */
export interface CatalogTarget {
    /** Directory holding this catalog's file. */
    readonly dir: string;
    /** Export const name emitted in the rendered file. */
    readonly handle: string;
    /** File stem; defaults to `handle` when omitted. */
    readonly filename?: string;
    readonly name: string;
    readonly kind: "app" | "element" | "org";
}
/** Target for an app catalog: `<root>/apps/<appHandle>/generated/catalog.ts`. */
export declare function appCatalogTarget(root: string, appHandle: string, appName: string): CatalogTarget;
/** Target for the org catalog: `<root>/generated/catalog.ts`. */
export declare function orgCatalogTarget(root: string): CatalogTarget;
/** The catalog file path for a target. */
export declare function barrelPathFor(target: CatalogTarget): string;
/**
 * Write a single flat catalog file for the given refs. This is the canonical
 * write path: one file, factory-based format, no sub-directories.
 *
 * With `preserveIfPulled: true`, skips the write when the file already
 * contains `export default` — indicating a pulled app source lives there and
 * should not be clobbered by a reference-only catalog write.
 */
export declare function writeFlatCatalog(target: CatalogTarget, refs: readonly CatalogRef[], opts?: {
    preserveIfPulled?: boolean;
    skipIfUnchanged?: boolean;
}): Promise<{
    catalogPath: string;
}>;
/** @deprecated Use writeFlatCatalog instead. */
export declare function writeFragment(target: CatalogTarget, ref: CatalogRef): Promise<{
    path: string;
    importPath: string;
}>;
/** @deprecated Use writeFlatCatalog instead. */
export declare function regenerateBarrel(target: CatalogTarget): Promise<string>;
/** @deprecated Use writeFlatCatalog instead. */
export declare function writeCatalog(target: CatalogTarget, refs: readonly CatalogRef[]): Promise<{
    barrelPath: string;
}>;
