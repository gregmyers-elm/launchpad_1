/**
 * Entity-agnostic helpers for CatalogFacet.renderEntry implementations.
 * Engine render.ts only dispatches; dialect lives on facets / deferred meta.
 */
import type { CatalogRenderResult } from "../graph/types.js";
/** Quote bag keys that are not safe bare identifiers. */
export declare function safeCatalogKey(name: string): string;
/** `key: factoryName(),` — agent/skill/empty automation style. */
export declare function bareFactoryEntry(factoryName: string, keyName: string, baseIndent: string): CatalogRenderResult;
export interface NamedFactoryEntryOpts {
    readonly factoryName: string;
    readonly keyName: string;
    readonly baseIndent: string;
    readonly name: string;
    /** When set, emit `parent: …`. Omit for appCatalog-injected kinds. */
    readonly parent?: string;
    /** Extra `key: value` fragments already formatted (e.g. `fieldType: "TEXT"`). */
    readonly extraParts?: readonly string[];
    readonly extraFactories?: readonly string[];
}
/** `key: factoryName({ name, … }),` */
export declare function namedFactoryEntry(opts: NamedFactoryEntryOpts): CatalogRenderResult;
/** Narrow an unknown catalog ref to a name-bearing record. */
export declare function asNamedRef(ref: unknown): {
    name: string;
    parent?: string;
    refName?: string;
};
/** `{ name }` only — org identity bags and similar. */
export declare function namedOnlyFactoryEntry(factoryName: string): (args: import("../graph/types.js").CatalogRenderArgs) => CatalogRenderResult;
/**
 * `{ name }` with parent omitted — appCatalog injects parent from the root
 * (search tables).
 */
export declare function rootParentFactoryEntry(factoryName: string, extraParts?: (ref: unknown) => readonly string[]): (args: import("../graph/types.js").CatalogRenderArgs) => CatalogRenderResult;
