import { EdkValidationError } from "../../../elementum-entity-client/errors.js";
export function secret(variable) {
    if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(variable)) {
        throw new EdkValidationError("organization secret variable must be a Terraform identifier; supply it as TF_VAR_<variable>.");
    }
    return { __kind: "organizationSecret", variable };
}
/** Pull-only placeholder for a write-only credential. Replace with secret(...). */
export function redactedSecret(variable) {
    return { __kind: "redactedOrganizationSecret", variable };
}
/** Pull-only marker for an input the platform does not expose. Replace it with the real value. */
export function unavailableValue(field) {
    return { __kind: "unavailableOrganizationValue", field };
}
export const cloudLinkScheduleUnit = {
    hours: "HOUR",
    minutes: "MINUTE",
};
export const cloudLinkWarehouseSize = {
    xSmall: "XSMALL",
    small: "SMALL",
    medium: "MEDIUM",
    large: "LARGE",
    xLarge: "XLARGE",
    xxLarge: "XXLARGE",
    xxxLarge: "XXXLARGE",
    x4Large: "X4LARGE",
    x5Large: "X5LARGE",
    x6Large: "X6LARGE",
};
export const cloudLinkUsageType = {
    standard: "STANDARD",
    provider: "PROVIDER",
    external: "EXTERNAL",
};
const cloudLinkType = {
    api: "api",
    bigQuery: "bigquery",
    databricks: "databricks",
    elementum: "elementum",
    elementumSnowflake: "elementum_snowflake",
    snowflake: "snowflake",
};
export const cloudLinkTypeMetadata = {
    api: { config: "api", credentials: true },
    bigquery: { config: "bigQuery", credentials: true },
    databricks: { config: "databricks", credentials: true },
    elementum: { config: undefined, credentials: false },
    elementum_snowflake: { config: "elementumSnowflake", credentials: false },
    snowflake: { config: "snowflake", credentials: true },
};
export function cloudLinkScheduleUnitKey(value) {
    return Object.entries(cloudLinkScheduleUnit)
        .find(([, candidate]) => candidate === value)?.[0];
}
export function cloudLinkWarehouseSizeKey(value) {
    return Object.entries(cloudLinkWarehouseSize)
        .find(([, candidate]) => candidate === value)?.[0];
}
export function cloudLinkUsageTypeKey(value) {
    return Object.entries(cloudLinkUsageType)
        .find(([, candidate]) => candidate === value)?.[0];
}
export function isCloudLinkWarehouseSize(value) {
    return Object.values(cloudLinkWarehouseSize).some((candidate) => candidate === value);
}
export const bearerTokenCredentials = (token) => ({ bearerToken: { token } });
export const passwordCredentials = (opts) => ({ password: opts });
export const privateKeyCredentials = (username) => ({ privateKey: { username } });
export const externalOauthCredentials = (opts) => ({ externalOauth: opts });
export const serviceAccountCredentials = (json) => ({ serviceAccount: { json } });
export function apiCloudLink(opts) {
    const { baseUrl, ...common } = opts;
    return { ...common, type: cloudLinkType.api, api: { baseUrl } };
}
export function bigQueryCloudLink(opts) {
    const { projectId, datasetId, location, readOnly, schedule, ...common } = opts;
    return { ...common, type: cloudLinkType.bigQuery, bigQuery: { projectId, datasetId, location, readOnly, ...(schedule !== undefined && { schedule }) } };
}
export function databricksCloudLink(opts) {
    const { hostname, port, database, schema, readOnly, schedule, ...common } = opts;
    return { ...common, type: cloudLinkType.databricks, databricks: { hostname, port, database, schema, readOnly, ...(schedule !== undefined && { schedule }) } };
}
export function elementumCloudLink(opts) {
    return { ...opts, type: cloudLinkType.elementum };
}
export function elementumSnowflakeCloudLink(opts) {
    const { region, warehouseSize, schedule, ...common } = opts;
    return { ...common, type: cloudLinkType.elementumSnowflake, elementumSnowflake: { region, warehouseSize, ...(schedule !== undefined && { schedule }) } };
}
export function snowflakeCloudLink(opts) {
    const { url, account, warehouse, role, readOnly, createDatabase, createSchema, gzip, schedule, ...common } = opts;
    return { ...common, type: cloudLinkType.snowflake, snowflake: { url, account, warehouse, role, readOnly, ...(createDatabase !== undefined && { createDatabase }), ...(createSchema !== undefined && { createSchema }), ...(gzip !== undefined && { gzip }), ...(schedule !== undefined && { schedule }) } };
}
export function emailAlias(alias) {
    if (alias.trim() === "")
        throw new EdkValidationError("email alias must not be empty");
    return { alias };
}
export function twilioPhoneProvider(opts) {
    return { name: opts.name, twilio: { accountId: opts.accountId, authToken: opts.authToken } };
}
function requireNames(bag, values) {
    for (const [key, value] of Object.entries(values ?? {})) {
        if (key.trim() === "")
            throw new EdkValidationError(`organization ${bag} keys must be non-empty.`);
        if (value.name.trim() === "")
            throw new EdkValidationError(`organization ${bag}.${key}.name must be non-empty.`);
    }
}
function validateCloudLink(key, value) {
    const metadata = cloudLinkTypeMetadata[value.type];
    if (metadata === undefined) {
        throw new EdkValidationError(`organization cloudLinks.${key} has unsupported type ${JSON.stringify(value.type)}.`);
    }
    const configs = {
        api: value.api,
        bigQuery: value.bigQuery,
        databricks: value.databricks,
        elementumSnowflake: value.elementumSnowflake,
        snowflake: value.snowflake,
    };
    for (const [configKey, config] of Object.entries(configs)) {
        if (configKey === metadata.config && config === undefined) {
            throw new EdkValidationError(`organization cloudLinks.${key} requires its ${configKey} configuration.`);
        }
        if (configKey !== metadata.config && config !== undefined) {
            throw new EdkValidationError(`organization cloudLinks.${key} type ${value.type} cannot include ${configKey} configuration.`);
        }
        const schedule = config !== undefined && "schedule" in config ? config.schedule : undefined;
        if (schedule !== undefined && (!Number.isInteger(schedule.interval) || schedule.interval < 1)) {
            throw new EdkValidationError(`organization cloudLinks.${key} schedule interval must be a positive integer.`);
        }
    }
    if (!metadata.credentials && value.credentials !== undefined) {
        throw new EdkValidationError(`organization cloudLinks.${key} type ${value.type} cannot include credentials.`);
    }
}
/** Organization-owned resources live in one ergonomic, optional `organization.ts` file. */
export function organization(opts) {
    requireNames("categories", opts.categories);
    requireNames("groups", opts.groups);
    requireNames("cloudLinks", opts.cloudLinks);
    requireNames("phoneProviders", opts.phoneProviders);
    for (const [key, value] of Object.entries(opts.emailAliases ?? {})) {
        if (key.trim() === "" || value.alias.trim() === "")
            throw new EdkValidationError(`organization emailAliases.${key} must have a non-empty alias.`);
    }
    for (const [key, value] of Object.entries(opts.cloudLinks ?? {}))
        validateCloudLink(key, value);
    for (const [key, group] of Object.entries(opts.groups ?? {})) {
        if (group.dynamic === true && (group.members?.length ?? 0) > 0) {
            throw new EdkValidationError(`organization groups.${key} cannot set members on a dynamic group.`);
        }
    }
    return {
        kind: "organizationConfig",
        categories: opts.categories ?? {},
        groups: opts.groups ?? {},
        cloudLinks: opts.cloudLinks ?? {},
        emailAliases: opts.emailAliases ?? {},
        phoneProviders: opts.phoneProviders ?? {},
    };
}
