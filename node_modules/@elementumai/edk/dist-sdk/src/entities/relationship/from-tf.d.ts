import { type FromTfContext } from "../../engine/pull/context.js";
import { type ChildResourceScope, type TfBlock } from "../../engine/tf/index.js";
import type { NormalizedRelationship } from "../app/deserializer/normalize.js";
export interface RelationshipOwner {
    /** Public EDK vocabulary; provider resource names stay at the TF boundary. */
    readonly kind: "app" | "element";
    readonly label: string;
    readonly ownerRefName: string;
    readonly ownerTfLabel: string;
    readonly scope: ChildResourceScope;
}
export declare function relationshipsFromTf(blocks: readonly TfBlock[], owner: RelationshipOwner, ctx: FromTfContext): NormalizedRelationship[];
