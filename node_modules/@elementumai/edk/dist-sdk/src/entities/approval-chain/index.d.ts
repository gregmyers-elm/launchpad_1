/**
 * Approval-process authoring. App-owned; PUT-updatable.
 * Standalone from v1's `src/approvalChains`.
 */
export { approvalProcess, unsafeApprovalStageId, type ApprovalProcessConfigured, type DefineApprovalProcessOptions, type UnsafeApprovalStageId, } from "./build.js";
