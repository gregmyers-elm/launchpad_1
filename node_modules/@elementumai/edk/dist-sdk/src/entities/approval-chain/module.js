import { rootParentFactoryEntry } from "../../catalog/render-helpers.js";
import { NO_DATA } from "../../graph/types.js";
import { approvalChainFromTf } from "./from-tf.js";
import { printApprovalChain } from "./printer.js";
import { approvalChainToTf } from "./to-tf.js";
export const APPROVAL_PROCESS_RESOURCE_TYPE = "elementum_approval_process";
function stub(refName, parent) { return { __kind: "approvalChain", refName, name: refName, parent }; }
function refFromAuthoring(args) {
    const value = args.defaultExport;
    if (typeof value === "object" &&
        value !== null &&
        typeof value.ref === "function" &&
        typeof value.def === "function") {
        const ref = value.ref(args.refName);
        return typeof ref === "object" && ref !== null && ref.__kind === "approvalChain"
            ? ref
            : undefined;
    }
    if (typeof value !== "object" || value === null || value.kind !== "approvalChain")
        return undefined;
    const entity = value;
    const name = entity.payload?.name;
    return { __kind: "approvalChain", refName: entity.refName || args.refName, name: typeof name === "string" ? name : args.refName, parent: entity.parent || args.parent };
}
function extract(blocks, _stream, ctx) {
    const items = [];
    const skipped = [];
    const diagnostics = [];
    for (const block of blocks) {
        try {
            items.push(approvalChainFromTf(block, ctx.appTfLabel, ctx));
        }
        catch (error) {
            skipped.push(`${block.type}.${block.label ?? ""}`);
            diagnostics.push(error instanceof Error ? error.message : String(error));
        }
    }
    return { items, skipped, diagnostics };
}
export const approvalChainModule = {
    kind: "approval-chain", stream: { role: "streamMember" }, authoring: { unit: "ownFile", bag: "approval-chains" },
    parents: [{ parent: "app", child: "approval-chain" }],
    resource: { shape: "single", resourceType: APPROVAL_PROCESS_RESOURCE_TYPE, adoptable: true }, data: NO_DATA,
    lifecycle: {
        participates: true, extractOwnFile: extract,
        printOwnFile: (item, opts) => { const chain = item; return { refName: chain.refName, source: printApprovalChain(chain, opts.appRefName) }; },
        toTf: (entity, ctx) => approvalChainToTf(entity, { appRefName: ctx.appRefName, appTfLabel: ctx.appTfLabel }),
    },
    canvas: { participates: true, label: "Approval processes", glyph: "✓", component: "generic" },
    catalog: { participates: true, refKind: "approvalChain", catalogBag: "approvalChains", renderOrder: 26, catalogRoot: "app", renderEntry: rootParentFactoryEntry("approvalChainRef"), stubRef: stub, refFromAuthoring },
};
