/** The skill entity's registry entry + resource-type constant (see src/graph/). */
import { rootParentFactoryEntry } from "../../catalog/render-helpers.js";
import { isTfExpr } from "../../engine/tf/index.js";
import { printSkillV2 } from "./deserializer/print-skill-v2.js";
import { skillFromTf } from "./from-tf.js";
import { skillToTf } from "./to-tf.js";
export const SKILL_RESOURCE_TYPE = "elementum_agentic_skill";
function isConfigured(value) {
    return (typeof value === "object" &&
        value !== null &&
        typeof value.ref === "function" &&
        typeof value.def === "function");
}
function skillStubRef(refName, parent) {
    return { __kind: "skill", refName, name: refName, parent };
}
function skillRefFromAuthoring(args) {
    const { refName, parent, defaultExport } = args;
    if (isConfigured(defaultExport)) {
        const catalogRef = defaultExport.ref(refName);
        if (catalogRef.__kind !== "skill")
            return undefined;
        return catalogRef;
    }
    if (typeof defaultExport !== "object" ||
        defaultExport === null ||
        defaultExport.kind !== "skill") {
        return undefined;
    }
    const entity = defaultExport;
    const entityRefName = entity.refName === "" ? refName : entity.refName;
    const entityParent = entity.parent === "" ? parent : entity.parent;
    const maybeRefName = entity.ref?.name;
    const payload = entity.payload;
    const name = typeof maybeRefName === "string"
        ? maybeRefName
        : typeof payload.name === "string"
            ? payload.name
            : typeof payload.skill?.name === "string"
                ? payload.skill.name
                : entityRefName;
    return { __kind: "skill", refName: entityRefName, name, parent: entityParent };
}
function extractSkills(ownBlocks, stream, ctx) {
    const toolBlocks = stream.byKind.get("skill-tool") ?? [];
    const items = [];
    const skipped = [];
    const diagnostics = [];
    for (const skillBlock of ownBlocks) {
        const skillLabel = skillBlock.label ?? "";
        const ownTools = toolBlocks.filter((b) => {
            const ref = b.attrs["skill_id"];
            return isTfExpr(ref) && ref.$expr === "resource-attr" && ref.type === SKILL_RESOURCE_TYPE && ref.label === skillLabel;
        });
        const { skill, skippedReason } = skillFromTf(skillBlock, ownTools, ctx.appTfLabel, ctx);
        if (skill !== undefined)
            items.push(skill);
        else if (skippedReason !== undefined) {
            skipped.push(`${SKILL_RESOURCE_TYPE}.${skillLabel}`);
            diagnostics.push(skippedReason);
        }
    }
    return { items, skipped, diagnostics };
}
export const skillModule = {
    kind: "skill",
    stream: { role: "streamMember" },
    authoring: { unit: "ownFile", bag: "skills" },
    parents: [
        { parent: "org", child: "skill" },
        { parent: "app", child: "skill" },
        { parent: "agent", child: "skill" },
    ],
    resource: { shape: "single", resourceType: SKILL_RESOURCE_TYPE, adoptable: true },
    // Foreign skills (an agent's skillAccess pointing at a skill owned by a
    // different app) emit `data.elementum_agentic_skill` — mirrors appModule.
    data: { participates: true, lookupAttr: "name", referencedByBuild: true },
    canvas: { participates: true, label: "Skills", glyph: "◇", component: "skillCard" },
    lifecycle: {
        participates: true,
        extractOwnFile: extractSkills,
        printOwnFile: (item, opts) => {
            const skill = item;
            return {
                refName: skill.refName,
                source: printSkillV2(skill, {
                    appRefName: opts.appRefName,
                    ...(opts.crossOrg !== undefined && { crossOrg: opts.crossOrg }),
                    ...(opts.foreignAppRefNames !== undefined && { foreignAppRefNames: opts.foreignAppRefNames }),
                    ...(opts.foreignElementRefNames !== undefined && { foreignElementRefNames: opts.foreignElementRefNames }),
                    ...(opts.foreignFieldRefsById !== undefined && { foreignFieldRefsById: opts.foreignFieldRefsById }),
                    ...(opts.foreignSearchTableRefsByName !== undefined && {
                        foreignSearchTableRefsByName: opts.foreignSearchTableRefsByName,
                    }),
                }),
            };
        },
        toTf: (entity, ctx) => skillToTf(entity, {
            appRefName: ctx.appRefName,
            appTfLabel: ctx.appTfLabel,
            orgRefs: ctx.orgRefs,
            workspaceAspects: ctx.workspaceAspects,
            serviceAccounts: ctx.streamRootEntity?.payload?.serviceAccounts,
            diagnostics: ctx.diagnostics,
            reports: ctx.reports,
            ...(ctx.siblingEntities !== undefined && { siblingEntities: ctx.siblingEntities }),
            ...(ctx.activeAutomationRefNames !== undefined && { activeAutomationRefNames: ctx.activeAutomationRefNames }),
        }),
    },
    catalog: {
        participates: true,
        refKind: "skill",
        catalogBag: "skills",
        renderOrder: 30,
        catalogRoot: "app",
        renderEntry: rootParentFactoryEntry("skillRef"),
        stubRef: skillStubRef,
        refFromAuthoring: skillRefFromAuthoring,
    },
};
