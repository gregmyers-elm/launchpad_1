/** The agent-tool entity's registry entry (see src/graph/). */
import { NO_CATALOG, NO_DATA, NO_LIFECYCLE, } from "../../graph/types.js";
import { AUTOMATION_RESOURCE_TYPE, WORKFLOW_PUBLISH_RESOURCE_TYPE, } from "../automation/types.js";
import { FIELD_DATA_RESOURCE_TYPE } from "../field/module.js";
import { AGENT_TOOL_SUB_TYPES, RUN_AUTOMATION_TOOL_RESOURCE_TYPE, } from "./types.js";
export const agentToolModule = {
    kind: "agent-tool",
    stream: { role: "streamMember" },
    authoring: { unit: "inline", bag: "tools" },
    parents: [{ parent: "agent", child: "agent-tool" }],
    resource: {
        shape: "subTypes",
        rows: AGENT_TOOL_SUB_TYPES.map((t) => ({
            key: t.typeKey,
            resourceType: t.resourceType,
            implemented: t.implemented === true,
        })),
    },
    data: NO_DATA,
    lifecycle: NO_LIFECYCLE,
    canvas: { participates: true, label: "Tools", glyph: "⚒", component: "toolCard" },
    catalog: NO_CATALOG,
    roundtrip: {
        referenceAliases: [
            {
                blockType: RUN_AUTOMATION_TOOL_RESOURCE_TYPE,
                attribute: "automation_id",
                fromType: WORKFLOW_PUBLISH_RESOURCE_TYPE,
                toType: AUTOMATION_RESOURCE_TYPE,
            },
        ],
        resolvedDataReferences: [{
                attribute: "fields",
                nestedAttribute: "field_id",
                dataType: FIELD_DATA_RESOURCE_TYPE,
            }],
    },
};
