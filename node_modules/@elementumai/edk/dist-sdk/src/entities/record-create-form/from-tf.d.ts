import { type FromTfContext } from "../../engine/pull/context.js";
import { type ChildResourceScope, type TfBlock } from "../../engine/tf/index.js";
import type { NormalizedRecordCreateForm } from "../app/deserializer/normalize.js";
export interface RecordCreateFormOwner {
    readonly type: string;
    readonly label: string;
    readonly ownerTfLabel: string;
    readonly scope: ChildResourceScope;
}
/** Provider resource -> id-free normalized create-form authoring. */
export declare function recordCreateFormFromTf(block: TfBlock | undefined, owner: RecordCreateFormOwner, ctx: FromTfContext): NormalizedRecordCreateForm | undefined;
