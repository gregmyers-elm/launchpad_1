/** The agent entity's registry entry + resource-type constant (see src/graph/). */
import { bareFactoryEntry } from "../../catalog/render-helpers.js";
import { isTfExpr } from "../../engine/tf/index.js";
import { printAgentV2 } from "./deserializer/print-agent-v2.js";
import { agentFromTf } from "./from-tf.js";
import { agentToTf } from "./to-tf.js";
export const AGENT_RESOURCE_TYPE = "elementum_agent";
function isConfigured(value) {
    return (typeof value === "object" &&
        value !== null &&
        typeof value.ref === "function" &&
        typeof value.def === "function");
}
function agentStubRef(refName, parent) {
    return { __kind: "agent", refName, name: refName, parent };
}
function agentRefFromAuthoring(args) {
    const { refName, parent, defaultExport } = args;
    if (isConfigured(defaultExport)) {
        const catalogRef = defaultExport.ref(refName);
        if (catalogRef.__kind !== "agent")
            return undefined;
        return catalogRef;
    }
    if (typeof defaultExport !== "object" ||
        defaultExport === null ||
        defaultExport.kind !== "agent") {
        return undefined;
    }
    const entity = defaultExport;
    const entityRefName = entity.refName === "" ? refName : entity.refName;
    const entityParent = entity.parent === "" ? parent : entity.parent;
    const maybeRefName = entity.ref?.name;
    const payloadName = entity.payload?.name;
    const name = typeof maybeRefName === "string"
        ? maybeRefName
        : typeof payloadName === "string"
            ? payloadName
            : entityRefName;
    return { __kind: "agent", refName: entityRefName, name, parent: entityParent };
}
function extractAgents(ownBlocks, stream, ctx) {
    const toolBlocks = stream.byKind.get("agent-tool") ?? [];
    const a2aSkillBlocks = stream.byKind.get("agent-a2a-skill") ?? [];
    const items = [];
    const skipped = [];
    const diagnostics = [];
    for (const agentBlock of ownBlocks) {
        const agentLabel = agentBlock.label ?? "";
        const ownsBlock = (b) => {
            const ref = b.attrs["agent_id"];
            return isTfExpr(ref) && ref.$expr === "resource-attr" && ref.type === AGENT_RESOURCE_TYPE && ref.label === agentLabel;
        };
        const ownTools = toolBlocks.filter(ownsBlock);
        const ownA2ASkills = a2aSkillBlocks.filter(ownsBlock);
        const { agent, skippedReason } = agentFromTf(agentBlock, ownTools, ctx.appTfLabel, ctx.appRefName, ctx, ownA2ASkills);
        if (agent !== undefined)
            items.push(agent);
        else if (skippedReason !== undefined) {
            skipped.push(`${AGENT_RESOURCE_TYPE}.${agentLabel}`);
            diagnostics.push(skippedReason);
        }
    }
    return { items, skipped, diagnostics };
}
export const agentModule = {
    kind: "agent",
    stream: { role: "streamMember" },
    authoring: { unit: "ownFile", bag: "agents" },
    parents: [{ parent: "app", child: "agent" }],
    resource: { shape: "single", resourceType: AGENT_RESOURCE_TYPE, adoptable: true },
    data: { participates: true, resourceType: AGENT_RESOURCE_TYPE, lookupAttr: "name", referencedByBuild: false },
    canvas: {
        participates: true,
        label: "Agents",
        glyph: "◇",
        component: "agentCard",
        toView: (instance) => {
            const a = instance;
            return {
                refName: a.refName,
                name: a.name,
                description: a.description,
                toolCount: a.tools.length,
            };
        },
    },
    lifecycle: {
        participates: true,
        extractOwnFile: extractAgents,
        printOwnFile: (item, opts) => {
            const agent = item;
            return {
                refName: agent.refName,
                source: printAgentV2(agent, {
                    appRefName: opts.appRefName,
                    ...(opts.crossOrg !== undefined && { crossOrg: opts.crossOrg }),
                    ...(opts.destinationConnectorKeys !== undefined && {
                        destinationConnectorKeys: opts.destinationConnectorKeys,
                    }),
                    ...(opts.connectorKeyOverride !== undefined && { connectorKeyOverride: opts.connectorKeyOverride }),
                    ...(opts.foreignAppRefNames !== undefined && { foreignAppRefNames: opts.foreignAppRefNames }),
                    ...(opts.foreignElementRefNames !== undefined && { foreignElementRefNames: opts.foreignElementRefNames }),
                    ...(opts.foreignFieldRefsById !== undefined && { foreignFieldRefsById: opts.foreignFieldRefsById }),
                    ...(opts.foreignSearchTableRefsByName !== undefined && {
                        foreignSearchTableRefsByName: opts.foreignSearchTableRefsByName,
                    }),
                }),
            };
        },
        toTf: (entity, ctx) => agentToTf(entity, {
            appRefName: ctx.appRefName,
            appTfLabel: ctx.appTfLabel,
            orgRefs: ctx.orgRefs,
            workspaceAspects: ctx.workspaceAspects,
            serviceAccounts: ctx.streamRootEntity?.payload?.serviceAccounts,
            diagnostics: ctx.diagnostics,
            ...(ctx.siblingEntities !== undefined && { siblingEntities: ctx.siblingEntities }),
            ...(ctx.activeAutomationRefNames !== undefined && { activeAutomationRefNames: ctx.activeAutomationRefNames }),
        }),
    },
    catalog: {
        participates: true,
        refKind: "agent",
        catalogBag: "agents",
        renderOrder: 20,
        catalogRoot: "app",
        renderEntry: ({ keyName, baseIndent }) => bareFactoryEntry("agentRef", keyName, baseIndent),
        stubRef: agentStubRef,
        refFromAuthoring: agentRefFromAuthoring,
    },
};
