/**
 * View extraction: `elementum_layout` / `elementum_list_view` /
 * `elementum_managed_view_order` resource blocks → the normalized
 * `layouts`/`listViews`/`viewOrder` entries the app printer consumes. The
 * owning app's from-tf drives this pass and says who the owner is — this
 * module never names the app's own resource type.
 */
import { type FromTfContext } from "../../engine/pull/context.js";
import { type ChildResourceScope, type TfBlock } from "../../engine/tf/index.js";
import type { NormalizedAgentView, NormalizedCalendarView, NormalizedDashboardView, NormalizedKanbanView, NormalizedLayout, NormalizedListView } from "../app/deserializer/normalize.js";
/** Report every attribute key not in `known` as dialect growth — shared by all four view/section passes below. */
export interface ViewOwner {
    /** The owner's resource type ("elementum_app") — what `object_id`/`aspect_id` must reference. */
    readonly type: string;
    /** The owner's tfLabel (namespace). */
    readonly label: string;
    /** Namespace tf label for stripping child label prefixes. */
    readonly ownerTfLabel: string;
    /** App vs element scope for prefix stripping. */
    readonly scope?: ChildResourceScope;
    /** Stage keys declared by the owning app, used when ei exports a raw stage UUID. */
    readonly stageKeys?: ReadonlySet<string>;
}
export interface ViewsFromTfResult {
    readonly layouts: readonly NormalizedLayout[];
    readonly listViews: readonly NormalizedListView[];
    readonly calendarViews: readonly NormalizedCalendarView[];
    readonly kanbanViews: readonly NormalizedKanbanView[];
    readonly dashboardViews: readonly NormalizedDashboardView[];
    readonly agentViews: readonly NormalizedAgentView[];
    readonly viewOrder?: readonly string[];
}
/** All three view resource kinds → the normalized shapes the app printer consumes. */
export declare function viewsFromTf(layoutBlocks: readonly TfBlock[], listViewBlocks: readonly TfBlock[], calendarViewBlocks: readonly TfBlock[], kanbanViewBlocks: readonly TfBlock[], dashboardViewBlocks: readonly TfBlock[], agentViewBlocks: readonly TfBlock[], managedViewOrderBlocks: readonly TfBlock[], owner: ViewOwner, ctx: FromTfContext): ViewsFromTfResult;
