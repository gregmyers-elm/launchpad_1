import type { FieldRef } from "../../catalog/types.js";
import type { UnsafePlatformId } from "../../core/platform-id.js";
import type { SystemFieldKey } from "../../core/system-fields.js";
/** Sort directions accepted by every managed-view resource. */
export declare const managedViewSortDirection: {
    readonly ascending: "ASC";
    readonly descending: "DESC";
};
export type ManagedViewSortDirection = (typeof managedViewSortDirection)[keyof typeof managedViewSortDirection];
/** Optional aggregations accepted by a managed-view sort order. */
export declare const managedViewSortAggregation: {
    readonly average: "AVERAGE";
    readonly count: "COUNT";
    readonly maximum: "MAX";
    readonly minimum: "MIN";
    readonly sum: "SUM";
};
export type ManagedViewSortAggregation = (typeof managedViewSortAggregation)[keyof typeof managedViewSortAggregation];
/** A custom/system field to order by, or an explicit non-portable id escape hatch. */
export type ManagedViewSortField = FieldRef | SystemFieldKey | UnsafePlatformId;
/** One order in the platform's managed-view Sort contract. */
export interface ManagedViewSortOrder {
    readonly field: ManagedViewSortField;
    readonly aggregation?: ManagedViewSortAggregation | null;
    readonly direction: ManagedViewSortDirection;
}
/** The provider's JSON-encoded managed-view Sort contract. */
export interface ManagedViewSort {
    readonly orders: readonly ManagedViewSortOrder[];
}
/** Pull-time projection after Terraform field references become source bag keys. */
export interface NormalizedManagedViewSortOrder {
    readonly field: string;
    readonly aggregation?: ManagedViewSortAggregation | null;
    readonly direction: ManagedViewSortDirection;
}
export interface NormalizedManagedViewSort {
    readonly orders: readonly NormalizedManagedViewSortOrder[];
}
/** Row-density values accepted by a managed list view. */
export declare const listViewDensity: {
    readonly comfortable: "COMFORTABLE";
    readonly compact: "COMPACT";
    readonly standard: "STANDARD";
};
export type ListViewDensity = (typeof listViewDensity)[keyof typeof listViewDensity];
