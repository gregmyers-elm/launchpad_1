import { bareFactoryEntry } from "../../catalog/render-helpers.js";
import { NO_CANVAS } from "../../graph/types.js";
import { chartFromTf } from "./from-tf.js";
import { printChart } from "./printer.js";
import { chartToTf } from "./to-tf.js";
import { CHART_RESOURCE_TYPE } from "./types.js";
export { CHART_RESOURCE_TYPE } from "./types.js";
function chartStubRef(refName, parent) {
    return { __kind: "chart", refName, name: refName, parent };
}
function chartRefFromAuthoring(args) {
    const configured = args.defaultExport;
    return typeof configured?.ref === "function" ? configured.ref(args.refName) : undefined;
}
export const chartModule = {
    kind: "chart",
    stream: { role: "streamMember" },
    authoring: { unit: "ownFile", bag: "charts" },
    parents: [{ parent: "app", child: "chart" }],
    resource: { shape: "single", resourceType: CHART_RESOURCE_TYPE, adoptable: true },
    data: { participates: true, resourceType: CHART_RESOURCE_TYPE, lookupAttr: "id", referencedByBuild: false },
    lifecycle: {
        participates: true,
        extractOwnFile: (ownBlocks, _stream, ctx) => {
            const items = [];
            const skipped = [];
            for (const block of ownBlocks) {
                const chart = chartFromTf(block, { appLabel: ctx.appTfLabel, appRefName: ctx.appRefName }, ctx);
                if (chart !== undefined)
                    items.push(chart);
                else if (block.label !== undefined)
                    skipped.push(`${CHART_RESOURCE_TYPE}.${block.label}`);
            }
            return { items, skipped, diagnostics: [] };
        },
        printOwnFile: (item, opts) => ({
            refName: item.refName,
            source: printChart(item, { appRefName: opts.appRefName, orgImportPath: opts.orgImportPath }),
        }),
        toTf: (entity, ctx) => chartToTf(entity, {
            appRefName: ctx.appRefName,
            appTfLabel: ctx.appTfLabel,
            orgRefs: ctx.orgRefs,
            workspaceAspects: ctx.workspaceAspects,
            streamRootEntity: ctx.streamRootEntity,
            diagnostics: ctx.diagnostics,
        }),
    },
    canvas: NO_CANVAS,
    catalog: {
        participates: true,
        refKind: "chart",
        catalogBag: "charts",
        renderOrder: 66,
        catalogRoot: "app",
        factoryName: "chartRef",
        renderEntry: ({ keyName, baseIndent }) => bareFactoryEntry("chartRef", keyName, baseIndent),
        stubRef: chartStubRef,
        refFromAuthoring: chartRefFromAuthoring,
    },
};
