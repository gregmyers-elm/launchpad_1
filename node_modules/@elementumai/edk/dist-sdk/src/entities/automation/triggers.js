import { serializeOnDemandTrigger, } from "../../core/triggers/on-demand.js";
import { chainableTrigger } from "./build.js";
function recordTriggerFields(opts) {
    return {
        aspectId: opts.app, // token → aspect id at deploy
        ...(opts.changedFields !== undefined ? { changedFieldIds: opts.changedFields } : {}),
        ...(opts.thinkUi !== undefined ? { thinkUi: opts.thinkUi } : {}),
        ...(opts.filter !== undefined ? { filter: opts.filter } : {}),
    };
}
/** Fires when a record is created on the given aspect. */
export function recordCreated(opts) {
    return chainableTrigger({ wire: { createRecord: recordTriggerFields(opts) }, __recordApp: opts.app.refName });
}
/** Fires when a record is updated on the given aspect. */
export function recordUpdated(opts) {
    return chainableTrigger({ wire: { updateRecord: recordTriggerFields(opts) }, __recordApp: opts.app.refName });
}
function buildRrule(opts) {
    const parts = [`FREQ=${opts.frequency}`];
    const interval = opts.interval ?? 1;
    if (interval > 1) {
        parts.push(`INTERVAL=${interval}`);
    }
    if (opts.frequency === "WEEKLY" && opts.weekdays && opts.weekdays.length > 0) {
        const map = {
            MONDAY: "MO",
            TUESDAY: "TU",
            WEDNESDAY: "WE",
            THURSDAY: "TH",
            FRIDAY: "FR",
            SATURDAY: "SA",
            SUNDAY: "SU",
        };
        const byday = opts.weekdays
            .map((w) => map[w.toUpperCase()])
            .filter(Boolean)
            .join(",");
        if (byday) {
            parts.push(`BYDAY=${byday}`);
        }
    }
    else if (opts.frequency === "MONTHLY" && opts.dayOfMonth !== undefined) {
        parts.push(`BYMONTHDAY=${opts.dayOfMonth}`);
    }
    return parts.join(";");
}
/** Runs an automation on a recurring schedule. */
export function schedule(opts) {
    const rrule = buildRrule(opts);
    return chainableTrigger({
        wire: {
            schedule: {
                rrule,
                dtStart: opts.startAt,
                startAt: opts.startAt,
                timezone: opts.timezone,
                frequency: opts.frequency,
                interval: opts.interval ?? 1,
                type: "SCHEDULE",
                ...(opts.frequency === "WEEKLY" && opts.weekdays !== undefined ? { weekdays: opts.weekdays } : {}),
                ...(opts.frequency === "MONTHLY" && opts.dayOfMonth !== undefined ? { dayOfMonth: opts.dayOfMonth } : {}),
            },
        },
    });
}
export function onDemand(schema, options) {
    const normalized = Object.fromEntries(Object.entries(schema).map(([k, v]) => [k, typeof v === "string" ? { type: v } : v]));
    return chainableTrigger({
        wire: serializeOnDemandTrigger(normalized, options),
        __params: Object.entries(normalized).map(([name, def]) => ({ name, required: def.required ?? false })),
    });
}
/** Fires on an inbound HTTP webhook. */
export function webHook(opts = {}) {
    return chainableTrigger({
        wire: {
            webhook: {
                // authenticated is schema-Optional but NOT Computed
                // (webhook_trigger_resource.go) - omitting it from generated
                // config when false reads to OpenTofu as "unset this" rather than
                // "leave at the live false", diffing forever (edk#557, same defect
                // class as the approval-chain approveButton fix, edk#325). Always
                // emit it, defaulting to the platform's real false.
                authenticated: opts.authenticated ?? false,
                ...(opts.customResponse !== undefined ? { customResponse: opts.customResponse } : {}),
            },
        },
    });
}
/** @deprecated Use {@link webHook}. */
export function webhook(opts = {}) {
    return webHook(opts);
}
/** Fires relative to a date/datetime field on records in the given aspect. */
export function temporal(opts) {
    return chainableTrigger({
        wire: {
            temporal: {
                aspectId: opts.app,
                aspectFieldId: opts.field,
                temporalUnit: opts.temporalUnit,
                value: opts.value,
                type: "TEMPORAL",
            },
        },
        __recordApp: opts.app.refName,
    });
}
/** Fires when an approval chain on a record changes to the configured status. */
export function approvalProcessStatusUpdated(opts) {
    return chainableTrigger({
        wire: {
            approvalChain: {
                aspectId: opts.app,
                ...(opts.approvalChain !== undefined ? { approvalChainTemplateId: opts.approvalChain } : {}),
                status: opts.status,
                type: "APPROVAL_CHAIN_UPDATE",
            },
        },
        __recordApp: opts.app.refName,
    });
}
/** Fires when an attachment is added to a record on the given aspect. */
export function attachment(opts) {
    return chainableTrigger({
        wire: {
            attachment: {
                aspectId: opts.app,
                type: "ATTACHMENT_UPDATE",
                ...(opts.filter !== undefined ? { filter: opts.filter } : {}),
                ...(opts.thinkUi !== undefined ? { thinkUi: opts.thinkUi } : {}),
            },
        },
        __recordApp: opts.app.refName,
    });
}
/** Fires when a data mine meets/no longer meets requirements. */
export function dataMine(opts) {
    return chainableTrigger({
        wire: {
            datamine: {
                datamineId: opts.datamine,
                type: "DATAMINE",
                ...(opts.fireOnAlert !== undefined ? { fireOnAlert: opts.fireOnAlert } : {}),
                ...(opts.fireOnRecovery !== undefined ? { fireOnRecovery: opts.fireOnRecovery } : {}),
            },
        },
    });
}
/** Fires when an email is received by the platform email alias. */
export function emailReceived(opts) {
    return chainableTrigger({
        wire: {
            emailReceived: {
                emailAliasId: opts.emailAliasId,
                type: "EMAIL_INGESTION",
            },
        },
    });
}
/** Fires when a comment is added to a record on the given aspect. */
export function commentAdded(opts) {
    return chainableTrigger({
        wire: {
            commentAdded: {
                aspectId: opts.app,
                conversationType: opts.conversationType,
                ...(opts.filter !== undefined ? { filter: opts.filter } : {}),
            },
        },
        __recordApp: opts.app.refName,
    });
}
/** Fires when a conversation of the selected type ends on the given aspect. */
export function conversationEnd(opts) {
    return chainableTrigger({
        wire: {
            conversationEnd: {
                aspectId: opts.app,
                conversationType: opts.conversationType,
                type: "CONVERSATION_END",
            },
        },
    });
}
/** Fires when a Slack message is received. */
export function slackMessage() {
    return chainableTrigger({
        wire: {
            slackMessage: {
                type: "SLACK_MESSAGE",
            },
        },
    });
}
/** Fires when a response is created for the selected survey template. */
export function surveyResponseCreate(opts) {
    return chainableTrigger({
        wire: {
            survey: {
                aspectId: opts.app,
                surveyTemplateId: opts.survey,
                type: "SURVEY_RESPONSE_CREATE",
            },
        },
    });
}
