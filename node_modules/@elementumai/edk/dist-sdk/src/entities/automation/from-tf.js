import { USER_FIELD_KEYS } from "../../catalog/types.js";
import { DEPLOY_DEFAULT_SYSTEM_FIELD_NAMES, SYSTEM_FIELD_KEYS } from "../../core/system-fields.js";
import { EntityTfError } from "../../engine/entity.js";
import { procedureDiscriminator } from "../../engine/pull/procedure-refs.js";
import { childPartFromScopedLabel, isTfExpr, sanitizeName, scopedLabelToBagKey, tfLabelToTsLabel, tsLabelToTfLabel, } from "../../engine/tf/index.js";
import { SUB_TYPE_BY_RESOURCE } from "../field/types.js";
import { IMPLEMENTED_FILE_READER_RESOURCE_TYPES } from "../file-reader/types.js";
import { connectorIdentityFromTf } from "../shared/connector-from-tf.js";
import { fieldTypeToValueType } from "../shared/filters/index.js";
import { UNRESOLVED_FIELD_MARKER } from "../shared/ref.js";
import { filterFromTf as deserializeFilterFromTf } from "../shared/filters/tf-codec.js";
import { TABLE_RESOURCE_TYPE } from "../table/module.js";
import { SEARCH_TABLE_RESOURCE_TYPES } from "../search-table/types.js";
import { outputFieldTypesFromScriptSchema } from "./output-field-types.js";
import { apiTaskRefAuthoringPath, OUTPUT_KEY_WIRE_NAME } from "./output-key-wire-name.js";
import { taskTfLabel } from "./task-label.js";
import { ALL_AUTOMATION_TASK_TYPE_SET, AUTOMATION_BRANCH_TYPE_SET, AUTOMATION_RESOURCE_TYPE, AUTOMATION_TRIGGER_TYPE_SET, WORKFLOW_PUBLISH_RESOURCE_TYPE, } from "./types.js";
const TRIGGER_KINDS = {
    elementum_record_created_trigger: "recordCreate",
    elementum_record_updated_trigger: "recordUpdate",
    elementum_attachment_added_trigger: "attachment",
    elementum_comment_added_trigger: "commentAdded",
    elementum_agent_conversation_ended_trigger: "conversationEnd",
    elementum_slack_message_trigger: "slackMessage",
    elementum_on_demand_trigger: "onDemand",
    elementum_webhook_trigger: "webhook",
    elementum_email_ingestion_trigger: "emailReceived",
    elementum_approval_chain_trigger: "approvalProcessStatusUpdated",
    elementum_datamine_trigger: "dataMine",
    elementum_survey_trigger: "surveyResponseCreate",
    elementum_schedule_trigger: "schedule",
};
const TASK_FACTORIES = {
    elementum_message_task: "postComment",
    elementum_update_field_task: "updateField",
    elementum_variable_task: "setVariable",
    elementum_update_variable_task: "setVariable",
    elementum_create_record_task: "createRecord",
    elementum_record_search_task: "recordSearch",
    elementum_table_records_search_task: "tableRecordsSearch",
    elementum_send_email_task: "sendEmail",
    elementum_initiate_call_task: "initiateCall",
    elementum_approval_chain_task: "approvalChain",
    elementum_approval_status_update_task: "approvalStatusUpdate",
    elementum_find_related_records_task: "findRelatedRecords",
    elementum_api_task: "apiRequest",
    elementum_ai_agent_task: "aiAgent",
    elementum_for_each_task: "forEach",
    elementum_switch_task: "switchTask",
    elementum_fork_join_task: "forkJoin",
    elementum_add_watcher_task: "addWatcher",
    elementum_notification_task: "notification",
    elementum_relate_records_task: "relateRecords",
    elementum_ai_classify_task: "aiClassify",
    elementum_ai_summarize_task: "aiSummarize",
    elementum_ai_search_table_task: "aiSearchTable",
    elementum_ai_transform_task: "aiTransform",
    elementum_save_attachment_task: "saveAttachment",
    elementum_user_search_task: "userSearch",
    elementum_calculation_task: "calculation",
    elementum_execute_script_task: "executeScript",
    elementum_file_reader_task: "documentModel",
    elementum_procedure_task: "procedure",
    elementum_run_automation_task: "runAutomation",
    elementum_aspect_record_field_locking_task: "recordFieldLocking",
    elementum_ai_file_read_task: "aiFileAnalysis",
    // Not a real task resource - a read-only reference to a platform task type
    // EDK doesn't implement (edk#785/edk#821). Synthesized into the same tree
    // walk as a real task (see buildTasks) so its downstream chain lands in its
    // real position instead of being silently hoisted to the automation's top level.
    elementum_unmanaged_task_reference: "unimplementedTask",
};
const ATTR_TO_ARG = {
    object_id: "app",
    table_id: "table",
    record_reference: "recordRef",
    source_record_ids: "sources",
    target_record_id: "target",
    message_body: "body",
    from_username: "fromUsername",
    from_personal: "fromDisplayName",
    external_emails: "toAddresses",
    users: "toUsers",
    groups: "toGroups",
    contents: "contents",
    message: "contents",
    agent_id: "agent",
    prompt: "prompt",
    output_type: "outputType",
    json_fields: "schema",
    ai_provider_connector_id: "connector",
    url: "url",
    response_type: "responseType",
    continue_on_error: "continueOnError",
    search_table_id: "table",
    phone_service_id: "phoneService",
    text_to_summarize: "text",
    text_to_classify: "text",
    field_type: "fieldType",
    category_source: "categorySource",
    user_ids: "userIds",
    group_ids: "groupIds",
    notify_watchers: "notifyWatchers",
    fields_to_lock: "fieldsToLock",
    fields_to_unlock: "fieldsToUnlock",
    view_state: "viewState",
    target_automation_id: "automation",
    target_automation_ref: "automation",
    dynamic_input_mappings: "inputs",
    output_mappings: "outputs",
    file_reader_id: "documentModel",
    file_id: "attachmentId",
    cloudlink_id: "cloudLinkId",
    // provider schema calls this stored_function_id; the authoring option
    // stayed "procedureId" (pre-dating the provider's rename) to avoid an
    // unnecessary breaking change to ProcedureOptions.
    stored_function_id: "procedureId",
};
function isObject(value) {
    return value !== null && typeof value === "object" && !Array.isArray(value) && !("$expr" in value);
}
/**
 * Detects a switch task's default/catch-all branch and which of the two
 * mutually-exclusive wire shapes the platform is currently using to mark it.
 * Both are live-confirmed on real orgs for the identical case pattern (#157:
 * one org's default case carries `is_default = true` with no `filter`;
 * another's carries `filter = {type:"true"}` with no `is_default`) - the
 * platform doesn't retroactively normalize existing cases to one shape, so
 * this is a genuine per-case fact, not just export-version drift. Returning
 * which shape was found (not just a boolean) lets to-tf.ts round-trip the
 * same shape back on rebuild instead of guessing, so a re-pull doesn't
 * propose a spurious "update" that provider bug #157 can't actually apply.
 */
function defaultSwitchShape(block) {
    if (block.attrs.is_default === true)
        return "isDefault";
    const filter = block.attrs.filter;
    if (block.label?.endsWith("_default") === true &&
        block.attrs.label === "Default" &&
        isObject(filter) &&
        filter.type === "true") {
        return "explicitTrueFilter";
    }
    return undefined;
}
function resourceRef(value) {
    return isTfExpr(value) && value.$expr === "resource-attr"
        ? { type: value.type, label: value.label, path: value.path }
        : undefined;
}
function references(value, type, label) {
    const ref = resourceRef(value);
    return ref?.type === type && ref.label === label;
}
/**
 * edk#785: a task's `parent` can be a `data.elementum_unmanaged_task_reference.*`
 * reference instead of a resource reference, when its real predecessor is a
 * task type EDK doesn't support at all (no HCL block ever emitted for it).
 * The data source is read-only and inherently "resolved" (its id/workflow_id/
 * etc. are always concrete inputs, never dependent on anything else in the
 * graph), so a task pointing at one is never itself unowned/orphaned - it
 * just has no real branch/case address to anchor its authored position under.
 */
function unmanagedTaskRef(value) {
    return isTfExpr(value) && value.$expr === "data-attr" && value.type === "elementum_unmanaged_task_reference"
        ? { label: value.label }
        : undefined;
}
/**
 * Order switch_case / fork_join_branch blocks by their previous_*_id chain.
 * Falls back to depends_on index / label only when the chain is incomplete.
 * Alphabetical label order inverts real evaluation order whenever a later
 * case's display name sorts before the first (Sanofi: "…Invalid" before
 * "…Valid"), which rebuilds previous_case_id edges backwards and plans
 * in-place updates on every case.
 */
function orderBranchesByPreviousLink(matching, previousAttr, dependencyOrder) {
    if (matching.length <= 1)
        return matching;
    const byAddress = new Map(matching.map((branch) => [`${branch.type}.${branch.label}`, branch]));
    const successor = new Map();
    const roots = [];
    for (const branch of matching) {
        const prev = resourceRef(branch.attrs[previousAttr]);
        if (prev === undefined) {
            roots.push(branch);
            continue;
        }
        const prevAddress = `${prev.type}.${prev.label}`;
        if (!byAddress.has(prevAddress)) {
            roots.push(branch);
            continue;
        }
        if (successor.has(prevAddress)) {
            // Ambiguous chain — fall back to legacy sort for the whole set.
            return [...matching].sort((a, b) => (dependencyOrder.get(`${a.type}.${a.label}`) ?? Number.MAX_SAFE_INTEGER)
                - (dependencyOrder.get(`${b.type}.${b.label}`) ?? Number.MAX_SAFE_INTEGER)
                || a.label.localeCompare(b.label));
        }
        successor.set(prevAddress, branch);
    }
    if (roots.length !== 1) {
        return [...matching].sort((a, b) => (dependencyOrder.get(`${a.type}.${a.label}`) ?? Number.MAX_SAFE_INTEGER)
            - (dependencyOrder.get(`${b.type}.${b.label}`) ?? Number.MAX_SAFE_INTEGER)
            || a.label.localeCompare(b.label));
    }
    const ordered = [];
    let cursor = roots[0];
    const seen = new Set();
    while (cursor !== undefined) {
        const address = `${cursor.type}.${cursor.label}`;
        if (seen.has(address))
            break;
        seen.add(address);
        ordered.push(cursor);
        cursor = successor.get(address);
    }
    if (ordered.length !== matching.length) {
        return [...matching].sort((a, b) => (dependencyOrder.get(`${a.type}.${a.label}`) ?? Number.MAX_SAFE_INTEGER)
            - (dependencyOrder.get(`${b.type}.${b.label}`) ?? Number.MAX_SAFE_INTEGER)
            || a.label.localeCompare(b.label));
    }
    return ordered;
}
function assertAllowed(block, allowed) {
    for (const key of Object.keys(block.attrs)) {
        if (!allowed.has(key))
            throw new EntityTfError(`${block.type}.${block.label}: unsupported attribute "${key}"`);
    }
}
const TRIGGER_ATTRIBUTES = new Set([
    "automation", "filter", "changed_fields", "think_ui", "conversation_type",
    "authenticated", "custom_response", "show_triggered_by", "parameters",
    "approval_chain_template_id", "status", "datamine_id", "fire_on_alert",
    "fire_on_recovery", "email_alias_id", "survey_template_id",
    "frequency", "interval", "timezone", "start_at", "weekdays", "day_of_month",
    "end_at", "ends_after_count", "interval_unit",
]);
const TASK_ATTRIBUTES = new Set([
    "parent", "name", "depends_on", "filter", "fields", "inputs", "output_schema",
    "list", "code", "variable_name", "variable_type", "variable_id", "variable", "value",
    "object_id", "table_id", "record_reference", "record_id", "source_record_ids",
    "approval_process_id", "requester_reference", "approver_references", "reason", "summary",
    "approval_reference", "status",
    "target_record_id", "message", "message_body", "body", "from_username",
    "from_personal", "external_emails", "users", "groups", "attachments", "contents",
    "agent_id", "prompt", "output_type", "json_fields", "ai_provider_connector_id",
    "url", "response_type", "continue_on_error", "search_table_id",
    "text_to_summarize", "text_to_classify", "field_type", "category_source",
    "title", "user_ids", "group_ids", "notify_watchers", "field_id", "user_id", "group_id",
    "fields_to_lock", "fields_to_unlock", "file", "file_id", "attachment", "attachment_id", "view_state", "target_automation_id",
    "target_automation_ref", "input_mappings", "dynamic_input_mappings",
    "output_mappings", "file_reader_id", "raw_text", "procedure_id",
    "calculations", "categories", "limit", "sort", "tag", "tags", "context", "length",
    "subject", "phone_service_id", "phone_number", "default_language", "first_message",
    "synchronous", "cloudlink_id", "stored_function_id", "variable_reference",
    "relate_to_record_reference", "method", "parameters", "headers",
    "basic_auth", "bearer_auth", "oauth", "json_body", "form_body", "custom_body", "multipart_body",
    "query",
]);
const BRANCH_ATTRIBUTES = new Set([
    "switch_task", "fork_join_task", "label", "is_default", "filter",
    "previous_case_id", "previous_branch_id",
]);
const PUBLISH_ATTRIBUTES = new Set([
    "automation", "workflow_revision", "depends_on", "outputs",
]);
function appRef(appRefName) {
    return { kind: "app", appHandle: appRefName };
}
// Fixed, well-known ref keys ONLY - keys derived from a field's display name
// plus a suffix (e.g. "Created By" + " Email" -> "Created By Email") must NOT
// be added here. A hardcoded flat entry here short-circuits triggerRefPath's
// field-name+suffix matching below (pathNames runs first), producing an
// invalid, un-typed property name instead of the real ["record", field.key,
// "email"] path - confirmed live: "Created By Email"/"Updated By Manager
// Email" were hardcoded this way and printed as ctx.trigger.createdByEmail /
// ctx.trigger.updatedByManagerEmail, neither of which exists on the typed
// ctx, so every pulled automation using them failed `tsc --noEmit`.
const WIRE_REF_SEGMENT_TO_TS = {
    "Changed Fields": "changedFieldIds",
    "Iteration": "iteration",
    "Slack Thread ID": "slackThreadId",
    "Slack Channel ID": "slackChannelId",
    "Message Author Email": "messageAuthorEmail",
    "Message Author ID": "messageAuthorId",
    "Message Author Name": "messageAuthorName",
    "Message Content": "messageContent",
    "Message ID": "messageId",
    "Message Metadata": "messageMetadata",
};
const FIELD_REF_SUFFIX_FROM_WIRE = [
    [" Manager Email", ["employee", "manager", "email"]],
    [" Manager Name", ["employee", "manager", "name"]],
    [" Manager", ["employee", "manager"]],
    [" Field ID", ["fieldId"]],
    // Dynamically sourced Dropdown selected source record. Confirmed
    // live as "<Field> - Record" with type ASPECT_RECORD_ID.
    [" - Record", ["recordId"]],
    // Deliberately the literal suffix text, not a camelCase "asText" segment:
    // Ref<T> has no ".asText" projection anywhere in the type system (checked
    // shared/ref.ts - it's a sealed brand, nothing else), so claiming one as a
    // resolved identifier printed a property that could never type-check. The
    // space keeps this segment identifier-unsafe, so printPathAccess's
    // unresolved-segment escape (`as any` + bracket access) kicks in instead of
    // printing a false, never-valid `.asText` access.
    [" as text", ["as text"]],
    [" Label", ["label"]],
    [" Email", ["email"]],
    [" Name", ["name"]],
    [" ID", ["id"]],
    ["", []],
];
function pathNames(path) {
    return path.map((segment) => {
        const name = typeof segment === "string" ? segment : segment.index;
        return WIRE_REF_SEGMENT_TO_TS[name] ?? name;
    });
}
/**
 * Resolve one platform field ref name (for example `"Facility"` or
 * `"Created By Email"`) to the typed record path exposed by the public
 * automation context. Shared by record-bearing trigger refs and
 * create-record task outputs; both use the same display-name + suffix wire
 * convention.
 */
function recordFieldRefPath(wireName, ctx, ownerRefName = ctx.appRefName, rootName = "record") {
    // Case-insensitive so exports remain readable across historical platform
    // casing while authored HCL targets the current exact live ref keys.
    const wireNameLower = wireName.toLowerCase();
    for (const [suffix, suffixPath] of FIELD_REF_SUFFIX_FROM_WIRE) {
        if (!wireNameLower.endsWith(suffix.toLowerCase()))
            continue;
        const base = wireName.slice(0, wireName.length - suffix.length);
        const fieldKey = lookupFieldKeyByDisplayName(ownerRefName, base, ctx);
        if (fieldKey !== undefined) {
            return [rootName, fieldKey, ...suffixPath];
        }
    }
    // System fields have no `*_field` resource block, so they never appear in
    // fieldById. Match their provider-default display names explicitly.
    for (const key of SYSTEM_FIELD_KEYS) {
        const defaultName = DEPLOY_DEFAULT_SYSTEM_FIELD_NAMES[key];
        for (const [suffix, suffixPath] of FIELD_REF_SUFFIX_FROM_WIRE) {
            if (wireNameLower === `${defaultName}${suffix}`.toLowerCase()) {
                return [rootName, key, ...suffixPath];
            }
        }
    }
    return undefined;
}
const RECORD_BEARING_TRIGGER_TYPES = new Set([
    "elementum_record_created_trigger",
    "elementum_record_updated_trigger",
    "elementum_attachment_added_trigger",
    "elementum_comment_added_trigger",
    "elementum_approval_chain_trigger",
    // On-demand workflows can also be launched with record context. Declared
    // parameters take precedence below so a parameter named like a field does
    // not get misclassified as `ctx.trigger.record.<field>`.
    "elementum_on_demand_trigger",
]);
function isDeclaredOnDemandParameter(trigger, wireName) {
    if (trigger.type !== "elementum_on_demand_trigger")
        return false;
    const parameters = trigger.attrs.parameters;
    return Array.isArray(parameters) && parameters.some((parameter) => isObject(parameter) && parameter.name === wireName);
}
function onDemandRecordParameters(trigger) {
    if (trigger.type !== "elementum_on_demand_trigger" || !Array.isArray(trigger.attrs.parameters))
        return [];
    return trigger.attrs.parameters.filter((parameter) => isObject(parameter) && parameter.type === "record" && typeof parameter.name === "string");
}
function prefixedOnDemandRecordRefPath(wireName, ctx) {
    const recordParams = onDemandRecordParameters(ctx.trigger);
    if (recordParams.length < 2)
        return undefined;
    const sortedParams = [...recordParams].sort((a, b) => String(b.name).length - String(a.name).length);
    for (const parameter of sortedParams) {
        const name = parameter.name;
        const prefix = `${name} - `;
        if (!wireName.startsWith(prefix))
            continue;
        const displayName = wireName.slice(prefix.length);
        if (displayName === "Record")
            return [name];
        const ownerRefName = aspectOwnerRefNameFromObjectId(parameter.default_value, ctx.appRefName, ctx.appTfLabel, ctx.dataBlocks, ctx.foreignAppRefNames, ctx.foreignElementRefNames);
        return ownerRefName === undefined
            ? undefined
            : recordFieldRefPath(displayName, ctx, ownerRefName, name);
    }
    return undefined;
}
// recordSearch's own output ref names ("Records Size" -> totalCount, "All
// found records" -> records) — deliberately NOT in WIRE_REF_SEGMENT_TO_TS,
// which triggerRefPath also consults: a record-created/updated trigger's ref
// path is built from a FIELD's DISPLAY NAME, and a real field could
// coincidentally be named "Records Size" — translating it globally would
// misdecode that trigger ref as the unrelated task-output name "totalCount"
// before triggerRefPath's field-name matching even runs. Scoped to
// recordSearch task refs only, where these two strings are the platform's
// fixed wire names, never a field's own display name.
const RECORD_SEARCH_TASK_REF_SEGMENT_TO_TS = {
    "Records Size": "totalCount",
    "All found records": "records",
    Record: "record",
};
/**
 * The same outputs as seen from a CALCULATION formula, which addresses a
 * reference by its WorkflowTaskReference name rather than the refs-map
 * display key above (`to-tf.ts`'s `CALCULATION_TASK_OUTPUT_REF_NAME` is the
 * write side). Only the names that differ from their authoring path are
 * listed; the rest already read back correctly.
 */
const RECORD_SEARCH_CALCULATION_REF_NAME_TO_TS = {
    count: ["totalCount"],
    recordId: ["record", "recordId"],
};
// aiSearchTable's own output ref name — same scoping reason as
// RECORD_SEARCH_TASK_REF_SEGMENT_TO_TS: "All Records" is a platform-fixed
// wire key on elementum_ai_search_table_task only. A trigger field could
// coincidentally share that display name and must not be rewritten here.
const AI_SEARCH_TABLE_TASK_REF_SEGMENT_TO_TS = {
    "All Records": "records",
};
// tableRecordsSearch collection output — platform wire key
// "All matching table rows" (not recordSearch's "All found records" or
// aiSearchTable's "All Records"). Leaving it unmapped prints
// `(ctx.actions.x as any)["All matching table rows"]`, which drops the Ref
// proxy at build time and omits the script/filter input entirely.
const TABLE_RECORDS_SEARCH_TASK_REF_SEGMENT_TO_TS = {
    "All matching table rows": "records",
};
/**
 * Restore the public aiAgent `.result` shape from either a provider refs-map
 * key (`Action Response` / authored schema field) or the nested platform
 * taskReference.name carried by workflow publish envelopes.
 */
function aiAgentTaskRefPath(block, name) {
    if (block.type !== "elementum_ai_agent_task")
        return undefined;
    if (name === "Action Response" || name === "ai_agent_task_response")
        return ["result"];
    const nestedPrefix = "ai_agent_task_structured_output.";
    const candidate = name.startsWith(nestedPrefix) ? name.slice(nestedPrefix.length) : name;
    const jsonFields = block.attrs.json_fields;
    if (!Array.isArray(jsonFields))
        return undefined;
    const schemaName = jsonFields
        .flatMap((field) => isObject(field) && typeof field.name === "string" ? [field.name] : [])
        .find((fieldName) => (fieldName === candidate || fieldName.toLowerCase() === candidate.toLowerCase()));
    return schemaName === undefined ? undefined : ["result", schemaName];
}
/**
 * A webhook trigger's `body`/`headers` refs come back as "Body (Plain
 * Text)"/"Headers (Content-Type)" - one wire segment naming both the
 * top-level property (always "body"/"headers", both typed
 * `Record<string, unknown>` on WebhookTriggerOutput) and an arbitrary
 * sub-key. Splitting it lets the sub-key print as a plain bracket access on
 * an already string-indexable type, instead of a false top-level property
 * like `ctx.trigger["Body (Plain Text)"]` that WebhookTriggerOutput never
 * declares. Gated on the trigger's actual resource type, not just the
 * string shape - a different trigger's field could otherwise coincidentally
 * be named "Body (something)" and get misinterpreted as this webhook-only
 * convention.
 */
const WEBHOOK_REF_PATTERN = /^(Body|Headers) \((.+)\)$/;
function aspectOwnerRefNameFromObjectId(objectId, appRefName, appTfLabel, dataBlocks, foreignAppRefNames, foreignElementRefNames) {
    if (!isTfExpr(objectId))
        return undefined;
    if (objectId.$expr === "data-attr" && (objectId.type === "elementum_app" || objectId.type === "elementum_element") && objectId.path[0] === "id") {
        // The data block's own tf label is an arbitrary export alias, not
        // necessarily the target's real refName (confirmed live: a
        // `data.elementum_element "approvals_log"` block's real namespace is
        // "approvals") - resolve through its name/namespace, same as decodeValue's
        // generic app/element data-attr case, falling back to the label only when
        // the target hasn't itself been pulled yet.
        const lookup = dataBlocks.get(`${objectId.type}.${objectId.label}`);
        const identity = lookup?.attrs.name ?? lookup?.attrs.namespace;
        const mapped = typeof identity === "string"
            ? (objectId.type === "elementum_element" ? foreignElementRefNames : foreignAppRefNames)?.get(identity)
            : undefined;
        return mapped ?? tfLabelToTsLabel(objectId.label);
    }
    if (objectId.$expr === "resource-attr" &&
        (objectId.type === "elementum_app" || objectId.type === "elementum_element") &&
        objectId.path[0] === "id") {
        return objectId.type === "elementum_app" && objectId.label === appTfLabel
            ? appRefName
            : tfLabelToTsLabel(objectId.label);
    }
    return undefined;
}
function aspectRefFromObjectId(objectId, ctx) {
    if (!isTfExpr(objectId))
        return undefined;
    if (objectId.$expr === "resource-attr" &&
        (objectId.type === "elementum_app" || objectId.type === "elementum_element") &&
        objectId.path[0] === "id") {
        return {
            kind: "app",
            appHandle: objectId.label === ctx.appTfLabel ? ctx.appRefName : tfLabelToTsLabel(objectId.label),
        };
    }
    if (objectId.$expr !== "data-attr" ||
        (objectId.type !== "elementum_app" && objectId.type !== "elementum_element") ||
        objectId.path[0] !== "id") {
        return undefined;
    }
    const lookup = ctx.dataBlocks.get(`${objectId.type}.${objectId.label}`);
    const identity = lookup?.attrs.name ?? lookup?.attrs.namespace;
    const mapped = typeof identity === "string"
        ? (objectId.type === "elementum_element" ? ctx.foreignElementRefNames : ctx.foreignAppRefNames)?.get(identity)
        : undefined;
    if (mapped !== undefined)
        return { kind: "app", appHandle: mapped };
    if (typeof identity !== "string" || identity === "")
        return undefined;
    return {
        kind: "app",
        appHandle: tfLabelToTsLabel(objectId.label),
        entityType: objectId.type === "elementum_element" ? "element" : "app",
        name: identity,
    };
}
function lookupFieldKeyByDisplayName(ownerRefName, displayName, ctx) {
    const lower = displayName.toLowerCase();
    const foreign = ctx.fieldNamesByOwnerRefName?.get(ownerRefName)?.get(lower);
    if (foreign !== undefined)
        return foreign;
    if (ownerRefName === ctx.appRefName) {
        for (const field of ctx.fieldById.values()) {
            if (field.name.toLowerCase() === lower)
                return field.key;
        }
    }
    for (const key of SYSTEM_FIELD_KEYS) {
        if (DEPLOY_DEFAULT_SYSTEM_FIELD_NAMES[key].toLowerCase() === lower)
            return key;
    }
    return undefined;
}
/**
 * `record.<field-uuid>[.suffix]` — how a calculation formula names a found
 * record's field (the calc engine resolves it by field id, not display
 * name). Mirrors the same shape already handled for live filters in
 * `decodeReferenceString`, resolving the id back to its authoring bag key.
 */
function calculationRecordFieldPath(taskAddress, wireName, ctx) {
    if (taskAddress === undefined)
        return undefined;
    const [head, ...tail] = wireName.split(".");
    if (head !== "record" || tail.length === 0 || !/^[a-f0-9-]{36}$/i.test(tail[0]))
        return undefined;
    const ownerRefName = ctx.taskTargetOwnerRefNameByAddress?.get(taskAddress) ?? ctx.appRefName;
    const fieldKey = resolveFieldKeyFromId(tail[0], ownerRefName, ctx);
    return ["record", fieldKey ?? tail[0], ...tail.slice(1)];
}
function taskRecordSearchFieldPath(taskAddress, wireName, ctx) {
    if (ctx.taskTargetOwnerRefNameByAddress?.get(taskAddress) === undefined)
        return undefined;
    const ownerRefName = ctx.taskTargetOwnerRefNameByAddress.get(taskAddress);
    const wireNameLower = wireName.toLowerCase();
    for (const [suffix, suffixPath] of FIELD_REF_SUFFIX_FROM_WIRE) {
        for (const base of suffix === "" ? [wireName] : wireName.endsWith(suffix) ? [wireName.slice(0, -suffix.length)] : []) {
            const fieldKey = lookupFieldKeyByDisplayName(ownerRefName, base, ctx);
            if (fieldKey !== undefined && (suffix === "" || wireNameLower === `${base}${suffix}`.toLowerCase())) {
                return ["record", fieldKey, ...suffixPath];
            }
        }
    }
    return undefined;
}
/**
 * Sentinel returned by {@link approverReferenceFieldAuthoringType} for a
 * field-shaped reference (matches one of the FIELD_REF_SUFFIX_FROM_WIRE
 * display-name suffixes) into a FOREIGN aspect whose fields have never been
 * captured locally AT ALL — not merely "this one field is unknown", but
 * "this aspect has never been pulled, so nothing about its fields can be
 * verified". Distinct from a real authoring-type string so callers can give
 * a different diagnostic (pull the foreign aspect first) than the
 * positively-wrong-type case (edk#655) below. See edk#768.
 */
const UNVERIFIABLE_FOREIGN_FIELD_TYPE = "__edkUnverifiableForeignFieldType";
/**
 * If `value` is a `refs["<Field> ID"]`-style reference into a field's
 * output, return that field's public authoring type (e.g. "USER", "GROUP")
 * so callers requiring a USER field (approvalChain's requester/approver
 * references) can catch a live automation already wired to the wrong field
 * type before it reaches the provider's own plan-time rejection (edk#655).
 * Covers both a LOCAL field (this app's own `fieldById`) and a FOREIGN one
 * (a non-recursive export never carries the foreign aspect's field
 * resources, so this falls back to the cross-pull field-refs registry,
 * populated once that aspect has itself been pulled at least once — see
 * field-refs-registry.ts's `fieldType`).
 *
 * When the reference targets a FOREIGN aspect that has never been pulled at
 * all, its field types are entirely unknown to the registry — genuinely
 * unverifiable, not "not a field". Returns
 * {@link UNVERIFIABLE_FOREIGN_FIELD_TYPE} in that case rather than silently
 * treating it as safe: confirmed live (eu/sanofi purchaserequisition, edk#768)
 * that letting an unverifiable foreign reference through crashes the WHOLE
 * app's adoption at `ei plan` the same way a known-bad type does. A LOCAL
 * non-match is never ambiguous this way — the app being pulled always
 * carries its own fields exhaustively in this same export — so this fallback
 * only applies to foreign owners.
 */
function approverReferenceFieldAuthoringType(value, ctx) {
    if (!isTfExpr(value) || value.$expr !== "resource-attr" || value.path[0] !== "refs")
        return undefined;
    const taskAddress = `${value.type}.${value.label}`;
    const ownerRefName = ctx.taskTargetOwnerRefNameByAddress?.get(taskAddress);
    if (ownerRefName === undefined)
        return undefined;
    const rawSegments = pathNames(value.path.slice(1));
    if (rawSegments.length !== 1)
        return undefined;
    const wireName = rawSegments[0];
    const wireNameLower = wireName.toLowerCase();
    const typeForFieldKey = (fieldKey) => {
        if (ownerRefName === ctx.appRefName) {
            for (const field of ctx.fieldById.values()) {
                if (field.key === fieldKey)
                    return SUB_TYPE_BY_RESOURCE.get(field.resourceType)?.authoringType;
            }
        }
        for (const foreign of ctx.foreignFieldRefsById?.values() ?? []) {
            if (foreign.ownerRefName === ownerRefName && foreign.fieldKey === fieldKey && foreign.fieldType !== undefined) {
                return foreign.fieldType;
            }
        }
        return undefined;
    };
    // An EXACT field display name always wins over a suffix-stripped guess: a
    // field literally named "Approver ID" must never be shadowed by treating
    // the wire name as the " ID" projection of a DIFFERENT field named
    // "Approver" (edk#768 PR review — this exact collision let a TEXT-typed
    // field through undetected because the wrong guess resolved to the base
    // field's USER type). Once the wire name matches a real field's own name,
    // that field's identity is settled: fall through only to the
    // "unverifiable" check below if ITS type can't be resolved — never guess
    // via a different field's suffix projection instead.
    const exactFieldKey = lookupFieldKeyByDisplayName(ownerRefName, wireName, ctx);
    if (exactFieldKey !== undefined) {
        const type = typeForFieldKey(exactFieldKey);
        if (type !== undefined)
            return type;
        return ownerRefName !== ctx.appRefName &&
            ![...ctx.foreignFieldRefsById?.values() ?? []].some((foreign) => foreign.ownerRefName === ownerRefName)
            ? UNVERIFIABLE_FOREIGN_FIELD_TYPE
            : undefined;
    }
    let fieldShaped = false;
    for (const [suffix] of FIELD_REF_SUFFIX_FROM_WIRE) {
        if (suffix === "" || !wireNameLower.endsWith(suffix.toLowerCase()))
            continue;
        fieldShaped = true;
        const base = wireName.slice(0, -suffix.length);
        const fieldKey = lookupFieldKeyByDisplayName(ownerRefName, base, ctx);
        if (fieldKey === undefined)
            continue;
        const type = typeForFieldKey(fieldKey);
        if (type !== undefined)
            return type;
    }
    if (fieldShaped &&
        ownerRefName !== ctx.appRefName &&
        ![...ctx.foreignFieldRefsById?.values() ?? []].some((foreign) => foreign.ownerRefName === ownerRefName)) {
        return UNVERIFIABLE_FOREIGN_FIELD_TYPE;
    }
    return undefined;
}
function decodeFieldAssignmentValue(value, ctx) {
    // Multi-Select assignments are exported as jsonencode([<option UUID>, ...])
    // rather than the options-by-label locals used for a scalar Dropdown. Walk
    // that payload with the state-derived option registry so pulled source stays
    // portable across organizations and fresh creates.
    if (isTfExpr(value) && value.$expr === "jsonencode") {
        return decodeFieldAssignmentValue(value.value, ctx);
    }
    if (Array.isArray(value)) {
        return value.map((item) => decodeFieldAssignmentValue(item, ctx));
    }
    if (typeof value === "string" && /^[a-f0-9-]{36}$/i.test(value)) {
        const label = ctx.picklistOptionLabelById?.get(value);
        if (label !== undefined)
            return { kind: "literal", value: label };
    }
    return decodeValue(value, ctx);
}
function triggerRefPath(path, ctx) {
    const names = pathNames(path);
    if (names.length !== 1)
        return names;
    const wireName = names[0];
    // Attachment-trigger payload refs ("Attachment" / "Attachment Id") — gated
    // on the trigger type and compared case-sensitively. Field matching below
    // is case-insensitive and uses suffix " ID"; for a coincidental field named
    // "Attachment", refs["Attachment ID"] is the field path
    // (ctx.trigger.record.<key>.id) while refs["Attachment Id"] is the trigger
    // ATTACHMENT_ID payload (ctx.trigger.attachment.id). Those strings collide
    // under .toLowerCase(), so this match must stay exact and must NOT reuse
    // FIELD_REF_SUFFIX_FROM_WIRE.
    if (ctx.trigger.type === "elementum_attachment_added_trigger") {
        if (wireName === "Attachment")
            return ["attachment"];
        if (wireName === "Attachment Id")
            return ["attachment", "id"];
    }
    if (ctx.trigger.type === "elementum_webhook_trigger") {
        const webhookMatch = WEBHOOK_REF_PATTERN.exec(wireName);
        if (webhookMatch)
            return [webhookMatch[1].toLowerCase(), webhookMatch[2]];
    }
    if (ctx.trigger.type === "elementum_on_demand_trigger") {
        if (wireName === "Triggered By's Id")
            return ["triggeredBy", "id"];
        if (wireName === "Triggered By's Name")
            return ["triggeredBy", "name"];
        if (wireName === "Triggered By's Email")
            return ["triggeredBy", "email"];
        const recordPath = prefixedOnDemandRecordRefPath(wireName, ctx);
        if (recordPath !== undefined)
            return recordPath;
    }
    // An on-demand parameter is an explicitly declared top-level ctx.trigger
    // property. Check this before record-field inference: a perfectly ordinary
    // parameter such as `FACILITY` can collide case-insensitively with the app
    // field display name "Facility".
    if (isDeclaredOnDemandParameter(ctx.trigger, wireName))
        return names;
    // A trigger's own bare record reference (confirmed live via `ei refs`,
    // see trigger-record-id.test.ts) — deliberately scoped to triggerRefPath,
    // not the shared WIRE_REF_SEGMENT_TO_TS table: createRecord/
    // findRelatedRecords TASKS also expose a wire key literally named
    // "Record", but as their scalar record-ID output (ASPECT_RECORD_ID, not
    // this trigger-level ASPECT_RECORD whole-record reference) — translating
    // it globally intercepted that task-level case before it could ever be
    // checked (decodeValue's own dedicated "Record" handling for those task
    // types).
    if (wireName === "Record") {
        const recordParams = onDemandRecordParameters(ctx.trigger);
        return recordParams.length === 1
            ? [recordParams[0].name]
            : ["record"];
    }
    const singleRecordParam = onDemandRecordParameters(ctx.trigger);
    if (singleRecordParam.length === 1) {
        const parameter = singleRecordParam[0];
        const ownerRefName = aspectOwnerRefNameFromObjectId(parameter.default_value, ctx.appRefName, ctx.appTfLabel, ctx.dataBlocks, ctx.foreignAppRefNames, ctx.foreignElementRefNames);
        if (ownerRefName !== undefined) {
            return recordFieldRefPath(wireName, ctx, ownerRefName, parameter.name) ?? names;
        }
    }
    if (!RECORD_BEARING_TRIGGER_TYPES.has(ctx.trigger.type ?? ""))
        return names;
    return recordFieldRefPath(wireName, ctx) ?? names;
}
/**
 * Resolve a forEach `.inner.refs["<display name>"]` key to the public record
 * bag key used by `ctx.item`. The provider exposes current-item fields by
 * display name (including system fields such as `"ID"`), while the authored
 * TypeScript surface uses stable keys (`id`, custom field bag keys, and typed
 * suffixes such as `.label`).
 */
function forEachItemRefPath(path, ctx) {
    const names = pathNames(path);
    if (names.length !== 1)
        return names.flatMap((segment) => segment.split("."));
    const wireName = names[0];
    if (wireName === "Record")
        return ["recordId"];
    const wireNameLower = wireName.toLowerCase();
    for (const field of ctx.fieldById.values()) {
        // Some provider paths already contain the stable EDK field key (for
        // example `for_each.<task-id>.productId`), while inner.refs uses the
        // display name in other workflow shapes. Accept both forms.
        if (wireName === field.key)
            return [field.key];
        for (const [suffix, suffixPath] of FIELD_REF_SUFFIX_FROM_WIRE) {
            if (wireNameLower === `${field.name}${suffix}`.toLowerCase()) {
                return [field.key, ...suffixPath];
            }
        }
    }
    for (const key of SYSTEM_FIELD_KEYS) {
        const defaultName = DEPLOY_DEFAULT_SYSTEM_FIELD_NAMES[key];
        for (const [suffix, suffixPath] of FIELD_REF_SUFFIX_FROM_WIRE) {
            if (wireNameLower === `${defaultName}${suffix}`.toLowerCase()) {
                return [key, ...suffixPath];
            }
        }
    }
    return names.flatMap((segment) => segment.split("."));
}
function resolveTaskRefKey(taskIdOrKey, ctx) {
    if (/^[a-f0-9-]{36}$/.test(taskIdOrKey)) {
        for (const [address, task] of ctx.taskByAddress) {
            if (ctx.resourceImportIds.get(address) === taskIdOrKey) {
                return { refKey: task.refKey, block: task.block };
            }
        }
    }
    for (const [, task] of ctx.taskByAddress) {
        if (task.refKey === taskIdOrKey)
            return { refKey: task.refKey, block: task.block };
    }
    return { refKey: taskIdOrKey };
}
function resolveFieldKeyFromId(fieldId, ownerRefName, ctx) {
    const local = ctx.fieldById.get(fieldId);
    if (local !== undefined) {
        if (ownerRefName === undefined || ownerRefName === ctx.appRefName)
            return local.key;
    }
    const state = ctx.foreignFieldRefsById?.get(fieldId);
    if (state === undefined)
        return undefined;
    const expectedOwner = ownerRefName ?? ctx.appRefName;
    return state.ownerRefName === expectedOwner ? state.fieldKey : undefined;
}
function decodeTaskReferenceFromString(taskIdOrKey, pathSegments, ctx) {
    const { refKey, block } = resolveTaskRefKey(taskIdOrKey, ctx);
    if (block !== undefined) {
        const aiAgentPath = aiAgentTaskRefPath(block, pathSegments.join("."));
        if (aiAgentPath !== undefined)
            return { kind: "taskRef", task: refKey, path: aiAgentPath };
    }
    // A task's own record output can be addressed by an unresolved field UUID
    // (`record.<field-uuid>[.suffix]`) the same way a recordSearch task's
    // found-record fields already are below - an updateField task's "record"
    // refers to the record it just updated (same-app field, live-confirmed on
    // purchaseorder/eu-sanofi), so it needs the identical UUID resolution or
    // it falls through to the raw-uuid escape even for a resolvable field.
    if (block?.type === "elementum_update_field_task" &&
        pathSegments.length >= 2 &&
        pathSegments[0] === "record" &&
        /^[a-f0-9-]{36}$/i.test(pathSegments[1])) {
        const taskAddress = block.label !== undefined ? `${block.type}.${block.label}` : undefined;
        const ownerRefName = (taskAddress !== undefined ? ctx.taskTargetOwnerRefNameByAddress?.get(taskAddress) : undefined) ?? ctx.appRefName;
        const fieldKey = resolveFieldKeyFromId(pathSegments[1], ownerRefName, ctx);
        return {
            kind: "taskRef",
            task: refKey,
            path: ["record", fieldKey ?? pathSegments[1], ...pathSegments.slice(2)],
        };
    }
    if (block?.type === "elementum_record_search_task" && pathSegments.length >= 1) {
        const [head, ...tail] = pathSegments;
        if (head === "count") {
            return { kind: "taskRef", task: refKey, path: ["totalCount", ...tail] };
        }
        const recordSearchName = RECORD_SEARCH_TASK_REF_SEGMENT_TO_TS[head];
        if (recordSearchName !== undefined) {
            return { kind: "taskRef", task: refKey, path: [recordSearchName, ...tail] };
        }
        const taskAddress = block.label !== undefined ? `${block.type}.${block.label}` : undefined;
        // Live filters often address a found-record field as
        // `record.<field-uuid>[.suffix]` (Sanofi: Status Label → record.<status-id>.label).
        // System fields have no `*_field` import; resolve the UUID through state the
        // same way trigger `record.<uuid>` already does (#432).
        if (taskAddress !== undefined &&
            head === "record" &&
            tail.length >= 1 &&
            /^[a-f0-9-]{36}$/i.test(tail[0])) {
            const ownerRefName = ctx.taskTargetOwnerRefNameByAddress?.get(taskAddress) ?? ctx.appRefName;
            const fieldKey = resolveFieldKeyFromId(tail[0], ownerRefName, ctx);
            return {
                kind: "taskRef",
                task: refKey,
                path: ["record", fieldKey ?? tail[0], ...tail.slice(1)],
            };
        }
        if (taskAddress !== undefined) {
            const recordFieldPath = taskRecordSearchFieldPath(taskAddress, head, ctx);
            if (recordFieldPath !== undefined) {
                return { kind: "taskRef", task: refKey, path: [...recordFieldPath, ...tail] };
            }
        }
    }
    if (block?.type === "elementum_ai_search_table_task" && pathSegments.length >= 1) {
        const [head, ...tail] = pathSegments;
        const aiSearchName = AI_SEARCH_TABLE_TASK_REF_SEGMENT_TO_TS[head];
        if (aiSearchName !== undefined) {
            return { kind: "taskRef", task: refKey, path: [aiSearchName, ...tail] };
        }
    }
    if (block?.type === "elementum_table_records_search_task" && pathSegments.length >= 1) {
        const [head, ...tail] = pathSegments;
        const tableSearchName = TABLE_RECORDS_SEARCH_TASK_REF_SEGMENT_TO_TS[head];
        if (tableSearchName !== undefined) {
            return { kind: "taskRef", task: refKey, path: [tableSearchName, ...tail] };
        }
    }
    // createRecord/findRelatedRecords record-id + field outputs (same as the
    // refs["Title"] / refs["Record"] resource-attr path in decodeValue).
    if ((block?.type === "elementum_create_record_task" || block?.type === "elementum_find_related_records_task") &&
        pathSegments.length >= 1) {
        const [head, ...tail] = pathSegments;
        if (pathSegments.length === 1 && (head === "Record" || head === "Record ID")) {
            return { kind: "taskRef", task: refKey, path: ["record", "recordId"] };
        }
        if (block.type === "elementum_create_record_task" &&
            head === "record" &&
            tail.length >= 1 &&
            /^[a-f0-9-]{36}$/i.test(tail[0])) {
            const taskAddress = block.label !== undefined ? `${block.type}.${block.label}` : undefined;
            const ownerRefName = taskAddress !== undefined
                ? ctx.taskTargetOwnerRefNameByAddress?.get(taskAddress) ?? ctx.appRefName
                : ctx.appRefName;
            const fieldKey = resolveFieldKeyFromId(tail[0], ownerRefName, ctx);
            return {
                kind: "taskRef",
                task: refKey,
                path: ["record", fieldKey ?? tail[0], ...tail.slice(1)],
            };
        }
        if (block.type === "elementum_create_record_task" && pathSegments.length === 1) {
            const recordPath = recordFieldRefPath(head, ctx);
            if (recordPath !== undefined) {
                return { kind: "taskRef", task: refKey, path: recordPath };
            }
        }
    }
    if (block?.type === "elementum_api_task" && pathSegments.length === 1) {
        const apiPath = apiTaskRefAuthoringPath(pathSegments[0]);
        if (apiPath !== undefined) {
            return { kind: "taskRef", task: refKey, path: apiPath };
        }
    }
    const keyPath = pathSegments.map((segment) => WIRE_REF_SEGMENT_TO_TS[segment] ?? segment);
    const taskKind = block?.type !== undefined ? TASK_FACTORIES[block.type] : undefined;
    const wireOutputKey = taskKind !== undefined ? OUTPUT_KEY_WIRE_NAME[taskKind] : undefined;
    const outputKeyPath = wireOutputKey !== undefined && keyPath[0] === wireOutputKey
        ? ["output", ...keyPath.slice(1)]
        : keyPath;
    return { kind: "taskRef", task: refKey, path: outputKeyPath };
}
function decodeReferenceString(value, ctx) {
    const pieces = value.split(".");
    if (pieces[0] === "trigger" && pieces.length > 1) {
        const path = pieces.slice(1);
        if (path[0] === "record" && path.length >= 2 && /^[a-f0-9-]{36}$/.test(path[1])) {
            const sourceFieldId = path[1];
            const field = ctx.fieldById.get(sourceFieldId);
            const stateField = ctx.foreignFieldRefsById?.get(sourceFieldId);
            const stateFieldKey = stateField?.ownerRefName === ctx.appRefName
                ? stateField.fieldKey
                : undefined;
            // Orphaned: the field this filter's value_reference points at no
            // longer exists in the app export at all (deleted/recreated field,
            // same drift class as the orphaned picklist option ids and skill_ids
            // fixed elsewhere) - confirmed live: a switch_case filter can
            // reference a field id no custom or system field resolves to.
            // Round-trip the raw id verbatim (printPathAccess's existing "as any"
            // unresolved-segment escape already degrades a non-identifier path
            // segment like this one) rather than throwing and losing the whole
            // automation.
            return {
                kind: "triggerRef",
                path: ["record", field?.key ?? stateFieldKey ?? sourceFieldId, ...path.slice(2)],
            };
        }
        return { kind: "triggerRef", path };
    }
    if (pieces[0] === "task" && pieces.length > 2) {
        return decodeTaskReferenceFromString(pieces[1], pieces.slice(2), ctx);
    }
    if (pieces[0] === "for_each" && pieces.length > 2) {
        const { refKey } = resolveTaskRefKey(pieces[1], ctx);
        return {
            kind: "forEachItemRef",
            forEachTask: refKey,
            path: forEachItemRefPath(pieces.slice(2), ctx),
        };
    }
    if (pieces[0] === "variable" && pieces.length > 1) {
        const varPart = pieces.slice(1).join(".");
        const resolved = resolveVariableReference(varPart, varPart, undefined, ctx);
        if (resolved !== undefined)
            return resolved;
    }
    if (value.startsWith("variable|")) {
        const varPart = value.slice("variable|".length);
        const resolved = resolveVariableReference(varPart, varPart, undefined, ctx);
        if (resolved !== undefined)
            return resolved;
    }
    return { kind: "unresolved", uuid: value, hint: pieces[0] ?? "reference" };
}
/**
 * Inverse of filterReferenceToTf's TRIGGER case (automation/to-tf.ts): a
 * filter's value_reference for a trigger-record field renders as a template
 * (`"trigger.record." + <live field ref> + ".<suffix>"?`), not a plain
 * string, so decodeReferenceString never sees it. Recognize that exact shape
 * and decode straight to a triggerRef, matching decodeReferenceString's
 * output — anything else falls through to decodeValue's generic "template"
 * kind, which refDataToFilterReference rejects (correctly, for a real
 * interpolated string that isn't this specific field-address pattern).
 */
function decodeTemplateTriggerRecordFieldRef(value, ctx) {
    if (!isTfExpr(value) || value.$expr !== "template")
        return undefined;
    const [prefix, fieldPart, suffixPart] = value.parts;
    if (prefix !== "trigger.record." || fieldPart === undefined || typeof fieldPart === "string")
        return undefined;
    if (suffixPart !== undefined && typeof suffixPart !== "string")
        return undefined;
    const fieldKey = fieldKeyFromTriggerFieldIdExpr(fieldPart, ctx);
    if (fieldKey === undefined)
        return undefined;
    const suffix = suffixPart === undefined ? [] : suffixPart.replace(/^\./, "").split(".").filter((s) => s.length > 0);
    if (suffix.length === 1 && suffix[0] === "10000001-2000-4000-a000-800000000000") {
        return { kind: "triggerRef", path: ["record", fieldKey, "recordId"] };
    }
    return { kind: "triggerRef", path: ["record", fieldKey, ...suffix] };
}
/**
 * Inverse of filterReferenceToTf's TASK case: filters render
 * `"task." + <task.id> + ".<wireName>"` templates so provider Read's plain
 * `task.<uuid>.<wireName>` strings round-trip without perpetual plan drift.
 */
function decodeTemplateTaskRef(value, ctx) {
    if (!isTfExpr(value) || value.$expr !== "template")
        return undefined;
    const [prefix, idPart, suffixPart] = value.parts;
    if (prefix !== "task." || idPart === undefined || typeof idPart === "string")
        return undefined;
    if (typeof suffixPart !== "string" || !suffixPart.startsWith("."))
        return undefined;
    if (!isTfExpr(idPart) || idPart.$expr !== "resource-attr" || idPart.path[0] !== "id")
        return undefined;
    const task = ctx.taskByAddress.get(`${idPart.type}.${idPart.label}`);
    if (task === undefined)
        return undefined;
    const nameParts = suffixPart.slice(1).split(".").filter((segment) => segment.length > 0);
    if (nameParts.length === 0)
        return undefined;
    return decodeTaskReferenceFromString(task.refKey, nameParts, ctx);
}
/**
 * Resolve the field-id expression at the middle of a `trigger.record.<id>[.suffix]`
 * template. A custom field's id is a `resource-attr` (`elementum_x_field.<label>.id`);
 * a system field (id/title/status/createdBy/updatedBy/...) has no resource block of
 * its own, so ei emits its id via a `<appTfLabel>_<field>_field_id` local instead -
 * confirmed live on a switch-case condition keyed off `updatedBy`. Mirrors
 * decodeValue's generic local handling, but resolves to a triggerRef bag key
 * instead of a "field" RefExpr so it stays a valid filter LHS.
 */
function fieldKeyFromTriggerFieldIdExpr(fieldPart, ctx) {
    if (fieldPart.$expr === "resource-attr" && fieldPart.type.endsWith("_field") && fieldPart.path[0] === "id") {
        return scopedLabelToBagKey(ctx.appTfLabel, fieldPart.label);
    }
    if (fieldPart.$expr === "local") {
        const prefix = `${ctx.appTfLabel}_`;
        if (fieldPart.name.startsWith(prefix) && fieldPart.name.endsWith("_field_id")) {
            return tfLabelToTsLabel(fieldPart.name.slice(prefix.length, -9));
        }
    }
    return undefined;
}
/** Same provider/model derivation as `agentFromTf` and `renderAiProvidersBag`, read off the stream's own data blocks. */
function resolveConnectorRef(connectorType, connectorLabel, ctx) {
    const identity = connectorIdentityFromTf(connectorType, connectorLabel, {
        dataName: (type, label) => {
            const block = ctx.dataBlocks.get(`${type}.${label}`);
            const key = type === "elementum_ai_provider_connector" ? "model_name" : "name";
            const value = block?.attrs[key];
            return typeof value === "string" ? value : undefined;
        },
        dataRef: (type, label) => {
            const value = ctx.dataBlocks.get(`${type}.${label}`)?.attrs["provider_id"];
            return value !== undefined && isTfExpr(value) && value.$expr === "data-attr"
                ? { resourceType: value.type, tfLabel: value.label }
                : undefined;
        },
    });
    return { handle: identity.handle, ...(identity.providerKey !== undefined && { providerKey: identity.providerKey }) };
}
/**
 * `field_ids["Display Name"]` on either the managed table resource or its
 * `data` lookup. The bag key is derived the same way `table/from-tf.ts` derives
 * it for an out-of-stream table, so the printed token lines up with the key the
 * table's own catalog publishes.
 */
function tableColumnRefFromPath(label, path, tableName) {
    const index = path[1];
    const name = typeof index === "string"
        ? index
        : typeof index === "object" && index !== null && "index" in index && typeof index.index === "string"
            ? index.index
            : undefined;
    if (name === undefined)
        throw new EntityTfError(`${TABLE_RESOURCE_TYPE}.${label}: field_ids reference has no column name`);
    return {
        kind: "tableColumn",
        tableHandle: tfLabelToTsLabel(label),
        columnKey: tfLabelToTsLabel(sanitizeName(name)),
        name,
        ...(tableName !== undefined ? { tableName } : {}),
    };
}
function resolveSearchTableFromBlock(block, searchTableLabel, ctx) {
    if (block.type === SEARCH_TABLE_RESOURCE_TYPES.table) {
        const tableId = block.attrs["table_id"];
        const targetTable = resourceRef(tableId);
        const tableLabel = targetTable?.label ??
            (isTfExpr(tableId) && tableId.$expr === "data-attr" && tableId.type === TABLE_RESOURCE_TYPE ? tableId.label : undefined);
        if (tableLabel !== undefined && tableLabel !== "") {
            const tableHandle = tfLabelToTsLabel(tableLabel);
            const prefix = `tables_${tableLabel}_`;
            const tableKey = tfLabelToTsLabel(searchTableLabel.startsWith(prefix) ? searchTableLabel.slice(prefix.length) : searchTableLabel);
            return { kind: "searchTable", appHandle: `tables.${tableHandle}`, tableKey };
        }
        return undefined;
    }
    if (block.type === SEARCH_TABLE_RESOURCE_TYPES.snowflake || block.type === SEARCH_TABLE_RESOURCE_TYPES.linked) {
        const objectId = block.attrs["object_id"];
        if (!isTfExpr(objectId) || (objectId.$expr !== "resource-attr" && objectId.$expr !== "data-attr") || objectId.type !== "elementum_element")
            return undefined;
        const elementHandle = aspectOwnerRefNameFromObjectId(objectId, ctx.appRefName, ctx.appTfLabel, ctx.dataBlocks, ctx.foreignAppRefNames, ctx.foreignElementRefNames);
        if (elementHandle === undefined)
            return undefined;
        const rawElementLabel = objectId.label.startsWith("elements_") ? objectId.label.slice("elements_".length) : objectId.label;
        const elementTfLabel = tsLabelToTfLabel(elementHandle);
        const tableKey = scopedLabelToBagKey(elementTfLabel, searchTableLabel, "element") !== tfLabelToTsLabel(searchTableLabel)
            ? scopedLabelToBagKey(elementTfLabel, searchTableLabel, "element")
            : scopedLabelToBagKey(rawElementLabel, searchTableLabel, "element");
        return { kind: "searchTable", appHandle: elementHandle, tableKey };
    }
    return undefined;
}
function decodeSearchTableReference(value, ctx) {
    if (isTfExpr(value) && value.$expr === "data-attr" && value.type === "elementum_search_table") {
        const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
        const name = typeof lookup?.attrs.name === "string" ? lookup.attrs.name : undefined;
        const objectId = lookup?.attrs.object_id ?? lookup?.attrs.aspect_id;
        const elementHandle = objectId !== undefined
            ? aspectOwnerRefNameFromObjectId(objectId, ctx.appRefName, ctx.appTfLabel, ctx.dataBlocks, ctx.foreignAppRefNames, ctx.foreignElementRefNames)
            : undefined;
        if (elementHandle !== undefined && name !== undefined) {
            const tableKey = tfLabelToTsLabel(sanitizeName(name));
            return { kind: "searchTable", appHandle: elementHandle, tableKey };
        }
        return { kind: "unresolved", uuid: `data.${value.type}.${value.label}`, hint: "search table" };
    }
    const ref = resourceRef(value);
    if (ref !== undefined &&
        (ref.type === SEARCH_TABLE_RESOURCE_TYPES.snowflake ||
            ref.type === SEARCH_TABLE_RESOURCE_TYPES.linked ||
            ref.type === SEARCH_TABLE_RESOURCE_TYPES.table)) {
        const block = ctx.resourceBlocks.get(`${ref.type}.${ref.label}`);
        if (block !== undefined) {
            const resolved = resolveSearchTableFromBlock(block, ref.label, ctx);
            if (resolved !== undefined)
                return resolved;
        }
        const uuid = ctx.resourceImportIds.get(`${ref.type}.${ref.label}`) ?? `${ref.type}.${ref.label}`;
        return { kind: "unresolved", uuid, hint: "search table" };
    }
    if (typeof value === "string") {
        for (const [address, id] of ctx.resourceImportIds) {
            if (id === value) {
                const block = ctx.resourceBlocks.get(address);
                if (block !== undefined) {
                    const searchTableLabel = address.split(".").slice(1).join(".");
                    const resolved = resolveSearchTableFromBlock(block, searchTableLabel, ctx);
                    if (resolved !== undefined)
                        return resolved;
                }
            }
        }
        return { kind: "unresolved", uuid: value, hint: "search table" };
    }
    if (isTfExpr(value) && (value.$expr === "data-attr" || value.$expr === "resource-attr")) {
        const prefix = value.$expr === "data-attr" ? "data." : "";
        return { kind: "unresolved", uuid: `${prefix}${value.type}.${value.label}`, hint: "search table" };
    }
    return { kind: "unresolved", uuid: typeof value === "string" ? value : JSON.stringify(value), hint: "search table" };
}
function decodeValue(value, ctx) {
    if (isTfExpr(value)) {
        if (value.$expr === "template") {
            return { kind: "template", parts: value.parts.map((part) => typeof part === "string" ? part : decodeValue(part, ctx)) };
        }
        if (value.$expr === "jsonencode")
            return decodePlain(value.value, ctx);
        if (value.$expr === "resource-attr") {
            const optionLabel = picklistOptionLabel(value);
            if (optionLabel !== undefined)
                return { kind: "literal", value: optionLabel };
            if (value.type === ctx.trigger.type && value.label === ctx.trigger.label && value.path[0] === "refs") {
                return { kind: "triggerRef", path: triggerRefPath(value.path.slice(1), ctx) };
            }
            const task = ctx.taskByAddress.get(`${value.type}.${value.label}`);
            if (task !== undefined && value.path[0] === "inner" && value.path[1] === "refs") {
                return {
                    kind: "forEachItemRef",
                    forEachTask: task.refKey,
                    path: forEachItemRefPath(value.path.slice(2), ctx),
                };
            }
            if (task !== undefined && value.path[0] === "refs") {
                const rawSegments = pathNames(value.path.slice(1));
                if (rawSegments.length === 1) {
                    const aiAgentPath = aiAgentTaskRefPath(task.block, rawSegments[0]);
                    if (aiAgentPath !== undefined) {
                        return { kind: "taskRef", task: task.refKey, path: aiAgentPath };
                    }
                }
                if (task.block.type === "elementum_record_search_task" && rawSegments.length === 1 && rawSegments[0] === "First found record id") {
                    return { kind: "taskRef", task: task.refKey, path: ["record", "recordId"] };
                }
                // createRecord/findRelatedRecords' own record-id output — confirmed
                // live via `ei refs`: neither task exposes a "recordId" key at all;
                // "Record" (ASPECT_RECORD_ID) is the real one, matching the
                // provider's own schema docs (build.ts's rewriteForwardTaskRefName
                // has the full citation). Previously unhandled here entirely — a
                // pulled automation referencing this output round-tripped as a raw
                // `ctx.actions.<t>.Record` property access that doesn't exist on
                // either task's typed output, failing tsc.
                if ((task.block.type === "elementum_create_record_task" || task.block.type === "elementum_find_related_records_task") &&
                    rawSegments.length === 1 &&
                    (rawSegments[0] === "Record" || rawSegments[0] === "Record ID")) {
                    return { kind: "taskRef", task: task.refKey, path: ["record", "recordId"] };
                }
                // A create-record task exposes each created record field by its
                // display name (for example refs["Title"] or refs["Facility"]). Its
                // public task output is the typed `.record` object, so restore that
                // missing hop and canonical field key on pull.
                if (task.block.type === "elementum_create_record_task" && rawSegments.length === 1) {
                    const recordPath = recordFieldRefPath(rawSegments[0], ctx);
                    if (recordPath !== undefined) {
                        return { kind: "taskRef", task: task.refKey, path: recordPath };
                    }
                }
                if (task.block.type === "elementum_record_search_task" && rawSegments.length >= 1) {
                    const recordSearchName = RECORD_SEARCH_TASK_REF_SEGMENT_TO_TS[rawSegments[0]];
                    if (recordSearchName !== undefined) {
                        return { kind: "taskRef", task: task.refKey, path: [recordSearchName, ...rawSegments.slice(1)] };
                    }
                    if (rawSegments.length === 1) {
                        const recordFieldPath = taskRecordSearchFieldPath(`${value.type}.${value.label}`, rawSegments[0], ctx);
                        if (recordFieldPath !== undefined) {
                            return { kind: "taskRef", task: task.refKey, path: recordFieldPath };
                        }
                    }
                }
                if (task.block.type === "elementum_ai_search_table_task" && rawSegments.length === 1) {
                    const aiSearchName = AI_SEARCH_TABLE_TASK_REF_SEGMENT_TO_TS[rawSegments[0]];
                    if (aiSearchName !== undefined) {
                        return { kind: "taskRef", task: task.refKey, path: [aiSearchName] };
                    }
                }
                if (task.block.type === "elementum_table_records_search_task" && rawSegments.length === 1) {
                    const tableSearchName = TABLE_RECORDS_SEARCH_TASK_REF_SEGMENT_TO_TS[rawSegments[0]];
                    if (tableSearchName !== undefined) {
                        return { kind: "taskRef", task: task.refKey, path: [tableSearchName] };
                    }
                }
                // apiRequest's refs map uses display labels ("File", "Content Type",
                // …) that don't match the lowercase authoring property names
                // (`file`, `file.contentType`) - see API_TASK_REF_AUTHORING_NAMES.
                if (task.block.type === "elementum_api_task" && rawSegments.length === 1) {
                    const apiPath = apiTaskRefAuthoringPath(rawSegments[0]);
                    if (apiPath !== undefined) {
                        return { kind: "taskRef", task: task.refKey, path: apiPath };
                    }
                }
                const keyPath = rawSegments.flatMap((segment) => segment.split("."));
                if (task.block.type === "elementum_variable_task") {
                    return { kind: "variableParamRef", varTask: task.refKey, name: keyPath.join(".") };
                }
                // Inverse of build.ts's OUTPUT_KEY_WIRE_NAME: a single-scalar-output
                // task kind's wire name (e.g. aiClassify's "Response") reads back as
                // the generic `output` accessor its EDK factory actually exposes.
                const taskKind = task.block.type !== undefined ? TASK_FACTORIES[task.block.type] : undefined;
                const wireOutputKey = taskKind !== undefined ? OUTPUT_KEY_WIRE_NAME[taskKind] : undefined;
                const outputKeyPath = wireOutputKey !== undefined && keyPath[0] === wireOutputKey
                    ? ["output", ...keyPath.slice(1)]
                    : keyPath;
                return { kind: "taskRef", task: task.refKey, path: outputKeyPath };
            }
            if (value.type === "elementum_app" && value.label === ctx.appTfLabel && value.path[0] === "id")
                return appRef(ctx.appRefName);
            if (value.type.endsWith("_field") && value.path[0] === "id") {
                return {
                    kind: "field",
                    appHandle: ctx.appRefName,
                    fieldKey: scopedLabelToBagKey(ctx.appTfLabel, value.label),
                };
            }
            if (value.type === AUTOMATION_RESOURCE_TYPE && value.path[0] === "id") {
                return {
                    kind: "automation",
                    appHandle: ctx.appRefName,
                    automationKey: scopedLabelToBagKey(ctx.appTfLabel, value.label),
                };
            }
            if (value.type === "elementum_agent" && value.path[0] === "id") {
                return { kind: "agent", appHandle: ctx.appRefName, agentKey: scopedLabelToBagKey(ctx.appTfLabel, value.label), name: value.label };
            }
            if (value.type === "elementum_phone_service" && value.path[0] === "id") {
                return {
                    kind: "phoneService",
                    appHandle: ctx.appRefName,
                    phoneServiceKey: scopedLabelToBagKey(ctx.appTfLabel, value.label),
                };
            }
            if (value.type === TABLE_RESOURCE_TYPE && value.path[0] === "id") {
                return { kind: "table", tableHandle: tfLabelToTsLabel(value.label) };
            }
            if (value.type === TABLE_RESOURCE_TYPE && value.path[0] === "field_ids") {
                return tableColumnRefFromPath(value.label, value.path, undefined);
            }
            if (value.type === "elementum_datamine" && value.path[0] === "id") {
                const datamineBlock = ctx.resourceBlocks.get(`elementum_datamine.${value.label}`);
                const tableId = datamineBlock?.attrs["table_id"];
                const tableLabel = tableId !== undefined && isTfExpr(tableId) && tableId.$expr === "resource-attr" && tableId.type === "elementum_table" && tableId.path[0] === "id"
                    ? tableId.label
                    : undefined;
                if (tableLabel === undefined)
                    throw new EntityTfError(`elementum_datamine.${value.label}: table_id is not a managed table reference`);
                const prefix = `${tableLabel}_`;
                return {
                    kind: "datamine",
                    tableHandle: tfLabelToTsLabel(tableLabel),
                    datamineKey: tfLabelToTsLabel(value.label.startsWith(prefix) ? value.label.slice(prefix.length) : value.label),
                };
            }
            // IMPLEMENTED only, not ALL: a readFile task can reference an
            // unimplemented reader type (ai/xml) - fileReaderFromTf correctly
            // excludes those from extraction (no catalog entry is ever printed for
            // them), so accepting the reference here would print a
            // catalog.<app>.fileReaders.<key> access to a key that will never
            // exist. Falling through to the throw below instead routes through
            // automationOwnedAddresses (via extractAutomations' catch), which
            // excludes the automation's FULL graph from adoption - not just this
            // reference - the same safety net already proven for other
            // unreconstructable references.
            if (IMPLEMENTED_FILE_READER_RESOURCE_TYPES.includes(value.type) && value.path[0] === "id") {
                return {
                    kind: "documentModel",
                    appHandle: ctx.appRefName,
                    documentModelKey: scopedLabelToBagKey(ctx.appTfLabel, value.label),
                };
            }
            if (value.type === "elementum_approval_process" && value.path[0] === "id") {
                const unresolvedId = ctx.resourceImportIds.get(`${value.type}.${value.label}`);
                return {
                    kind: "approvalChain",
                    appHandle: ctx.appRefName,
                    approvalChainKey: scopedLabelToBagKey(ctx.appTfLabel, value.label),
                    ...(unresolvedId !== undefined ? { unresolvedId } : {}),
                };
            }
            if ((value.type === SEARCH_TABLE_RESOURCE_TYPES.snowflake ||
                value.type === SEARCH_TABLE_RESOURCE_TYPES.linked ||
                value.type === SEARCH_TABLE_RESOURCE_TYPES.table) &&
                value.path[0] === "id") {
                return decodeSearchTableReference(value, ctx);
            }
            throw new EntityTfError(`unreconstructable resource reference ${value.type}.${value.label}`);
        }
        if (value.$expr === "data-attr" && value.type === TABLE_RESOURCE_TYPE && value.path[0] === "id") {
            const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const name = lookup?.attrs.name;
            return {
                kind: "table",
                tableHandle: tfLabelToTsLabel(value.label),
                ...(typeof name === "string" ? { name } : {}),
            };
        }
        if (value.$expr === "data-attr" && value.type === TABLE_RESOURCE_TYPE && value.path[0] === "field_ids") {
            const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const tableName = lookup?.attrs.name;
            return tableColumnRefFromPath(value.label, value.path, typeof tableName === "string" ? tableName : undefined);
        }
        if (value.$expr === "data-attr" && value.type === "elementum_datamine" && value.path[0] === "id") {
            const datamineLookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const tableId = datamineLookup?.attrs["table_id"];
            if (tableId === undefined ||
                !isTfExpr(tableId) ||
                tableId.$expr !== "data-attr" ||
                tableId.type !== TABLE_RESOURCE_TYPE ||
                tableId.path[0] !== "id") {
                throw new EntityTfError(`data.elementum_datamine.${value.label}: table_id is not a table data reference`);
            }
            const prefix = `${tableId.label}_`;
            return {
                kind: "datamine",
                tableHandle: tfLabelToTsLabel(tableId.label),
                datamineKey: tfLabelToTsLabel(value.label.startsWith(prefix) ? value.label.slice(prefix.length) : value.label),
            };
        }
        if (value.$expr === "data-attr" && value.type === "elementum_search_table" && value.path[0] === "id") {
            return decodeSearchTableReference(value, ctx);
        }
        if (value.$expr === "data-attr" && (value.type === "elementum_app" || value.type === "elementum_element") && value.path[0] === "id") {
            const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const name = lookup?.attrs.name ?? lookup?.attrs.namespace;
            const mappedHandle = typeof name === "string"
                ? (value.type === "elementum_element" ? ctx.foreignElementRefNames : ctx.foreignAppRefNames)?.get(name)
                : undefined;
            return {
                kind: "app",
                appHandle: mappedHandle ?? tfLabelToTsLabel(value.label),
                entityType: value.type === "elementum_element" ? "element" : "app",
                ...(typeof name === "string" ? { name } : {}),
            };
        }
        if (value.$expr === "data-attr" && value.type === AUTOMATION_RESOURCE_TYPE && value.path[0] === "id") {
            const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const ownerId = lookup?.attrs["app_id"];
            if (ownerId === undefined ||
                !isTfExpr(ownerId) ||
                ownerId.$expr !== "data-attr" ||
                (ownerId.type !== "elementum_app" && ownerId.type !== "elementum_element") ||
                ownerId.path[0] !== "id") {
                throw new EntityTfError(`data.${AUTOMATION_RESOURCE_TYPE}.${value.label}: app_id is not a resolvable app/element data reference`);
            }
            const ownerLookup = ctx.dataBlocks.get(`${ownerId.type}.${ownerId.label}`);
            const ownerIdentity = ownerLookup?.attrs.namespace ?? ownerLookup?.attrs.name;
            const mappedHandle = typeof ownerIdentity === "string"
                ? (ownerId.type === "elementum_element" ? ctx.foreignElementRefNames : ctx.foreignAppRefNames)?.get(ownerIdentity)
                : undefined;
            const prefix = `${ownerId.label}_`;
            return {
                kind: "automation",
                appHandle: mappedHandle ?? tfLabelToTsLabel(ownerId.label),
                automationKey: tfLabelToTsLabel(value.label.startsWith(prefix) ? value.label.slice(prefix.length) : value.label),
            };
        }
        if (value.$expr === "data-attr" && value.type === "elementum_field" && value.path[0] === "id") {
            // The reverse of to-tf.ts's `crossAppFieldToTf`: a field belonging to a
            // DIFFERENT app/element than this automation's own — resolved via the
            // field's own `data.elementum_field` block, whose `object_id` names
            // the owner's `data.elementum_app`/`data.elementum_element` lookup.
            const fieldBlock = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const objectId = fieldBlock?.attrs["object_id"];
            if (objectId === undefined || !isTfExpr(objectId) || objectId.$expr !== "data-attr" || (objectId.type !== "elementum_app" && objectId.type !== "elementum_element")) {
                throw new EntityTfError(`data.elementum_field.${value.label}: object_id is not a resolvable app/element data reference`);
            }
            const entityType = objectId.type === "elementum_element" ? "element" : "app";
            const ownerLabel = objectId.label;
            const kindPrefix = `${entityType}_${ownerLabel}_`;
            const elementumKindPrefix = `elementum_${kindPrefix}`;
            const bareLabel = value.label.startsWith(elementumKindPrefix)
                ? value.label.slice(elementumKindPrefix.length)
                : value.label.startsWith(kindPrefix)
                    ? value.label.slice(kindPrefix.length)
                    : value.label.startsWith(`${ownerLabel}_`)
                        ? value.label.slice(ownerLabel.length + 1)
                        : value.label;
            const ownerName = ctx.dataBlocks.get(`${objectId.type}.${objectId.label}`)?.attrs["name"];
            const fieldName = fieldBlock?.attrs["name"];
            return {
                kind: "field",
                appHandle: tfLabelToTsLabel(ownerLabel),
                fieldKey: tfLabelToTsLabel(bareLabel),
                foreign: {
                    entityType,
                    name: typeof ownerName === "string" ? ownerName : tfLabelToTsLabel(ownerLabel),
                    ...(typeof fieldName === "string" ? { fieldName } : {}),
                },
            };
        }
        if (value.$expr === "data-attr" && value.type === "elementum_ai_provider_connector" && value.path[0] === "id") {
            return { kind: "connector", id: "", ...resolveConnectorRef(value.type, value.label, ctx) };
        }
        if (value.$expr === "data-attr" && value.type === "elementum_stored_function" && value.path[0] === "id") {
            const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const name = lookup?.attrs.name;
            const cloudLinkId = lookup?.attrs.cloudlink_id;
            const databaseName = lookup?.attrs.database_name;
            const schemaName = lookup?.attrs.schema_name;
            const functionType = lookup?.attrs.type;
            const parameterTypes = lookup?.attrs.parameter_types;
            if (typeof name !== "string" || name === "") {
                throw new EntityTfError(`data.elementum_stored_function.${value.label}: name lookup is missing`);
            }
            if (functionType !== "PROCEDURE") {
                throw new EntityTfError(`data.elementum_stored_function.${value.label}: type must be PROCEDURE for an automation procedure task`);
            }
            if (!Array.isArray(parameterTypes) || !parameterTypes.every((item) => typeof item === "string")) {
                throw new EntityTfError(`data.elementum_stored_function.${value.label}: parameter_types must carry the exact procedure signature`);
            }
            if (typeof databaseName !== "string" || databaseName === "" || typeof schemaName !== "string" || schemaName === "") {
                throw new EntityTfError(`data.elementum_stored_function.${value.label}: database_name and schema_name must identify the procedure location`);
            }
            if (cloudLinkId === undefined ||
                !isTfExpr(cloudLinkId) ||
                cloudLinkId.$expr !== "data-attr" ||
                cloudLinkId.type !== "elementum_cloudlink" ||
                cloudLinkId.path[0] !== "id") {
                throw new EntityTfError(`data.elementum_stored_function.${value.label}: cloudlink_id is not a resolvable CloudLink data reference`);
            }
            // Two procedures can share a sanitized name within one CloudLink
            // (overloads) — org.ts disambiguates them with a numeric suffix
            // assigned across the WHOLE procedure list, a decision this single
            // data block can't reconstruct alone. Resolve the exact bag key from
            // the already-pulled org catalog by the same discriminator
            // (CloudLink + name + database + schema + ordered parameter types);
            // fall back to the base sanitized key, which is already correct
            // whenever there's no overload to disambiguate.
            const cloudLinkName = ctx.dataBlocks.get(`${cloudLinkId.type}.${cloudLinkId.label}`)?.attrs.name;
            const resolvedKey = typeof cloudLinkName === "string"
                ? ctx.procedureKeysByDiscriminator?.get(procedureDiscriminator(cloudLinkName, name, databaseName, schemaName, parameterTypes))
                : undefined;
            return {
                kind: "procedure",
                cloudLinkKey: tfLabelToTsLabel(cloudLinkId.label),
                procedureKey: resolvedKey ?? tfLabelToTsLabel(sanitizeName(name)),
            };
        }
        if (value.$expr === "data-attr" && value.type === "elementum_cloudlink" && value.path[0] === "id") {
            const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const name = lookup?.attrs.name;
            return {
                kind: "cloudLink",
                handle: tfLabelToTsLabel(value.label),
                ...(typeof name === "string" ? { name } : {}),
            };
        }
        if (value.$expr === "data-attr" && value.type === "elementum_user" && value.path[0] === "id") {
            const lookup = ctx.dataBlocks.get(`${value.type}.${value.label}`);
            const email = lookup?.attrs.email;
            if (typeof email !== "string" || email === "") {
                throw new EntityTfError(`data.elementum_user.${value.label}: email lookup is missing`);
            }
            return { kind: "user", email };
        }
        if (value.$expr === "local") {
            const stageName = stageFilterName(value, ctx);
            if (stageName !== undefined)
                return { kind: "literal", value: stageName };
            const prefix = `${ctx.appTfLabel}_`;
            if (value.name.startsWith(prefix) && value.name.endsWith("_field_id")) {
                return { kind: "field", appHandle: ctx.appRefName, fieldKey: tfLabelToTsLabel(value.name.slice(prefix.length, -9)) };
            }
            // Task field assignments have their field token alongside the value.
            // Preserve public authoring as the readable option label; automation
            // build validates that label against field metadata and reconstructs
            // the exact options-by-label local expression.
            const optionLabel = picklistOptionLabel(value);
            if (optionLabel !== undefined)
                return { kind: "literal", value: optionLabel };
            throw new EntityTfError(`unreconstructable local reference local.${value.name}`);
        }
        throw new EntityTfError(`unsupported expression kind "${value.$expr}" in automation`);
    }
    if (Array.isArray(value))
        return value.map((item) => decodeValue(item, ctx));
    if (isObject(value))
        return Object.fromEntries(Object.entries(value).map(([key, child]) => [key, decodeValue(child, ctx)]));
    return { kind: "literal", value };
}
function decodePlain(value, ctx) {
    const unwrap = (decoded) => {
        if (isObject(decoded) && decoded.kind === "literal")
            return decoded.value;
        if (isObject(decoded) && typeof decoded.kind === "string")
            return decoded;
        if (Array.isArray(decoded))
            return decoded.map(unwrap);
        if (isObject(decoded))
            return Object.fromEntries(Object.entries(decoded).map(([key, child]) => [key, unwrap(child)]));
        return decoded;
    };
    return unwrap(decodeValue(value, ctx));
}
function decodeCustomResponse(value, ctx) {
    if (!isObject(value))
        throw new EntityTfError("invalid custom_response");
    const decodeSection = (section) => {
        if (!isObject(section))
            return undefined;
        const format = typeof section.format === "string" ? section.format : "JSON";
        // The provider contract stores webhook bodies as strings, including JSON
        // strings. Decoding a plain string as a generic automation value wraps it
        // in a literal RefExpr; stringifying that wrapper on every pull grows an
        // additional { kind: "literal", value: ... } layer each round trip.
        const bodyValue = typeof section.body === "string"
            ? section.body
            : section.body !== undefined ? decodeValue(section.body, ctx) : "";
        const body = typeof bodyValue === "string" ? bodyValue : JSON.stringify(bodyValue);
        return {
            enabled: section.enabled === true,
            format: format,
            ...(typeof section.status_code === "number" ? { statusCode: section.status_code } : {}),
            body,
        };
    };
    const success = decodeSection(value.success);
    const failure = decodeSection(value.failure);
    if (success === undefined && failure === undefined)
        return undefined;
    return { ...(success !== undefined ? { success } : {}), ...(failure !== undefined ? { failure } : {}) };
}
function fieldRef(value, ctx) {
    if (isTfExpr(value) && value.$expr === "data-attr" && value.path.join(".") === "id") {
        const foreign = ctx.foreignFieldRefsByDataAddress?.get(`${value.type}.${value.label}`);
        if (foreign !== undefined)
            return { kind: "field", appHandle: foreign.ownerRefName, fieldKey: foreign.fieldKey };
    }
    const decoded = decodeValue(value, ctx);
    if (isObject(decoded) && decoded.kind === "field")
        return decoded;
    if (typeof value === "string") {
        const foreign = ctx.foreignFieldRefsById?.get(value);
        if (foreign !== undefined) {
            return { kind: "field", appHandle: foreign.ownerRefName, fieldKey: foreign.fieldKey };
        }
        return { kind: "unresolved", uuid: value, hint: "field" };
    }
    throw new EntityTfError("field reference cannot be reconstructed");
}
/**
 * A TABLE search's filter addresses table COLUMNS, not app fields. The bridge
 * now hydrates the searched table, so the left side arrives as
 * `field_ids["Column"]` where it used to be the raw column UUID that
 * `fieldRef` degraded to an `unresolved` field.
 */
function filterFieldRef(value, ctx) {
    if (isTfExpr(value) &&
        (value.$expr === "resource-attr" || value.$expr === "data-attr") &&
        value.type === TABLE_RESOURCE_TYPE &&
        value.path[0] === "field_ids") {
        const tableName = value.$expr === "data-attr"
            ? ctx.dataBlocks.get(`${value.type}.${value.label}`)?.attrs.name
            : undefined;
        return tableColumnRefFromPath(value.label, value.path, typeof tableName === "string" ? tableName : undefined);
    }
    return fieldRef(value, ctx);
}
const USER_FIELD_KEY_SET = new Set(USER_FIELD_KEYS);
function userFieldRef(value, ctx) {
    if (typeof value === "string" && USER_FIELD_KEY_SET.has(value)) {
        return { kind: "userField", fieldKey: value };
    }
    return fieldRef(value, ctx);
}
function picklistOptionLabel(value) {
    if (value === undefined || !isTfExpr(value))
        return undefined;
    const lookup = value.$expr === "local"
        ? value.name
        : value.$expr === "resource-attr" && value.path.length === 2 && typeof value.path[0] === "string"
            ? value.path[0]
            : undefined;
    const segment = value.$expr === "local" ? value.path[0] : value.$expr === "resource-attr" ? value.path[1] : undefined;
    const isOptionsLookup = lookup?.endsWith("_options_by_label") === true || lookup?.endsWith("_option_ids_by_label") === true;
    return isOptionsLookup &&
        typeof segment === "object" &&
        segment !== null &&
        "index" in segment &&
        typeof segment.index === "string"
        ? segment.index
        : undefined;
}
// The platform emits a system-stage filter as a KEY-keyed local
// `<appTfLabel>_stage_ids_by_key["<stageKey>"]` (unlike status/dropdown, which are
// LABEL-keyed). Resolve the key against the app's reconstructed stages so the filter
// decodes to the stage name instead of leaking the raw local expression (#116).
function stageFilterName(value, ctx) {
    if (value === undefined || !isTfExpr(value) || value.$expr !== "local" || value.path.length !== 1)
        return undefined;
    if (value.name !== `${ctx.appTfLabel}_stage_ids_by_key`)
        return undefined;
    const segment = value.path[0];
    if (typeof segment !== "object" || segment === null || !("index" in segment) || typeof segment.index !== "string")
        return undefined;
    return ctx.stagesByKey.get(segment.index);
}
/**
 * ei exports `elementum_calculation_task.calculations[].formula` as the calc
 * engine's mustache / short-form string, not as an HCL `refs["…"]`
 * expression. `decodeValue` would keep platform ids in authored source
 * (#312). Rewrite each embedded ref through the same resolvers used for
 * filters, using import-block ids already in this stream.
 *
 * Recognized shapes:
 * - `{{trigger name="record.<field-uuid>"}}`
 * - `{{task name="<wire output>" id="<task-uuid>"}}`
 * - `variable.<variable-uuid>` (calc-engine short form)
 */
function decodeCalculationFormula(value, ctx) {
    if (typeof value !== "string")
        return decodeValue(value, ctx);
    const parts = [];
    let cursor = 0;
    const pattern = /\{\{(trigger|task)\s+([^}]*)\}\}|variable\.([a-f0-9-]{36})/g;
    for (const match of value.matchAll(pattern)) {
        const index = match.index ?? 0;
        if (index > cursor)
            parts.push(value.slice(cursor, index));
        if (match[3] !== undefined) {
            const resolved = resolveVariableReference(match[3], match[3], undefined, ctx);
            parts.push(resolved ?? { kind: "unresolved", uuid: `variable.${match[3]}`, hint: "variable" });
            cursor = index + match[0].length;
            continue;
        }
        const kind = match[1];
        const attrs = match[2];
        if (kind === "trigger") {
            const name = /name="([^"]*)"/.exec(attrs)?.[1];
            const decoded = name !== undefined ? decodeReferenceString(`trigger.${name}`, ctx) : undefined;
            parts.push(decoded ?? { kind: "unresolved", uuid: name ?? match[0], hint: "calculation-reference" });
        }
        else {
            const name = /name="([^"]*)"/.exec(attrs)?.[1];
            const id = /id="([^"]*)"/.exec(attrs)?.[1];
            parts.push(decodeCalculationTaskMustache(id, name, ctx));
        }
        cursor = index + match[0].length;
    }
    if (parts.length === 0)
        return { kind: "literal", value };
    if (cursor < value.length)
        parts.push(value.slice(cursor));
    if (parts.length === 1 && typeof parts[0] !== "string")
        return parts[0];
    return { kind: "template", parts };
}
function decodeCalculationTaskMustache(taskId, wireName, ctx) {
    if (taskId === undefined || wireName === undefined) {
        return { kind: "unresolved", uuid: `task:${taskId ?? "?"}:${wireName ?? "?"}`, hint: "calculation-reference" };
    }
    let refKey;
    let taskBlock;
    for (const [address, task] of ctx.taskByAddress) {
        if (ctx.resourceImportIds.get(address) === taskId) {
            refKey = task.refKey;
            taskBlock = task.block;
            break;
        }
    }
    if (refKey === undefined) {
        return { kind: "unresolved", uuid: `task.${taskId}.${wireName}`, hint: "calculation-reference" };
    }
    if (taskBlock !== undefined) {
        const aiAgentPath = aiAgentTaskRefPath(taskBlock, wireName);
        if (aiAgentPath !== undefined)
            return { kind: "taskRef", task: refKey, path: aiAgentPath };
    }
    // A calculation mustache carries the WorkflowTaskReference name, not the
    // refs-map display key: a found record's field is `record.<field-uuid>`
    // and the task's own outputs are their internal names. Both are addressed
    // here before the display-key tables below, which never match them.
    const taskAddress = taskBlock?.label !== undefined ? `${taskBlock.type}.${taskBlock.label}` : undefined;
    const calcRecordFieldPath = calculationRecordFieldPath(taskAddress, wireName, ctx);
    if (calcRecordFieldPath !== undefined)
        return { kind: "taskRef", task: refKey, path: calcRecordFieldPath };
    if (taskBlock?.type === "elementum_record_search_task") {
        const calcOwnOutput = RECORD_SEARCH_CALCULATION_REF_NAME_TO_TS[wireName];
        if (calcOwnOutput !== undefined)
            return { kind: "taskRef", task: refKey, path: [...calcOwnOutput] };
        const recordSearchName = RECORD_SEARCH_TASK_REF_SEGMENT_TO_TS[wireName];
        if (recordSearchName !== undefined) {
            return { kind: "taskRef", task: refKey, path: [recordSearchName] };
        }
        if (taskAddress !== undefined) {
            const recordFieldPath = taskRecordSearchFieldPath(taskAddress, wireName, ctx);
            if (recordFieldPath !== undefined) {
                return { kind: "taskRef", task: refKey, path: recordFieldPath };
            }
        }
    }
    if (taskBlock?.type === "elementum_ai_search_table_task") {
        const aiSearchName = AI_SEARCH_TABLE_TASK_REF_SEGMENT_TO_TS[wireName];
        if (aiSearchName !== undefined) {
            return { kind: "taskRef", task: refKey, path: [aiSearchName] };
        }
    }
    if (taskBlock?.type === "elementum_table_records_search_task") {
        const tableSearchName = TABLE_RECORDS_SEARCH_TASK_REF_SEGMENT_TO_TS[wireName];
        if (tableSearchName !== undefined) {
            return { kind: "taskRef", task: refKey, path: [tableSearchName] };
        }
    }
    if (taskBlock?.type === "elementum_api_task") {
        const apiPath = apiTaskRefAuthoringPath(wireName);
        if (apiPath !== undefined) {
            return { kind: "taskRef", task: refKey, path: apiPath };
        }
    }
    const keyPath = wireName.split(".");
    const taskKind = taskBlock?.type !== undefined ? TASK_FACTORIES[taskBlock.type] : undefined;
    const wireOutputKey = taskKind !== undefined ? OUTPUT_KEY_WIRE_NAME[taskKind] : undefined;
    const outputKeyPath = wireOutputKey !== undefined && keyPath[0] === wireOutputKey
        ? ["output", ...keyPath.slice(1)]
        : keyPath.map((segment) => WIRE_REF_SEGMENT_TO_TS[segment] ?? segment);
    return { kind: "taskRef", task: refKey, path: outputKeyPath };
}
const PULLED_CALCULATION_RESULT_TYPES = {
    ATTACHMENT_ID: "attachment",
    BOOLEAN: "bool",
    DATE: "date",
    DATETIME: "datetime",
    DECIMAL: "decimal",
    GROUP_ID: "group",
    HANDLE: "handle",
    HTML: "html",
    JSON: "json",
    MULTI_PICKLIST: "multiPicklist",
    NUMBER: "number",
    PICKLIST: "picklist",
    TEXT: "text",
    USER_ID: "user",
};
function pulledRawCalculation(formula, outputType, multiple) {
    if (typeof formula !== "string" || multiple === true || typeof outputType !== "string")
        return undefined;
    const resultType = PULLED_CALCULATION_RESULT_TYPES[outputType];
    return resultType === undefined ? undefined : { kind: "calculationRaw", text: formula, resultType };
}
function resolveVariableReference(rawId, rawName, outputName, ctx) {
    const variableTasks = [];
    for (const [, task] of ctx.taskByAddress) {
        if (task.block.type === "elementum_variable_task") {
            const varName = typeof task.block.attrs.variable_name === "string"
                ? task.block.attrs.variable_name
                : (typeof task.block.attrs.name === "string" ? task.block.attrs.name : task.refKey);
            const varId = typeof task.block.attrs.variable_id === "string"
                ? task.block.attrs.variable_id
                : undefined;
            variableTasks.push({ refKey: task.refKey, varName, varId });
        }
    }
    if (variableTasks.length === 0)
        return undefined;
    // 1. By variable ID if available on the variable task block
    if (rawId !== undefined) {
        const byId = variableTasks.find((vt) => vt.varId === rawId);
        if (byId !== undefined) {
            return { kind: "variableParamRef", varTask: byId.refKey, name: byId.varName };
        }
    }
    // 2. By variable name or refKey if rawName is not a synthetic variable|<uuid> name
    if (rawName !== undefined && !rawName.startsWith("variable|") && !rawName.startsWith("variable.")) {
        const byName = variableTasks.find((vt) => vt.varName === rawName || vt.refKey === rawName);
        if (byName !== undefined) {
            return { kind: "variableParamRef", varTask: byName.refKey, name: byName.varName };
        }
    }
    // 3. By target output name (only for workflow_publish outputs where outputName is provided)
    if (outputName !== undefined) {
        const byOutput = variableTasks.find((vt) => vt.varName === outputName || vt.refKey === outputName);
        if (byOutput !== undefined) {
            return { kind: "variableParamRef", varTask: byOutput.refKey, name: byOutput.varName };
        }
    }
    // 4. The variable's real platform id only exists in deployed state, never
    // in the static export text a `variable_task`/`update_variable_task` block
    // carries (no `variable_id`/`id` attribute on the wire) - so a workflow
    // OUTPUT's `variableReference.name` is always the synthetic
    // "variable|<uuid>" placeholder, and #2/#3's name-based matches routinely
    // miss on casing/spacing (output "Output Message" vs. variable "Output
    // message" - live-confirmed on purchaseorder/eu-sanofi's Authorization
    // Check automation). With exactly one variable task in the whole
    // automation there is no other candidate it could mean. Scoped to
    // outputName (workflow_publish outputs only) - a bare filter reference's
    // "variable.<uuid>" is a DIFFERENT wire dialect with no such guarantee
    // (board issue #171 contract: a filter referencing variable.<uuid> must
    // NOT bind to a same-automation variable task).
    if (rawId !== undefined && outputName !== undefined && variableTasks.length === 1) {
        const only = variableTasks[0];
        return { kind: "variableParamRef", varTask: only.refKey, name: only.varName };
    }
    return undefined;
}
function decodeFilterReferenceEnvelope(value, ctx, outputName) {
    let raw = isTfExpr(value) && value.$expr === "jsonencode" ? value.value : value;
    while (isObject(raw) && "value" in raw && Object.keys(raw).length === 1) {
        raw = raw.value;
    }
    if (!isObject(raw))
        return undefined;
    const taskRef = raw.taskReference;
    if (isObject(taskRef) && typeof taskRef.name === "string") {
        const taskIdOrKey = workflowOutputTaskIdOrKey(taskRef.taskId, ctx);
        if (taskIdOrKey !== undefined) {
            return decodeTaskReferenceFromString(taskIdOrKey, taskRef.name.split("."), ctx);
        }
    }
    const triggerRef = raw.triggerReference;
    if (isObject(triggerRef) && typeof triggerRef.name === "string") {
        return decodeReferenceString(`trigger.${triggerRef.name}`, ctx);
    }
    const variableRef = raw.variableReference;
    if (isObject(variableRef)) {
        let rawId = typeof variableRef.id === "string" && variableRef.id !== "" ? variableRef.id : undefined;
        let rawName = typeof variableRef.name === "string" && variableRef.name !== "" ? variableRef.name : undefined;
        if (rawName !== undefined) {
            if (rawName.startsWith("variable|")) {
                const afterPrefix = rawName.slice("variable|".length);
                if (rawId === undefined)
                    rawId = afterPrefix;
            }
            else if (rawName.startsWith("variable.")) {
                const afterPrefix = rawName.slice("variable.".length);
                if (rawId === undefined)
                    rawId = afterPrefix;
            }
        }
        const resolved = resolveVariableReference(rawId, rawName, outputName, ctx);
        if (resolved !== undefined)
            return resolved;
        const fallbackUuid = rawId ?? (rawName !== undefined ? rawName.replace(/^variable[|.]/, "") : "unknown");
        return { kind: "unresolved", uuid: `variable.${fallbackUuid}`, hint: "variable" };
    }
    return undefined;
}
/** Resolve export UUID or build-time `task.id` resource attr to a task id/refKey. */
function workflowOutputTaskIdOrKey(taskId, ctx) {
    if (typeof taskId === "string" && taskId !== "")
        return taskId;
    if (typeof taskId === "object" && taskId !== null && isTfExpr(taskId)) {
        const exprValue = taskId;
        if (exprValue.$expr === "resource-attr" && "path" in exprValue && Array.isArray(exprValue.path) && exprValue.path[0] === "id"
            && "type" in exprValue && "label" in exprValue
            && typeof exprValue.type === "string" && typeof exprValue.label === "string") {
            const task = ctx.taskByAddress.get(`${exprValue.type}.${exprValue.label}`);
            return task?.refKey;
        }
    }
    return undefined;
}
function filterReference(value, ctx) {
    return typeof value === "string"
        ? decodeReferenceString(value, ctx) ?? { kind: "unresolved", uuid: value, hint: "filter-reference" }
        : decodeFilterReferenceEnvelope(value, ctx)
            ?? decodeTemplateTriggerRecordFieldRef(value, ctx)
            ?? decodeTemplateTaskRef(value, ctx)
            ?? decodeValue(value, ctx);
}
function filterReferenceType(value, fallback, ctx) {
    const raw = isTfExpr(value) && value.$expr === "jsonencode" ? value.value : value;
    if (!isObject(raw))
        return fallback;
    const taskRef = raw.taskReference;
    if (isObject(taskRef) && typeof taskRef.name === "string") {
        const taskIdOrKey = workflowOutputTaskIdOrKey(taskRef.taskId, ctx);
        const task = taskIdOrKey === undefined ? undefined : resolveTaskRefKey(taskIdOrKey, ctx).block;
        if (task?.type === "elementum_execute_script_task") {
            const schemaValue = task.attrs.output_schema;
            const schema = isTfExpr(schemaValue) && schemaValue.$expr === "jsonencode"
                ? schemaValue.value
                : schemaValue;
            const declared = outputFieldTypesFromScriptSchema(schema)?.get(taskRef.name);
            if (declared !== undefined)
                return fieldTypeToValueType(declared.fieldType);
        }
    }
    return typeof raw.fieldType === "string" ? fieldTypeToValueType(raw.fieldType) : fallback;
}
function decodeFilter(value, ctx, fieldScope = "app") {
    return deserializeFilterFromTf(value, {
        field: (fieldValue, scope) => {
            const field = scope === "user" ? userFieldRef(fieldValue, ctx) : filterFieldRef(fieldValue, ctx);
            return field ?? { kind: "unresolved", uuid: String(fieldValue), hint: "field" };
        },
        reference: (reference) => filterReference(reference, ctx),
        referenceType: (reference, fallback) => filterReferenceType(reference, fallback, ctx),
        literal: (literal) => picklistOptionLabel(literal) ?? stageFilterName(literal, ctx) ?? literal,
    }, fieldScope);
}
function triggerFromBlock(block, ctx) {
    const kind = TRIGGER_KINDS[block.type];
    if (kind === undefined)
        throw new EntityTfError(`unknown trigger resource type "${block.type}"`);
    const app = appRef(ctx.appRefName);
    const filter = block.attrs.filter !== undefined ? decodeFilter(block.attrs.filter, ctx) : undefined;
    switch (kind) {
        case "onDemand": {
            const parameters = block.attrs.parameters ?? [];
            if (!Array.isArray(parameters))
                throw new EntityTfError("on-demand parameters must be an array");
            return {
                kind, inputs: parameters.map((parameter) => {
                    if (!isObject(parameter) || typeof parameter.name !== "string" || typeof parameter.type !== "string")
                        throw new EntityTfError("invalid on-demand parameter");
                    // Inverse of to-tf's "attachment" special case above — the wire has
                    // no "attachmentId" spelling at all, but the authoring alias
                    // (onDemand's TriggerFieldTypeAlias) does. Map back so printed
                    // source uses a type-checkable alias instead of a raw wire string
                    // "attachment" that TriggerFieldTypeAlias doesn't accept.
                    const type = parameter.type === "attachment" ? "attachmentId" : parameter.type;
                    const record = type === "record";
                    const recordApp = record
                        ? aspectRefFromObjectId(parameter.default_value, ctx)
                        : undefined;
                    if (record && recordApp === undefined) {
                        throw new EntityTfError(`on-demand record parameter "${parameter.name}" has no resolvable app/element default_value`);
                    }
                    return {
                        name: parameter.name,
                        type,
                        ...(recordApp !== undefined ? { app: recordApp } : {}),
                        ...(parameter.required === true ? { required: true } : {}),
                        ...(parameter.multiple === true ? { multiple: true } : {}),
                        ...(!record && parameter.default_value !== undefined ? { defaultValue: parameter.default_value } : {}),
                        ...(typeof parameter.description === "string" ? { description: parameter.description } : {}),
                    };
                }), ...(block.attrs.show_triggered_by === true ? { showTriggeredBy: true } : {})
            };
        }
        case "recordCreate":
        case "recordUpdate":
        case "attachment":
            return { kind, app, ...(filter ? { filter } : {}), ...(Array.isArray(block.attrs.changed_fields) ? { changedFields: block.attrs.changed_fields.map((item) => fieldRef(item, ctx)) } : {}), ...(typeof block.attrs.think_ui === "boolean" ? { thinkUi: block.attrs.think_ui } : {}) };
        case "commentAdded":
            if (typeof block.attrs.conversation_type !== "string")
                throw new EntityTfError("comment trigger requires conversation_type");
            return { kind, app, conversationType: block.attrs.conversation_type, ...(filter ? { filter } : {}) };
        case "conversationEnd":
            if (typeof block.attrs.conversation_type !== "string")
                throw new EntityTfError("conversation trigger requires conversation_type");
            return { kind, app, conversationType: block.attrs.conversation_type };
        case "webhook":
            // ei's export only reliably emits `authenticated` when it can (see
            // cmd/elementum/export/hcl_trigger.go) - a real captured stream can
            // still omit the key, so default to false rather than requiring it
            // (edk#557, same asymmetry as approval-chain's approveButton, #325).
            return {
                kind,
                authenticated: typeof block.attrs.authenticated === "boolean" ? block.attrs.authenticated : false,
                ...(block.attrs.custom_response !== undefined ? { customResponse: decodeCustomResponse(block.attrs.custom_response, ctx) } : {}),
            };
        case "approvalProcessStatusUpdated": {
            if (typeof block.attrs.status !== "string")
                throw new EntityTfError("approval trigger is incomplete");
            // approval_chain_template_id is genuinely omittable on the platform
            // (edk#712) - a null/never-set template means "any", not "unresolved".
            if (block.attrs.approval_chain_template_id === undefined) {
                return { kind, app, status: block.attrs.status };
            }
            const approvalChain = typeof block.attrs.approval_chain_template_id === "string"
                ? block.attrs.approval_chain_template_id
                : decodeValue(block.attrs.approval_chain_template_id, ctx);
            if (typeof approvalChain !== "string" && (!isObject(approvalChain) || approvalChain.kind !== "approvalChain")) {
                throw new EntityTfError("approval trigger has an unreconstructable approval chain");
            }
            return { kind, app, approvalChain: approvalChain, status: block.attrs.status };
        }
        case "dataMine":
            {
                const raw = block.attrs.datamine_id;
                const datamine = typeof raw === "string" ? raw : decodeValue(raw, ctx);
                if (typeof datamine !== "string" && (!isObject(datamine) || datamine.kind !== "datamine")) {
                    throw new EntityTfError("datamine trigger requires a managed datamine reference or explicit platform id");
                }
                return { kind, datamine: datamine, ...(typeof block.attrs.fire_on_alert === "boolean" ? { fireOnAlert: block.attrs.fire_on_alert } : {}), ...(typeof block.attrs.fire_on_recovery === "boolean" ? { fireOnRecovery: block.attrs.fire_on_recovery } : {}) };
            }
        case "emailReceived":
            if (typeof block.attrs.email_alias_id !== "string")
                throw new EntityTfError("email trigger requires email_alias_id");
            return { kind, emailAliasId: block.attrs.email_alias_id };
        case "slackMessage": return { kind };
        case "surveyResponseCreate":
            if (typeof block.attrs.survey_template_id !== "string")
                throw new EntityTfError("survey trigger requires survey_template_id");
            return { kind, app, survey: block.attrs.survey_template_id };
        case "schedule": {
            if (typeof block.attrs.frequency !== "string")
                throw new EntityTfError("schedule trigger requires frequency");
            if (typeof block.attrs.timezone !== "string")
                throw new EntityTfError("schedule trigger requires timezone");
            if (typeof block.attrs.start_at !== "string")
                throw new EntityTfError("schedule trigger requires start_at");
            const frequency = block.attrs.frequency;
            if (frequency !== "HOURLY" && frequency !== "DAILY" && frequency !== "WEEKLY" && frequency !== "MONTHLY") {
                throw new EntityTfError(`schedule trigger frequency "${frequency}" is not a public EDK schedule frequency`);
            }
            if (block.attrs.end_at !== undefined) {
                throw new EntityTfError("schedule trigger end_at is not a public EDK schedule option");
            }
            if (block.attrs.ends_after_count !== undefined) {
                throw new EntityTfError("schedule trigger ends_after_count is not a public EDK schedule option");
            }
            if (block.attrs.interval_unit !== undefined) {
                throw new EntityTfError("schedule trigger interval_unit is not a public EDK schedule option");
            }
            if (block.attrs.filter !== undefined) {
                throw new EntityTfError("schedule trigger filter is not a public EDK schedule option");
            }
            const weekdays = Array.isArray(block.attrs.weekdays)
                ? block.attrs.weekdays.filter((day) => typeof day === "string")
                : undefined;
            return {
                kind,
                frequency,
                timezone: block.attrs.timezone,
                startAt: block.attrs.start_at,
                ...(typeof block.attrs.interval === "number" ? { interval: block.attrs.interval } : {}),
                ...(weekdays !== undefined && weekdays.length > 0 ? { weekdays } : {}),
                ...(typeof block.attrs.day_of_month === "number" ? { dayOfMonth: block.attrs.day_of_month } : {}),
            };
        }
        default: throw new EntityTfError(`trigger "${kind}" is outside the Terraform contract`);
    }
}
function taskArgs(block, factory, ctx) {
    const args = {};
    for (const [attr, value] of Object.entries(block.attrs)) {
        if (attr === "parent" || attr === "name" || attr === "depends_on")
            continue;
        if (attr === "filter") {
            args.filter = decodeFilter(value, ctx, factory === "userSearch" ? "user" : "app");
            continue;
        }
        if (attr === "fields" && (factory === "createRecord" || factory === "updateField")) {
            if (!Array.isArray(value))
                throw new EntityTfError(`${block.type}.${block.label}: fields must be an array`);
            const values = {};
            for (const item of value) {
                if (!isObject(item) || item.field_id === undefined)
                    throw new EntityTfError(`${block.type}.${block.label}: invalid field assignment`);
                const field = fieldRef(item.field_id, ctx);
                // Non-recursive exports preserve a real foreign target as
                // data.elementum_app/element, but its field assignments are bare ids
                // because the target's fields were not exported. Keep the automation
                // extractable and mark only those field keys as unresolved. The
                // printer makes the non-portability explicit with unsafePlatformId;
                // `--with-dependencies` pulls the target and the second extraction
                // pass upgrades these ids through foreignFieldRefsById to typed refs.
                const fieldKey = field.kind === "field"
                    ? field.fieldKey
                    : `${UNRESOLVED_FIELD_MARKER}${field.uuid}`;
                const decodedValue = decodeFieldAssignmentValue(item.value, ctx);
                values[fieldKey] = item.update_mode === "add"
                    ? { __fieldAssignment: true, value: decodedValue, updateMode: "add" }
                    : decodedValue;
            }
            args.values = values;
            continue;
        }
        if (attr === "inputs" && factory === "executeScript") {
            if (!Array.isArray(value))
                throw new EntityTfError("script inputs must be an array");
            args.inputs = Object.fromEntries(value.map((item) => {
                if (!isObject(item) || typeof item.name !== "string")
                    throw new EntityTfError("invalid script input");
                return [item.name, decodeValue(item.value, ctx)];
            }));
            continue;
        }
        if (attr === "output_schema" && factory === "executeScript") {
            const raw = isTfExpr(value) && value.$expr === "jsonencode" ? value.value : value;
            const root = isObject(raw) && isObject(raw.object) ? raw.object : undefined;
            const props = Array.isArray(root?.properties) ? root.properties : [];
            const properties = Object.fromEntries(props.map((property) => {
                if (!isObject(property))
                    throw new EntityTfError("invalid script output schema");
                const [type, definition] = Object.entries(property)[0] ?? [];
                if (type === undefined || !isObject(definition) || typeof definition.name !== "string")
                    throw new EntityTfError("invalid script output property");
                return [definition.name, type === "bool" ? "boolean" : type];
            }));
            // The root object's own name is inert to the platform but real stored
            // data, so carry it through rather than rebuilding a schema that
            // differs from the one just read.
            args.outputs = typeof root?.name === "string" && root.name !== ""
                ? { name: root.name, properties }
                : properties;
            continue;
        }
        if (attr === "input_mappings" && factory === "runAutomation") {
            if (!Array.isArray(value))
                throw new EntityTfError(`${block.type}.${block.label}: input_mappings must be an array`);
            args.inputs = value.map((item) => {
                if (!isObject(item) || item.parameter_id === undefined || item.value === undefined) {
                    throw new EntityTfError(`${block.type}.${block.label}: invalid input mapping`);
                }
                const parameter = item.parameter_id;
                if (isTfExpr(parameter) &&
                    (parameter.$expr === "resource-attr" &&
                        parameter.type === "elementum_on_demand_trigger" ||
                        parameter.$expr === "data-attr" &&
                            parameter.type === AUTOMATION_RESOURCE_TYPE) &&
                    parameter.path[0] === "parameter_ids" &&
                    typeof parameter.path[1] === "object" &&
                    parameter.path[1] !== null &&
                    "index" in parameter.path[1] &&
                    typeof parameter.path[1].index === "string") {
                    return { name: parameter.path[1].index, value: decodeValue(item.value, ctx) };
                }
                // ei emits bare platform parameter UUIDs when the target automation's
                // onDemand trigger is not in this export (foreign / undiscovered).
                // Throwing here used to abort the WHOLE parent automation extract, so
                // skill tools that only needed the automation catalog key (e.g.
                // ridesTriggerRides) were dropped on build. Keep extract alive with an
                // unresolved placeholder — printer emits a TODO, build stays honest.
                if (typeof parameter === "string" && parameter !== "") {
                    return {
                        name: parameter,
                        value: decodeValue(item.value, ctx),
                    };
                }
                throw new EntityTfError(`${block.type}.${block.label}: input mapping parameter_id is not a named trigger parameter reference`);
            });
            continue;
        }
        // `file_reader_id` resolves to a real `documentModel` RefExpr only
        // through decodeValue's resource-attr branch (an implemented reader
        // type's own resource block in this export). When ei instead emits a
        // bare platform id — an unimplemented reader type (ai/xml, per
        // `IMPLEMENTED_FILE_READER_RESOURCE_TYPES`) or a dangling/foreign file
        // reader this export never discovered — decodeValue's generic fallback
        // would otherwise wrap it as a plain `{kind: "literal"}` string, which
        // the printer emits as a bare string literal where readFile's `fileReader`
        // option requires a `FileReaderCatalogRef` object, failing tsc on the
        // generated file (edk#483). Route it through the same `unresolved`
        // escape hatch already used for other unreconstructable references
        // (`"<uuid>" as any /* TODO: ... */`) instead.
        if (attr === "file_reader_id" && factory === "documentModel") {
            const decoded = decodeValue(value, ctx);
            args.documentModel =
                typeof decoded === "object" && decoded !== null && decoded.kind === "documentModel"
                    ? decoded
                    : { kind: "unresolved", uuid: typeof value === "string" ? value : JSON.stringify(value), hint: "file reader" };
            continue;
        }
        if (attr === "calculations" && factory === "calculation") {
            if (!Array.isArray(value))
                throw new EntityTfError("calculations must be an array");
            args.values = Object.fromEntries(value.map((item) => {
                if (!isObject(item) || typeof item.variable_name !== "string" || item.formula === undefined) {
                    throw new EntityTfError(`${block.type}.${block.label}: invalid calculation entry`);
                }
                const decodedExpression = decodeCalculationFormula(item.formula, ctx);
                const expression = isObject(decodedExpression) && decodedExpression.kind === "literal"
                    ? pulledRawCalculation(item.formula, item.output_type, item.multiple) ?? decodedExpression
                    : decodedExpression;
                const include = item.include_type === "MANUAL" ? "manual"
                    : item.include_type === "BOTH" ? "both"
                        : item.include_type === "PICKLIST" && item.picklist_field_id !== undefined
                            ? { picklist: fieldRef(item.picklist_field_id, ctx) ?? decodeValue(item.picklist_field_id, ctx) }
                            : undefined;
                if (item.id !== undefined && typeof item.id !== "string") {
                    throw new EntityTfError(`${block.type}.${block.label}: calculation entry id must be a string`);
                }
                const advanced = item.id !== undefined || item.record_id !== undefined || include !== undefined;
                return [item.variable_name, advanced ? {
                        expression,
                        ...(typeof item.id === "string" ? { id: item.id } : {}),
                        ...(item.record_id !== undefined ? { record: decodeValue(item.record_id, ctx) } : {}),
                        ...(include !== undefined ? { include } : {}),
                    } : expression];
            }));
            continue;
        }
        if (attr === "categories" && factory === "aiClassify") {
            if (!Array.isArray(value))
                throw new EntityTfError("classification categories must be an array");
            args.categorySource = {
                type: "static",
                categories: value.map((item) => {
                    if (!isObject(item) || item.label === undefined)
                        throw new EntityTfError("invalid classification category");
                    return {
                        name: decodeValue(item.label, ctx),
                        ...(item.description !== undefined ? { description: decodeValue(item.description, ctx) } : {}),
                    };
                }),
            };
            continue;
        }
        if (attr === "table_id" && factory === "tableRecordsSearch") {
            const foreign = typeof value === "string" ? ctx.foreignTableRefsById?.get(value) : undefined;
            args.table = foreign !== undefined
                ? { kind: "table", tableHandle: foreign.tableRefName, name: foreign.name }
                : typeof value === "string"
                    ? { kind: "unresolved", uuid: value, hint: "table" }
                    : decodeValue(value, ctx);
            continue;
        }
        if (attr === "search_table_id" && factory === "aiSearchTable") {
            args.table = decodeSearchTableReference(value, ctx);
            continue;
        }
        if (factory === "approvalChain" && attr === "approval_process_id") {
            args.approvalProcess = decodeValue(value, ctx);
            continue;
        }
        if (factory === "approvalChain" && attr === "record_reference") {
            args.record = decodeValue(value, ctx);
            continue;
        }
        if (factory === "approvalChain" && attr === "requester_reference") {
            const fieldType = approverReferenceFieldAuthoringType(value, ctx);
            if (fieldType === UNVERIFIABLE_FOREIGN_FIELD_TYPE) {
                throw new EntityTfError(`${block.type}.${block.label}: requester_reference targets a field on a foreign aspect that has never been ` +
                    "pulled locally — its type cannot be verified against the platform's required USER_ID reference; pull that " +
                    "aspect first (`elementum pull app`/`pull element`), then re-pull this app (edk#768)");
            }
            if (fieldType !== undefined && fieldType !== "USER") {
                throw new EntityTfError(`${block.type}.${block.label}: requester_reference targets a "${fieldType}" field, not a USER field — ` +
                    `the platform's approval-chain validation requires a USER_ID type reference (edk#655)`);
            }
            args.requester = decodeValue(value, ctx);
            continue;
        }
        if (factory === "approvalChain" && attr === "approver_references") {
            if (!Array.isArray(value))
                throw new EntityTfError(`${block.type}.${block.label}: approver_references must be an array`);
            for (const item of value) {
                const fieldType = approverReferenceFieldAuthoringType(item, ctx);
                if (fieldType === UNVERIFIABLE_FOREIGN_FIELD_TYPE) {
                    throw new EntityTfError(`${block.type}.${block.label}: approver_references targets a field on a foreign aspect that has never been ` +
                        "pulled locally — its type cannot be verified against the platform's required USER_ID reference; pull that " +
                        "aspect first (`elementum pull app`/`pull element`), then re-pull this app (edk#768)");
                }
                if (fieldType !== undefined && fieldType !== "USER") {
                    throw new EntityTfError(`${block.type}.${block.label}: approver_references targets a "${fieldType}" field, not a USER field — ` +
                        `the platform's approval-chain validation requires a USER_ID type reference (edk#655)`);
                }
            }
            args.approvers = value.map((item) => decodeValue(item, ctx));
            continue;
        }
        if (attr === "list" && factory === "forEach") {
            args.collection = decodeValue(value, ctx);
            continue;
        }
        if (attr === "code" && factory === "executeScript") {
            args.code = value;
            continue;
        }
        if (attr === "record_id" && factory === "saveAttachment") {
            args.recordRef = decodeValue(value, ctx);
            continue;
        }
        if (attr === "source_record_ids" && factory === "relateRecords") {
            const decoded = decodeValue(value, ctx);
            args.sources = Array.isArray(decoded) ? decoded : [decoded];
            continue;
        }
        if (attr === "output_type" && factory === "aiAgent") {
            args.outputType = typeof value === "string" ? value.toLowerCase() : decodeValue(value, ctx);
            continue;
        }
        if (attr === "json_fields" && factory === "aiAgent") {
            const outputType = block.attrs.output_type;
            if (typeof outputType !== "string" || outputType.toLowerCase() !== "json")
                continue;
            if (!Array.isArray(value))
                throw new EntityTfError(`${block.type}.${block.label}: json_fields must be an array`);
            args.schema = Object.fromEntries(value.map((field) => {
                if (!isObject(field) || typeof field.name !== "string" || typeof field.type !== "string") {
                    throw new EntityTfError(`${block.type}.${block.label}: invalid json_fields entry`);
                }
                return [
                    field.name,
                    {
                        type: field.type.toLowerCase(),
                        ...(typeof field.description === "string" && field.description !== ""
                            ? { description: field.description }
                            : {}),
                        ...(typeof field.required === "boolean" ? { required: field.required } : {}),
                    },
                ];
            }));
            continue;
        }
        // procedure()'s wire shape wraps each positional argument as
        // `{index, value}` (see tasks.ts's procedure()); ProcedureOptions.parameters
        // is a plain, position-ordered array - unwrap rather than round-tripping
        // the index wrapper verbatim, which procedure() doesn't accept.
        if (attr === "parameters" && factory === "procedure") {
            if (!Array.isArray(value))
                throw new EntityTfError(`${block.type}.${block.label}: procedure parameters must be an array`);
            const sorted = [...value].sort((a, b) => {
                const ai = isObject(a) && typeof a.index === "number" ? a.index : 0;
                const bi = isObject(b) && typeof b.index === "number" ? b.index : 0;
                return ai - bi;
            });
            args.parameters = sorted.map((item) => {
                if (!isObject(item) || item.value === undefined)
                    throw new EntityTfError(`${block.type}.${block.label}: invalid procedure parameter`);
                return decodeValue(item.value, ctx);
            });
            continue;
        }
        // createRecord's relate-to-record feature is carried by ONE attribute on
        // the wire (relate_to_record_reference) - its presence alone implies
        // relateToRecord: true (the provider has no separate boolean attribute).
        if (attr === "relate_to_record_reference" && factory === "createRecord") {
            args.record = decodeValue(value, ctx);
            args.relateToRecord = true;
            continue;
        }
        // elementum_file_reader_task's four data-source attrs are mutually
        // exclusive (the provider schema enforces exactly one) - readFile's
        // authoring shape collapses them into one discriminated dataSource arg.
        if (factory === "documentModel" && (attr === "raw_text" || attr === "attachment" || attr === "attachment_id" || attr === "file")) {
            const type = attr === "raw_text" ? "rawText" : attr === "attachment_id" ? "attachmentId" : attr;
            args.dataSource = { type, value: decodeValue(value, ctx) };
            continue;
        }
        if (attr === "source_record_ids" && factory === "relateRecords") {
            const decoded = decodeValue(value, ctx);
            args.sources = Array.isArray(decoded) ? decoded : [decoded];
            continue;
        }
        // apiRequest's bearer_auth/oauth/custom_body/multipart_body (and
        // basic_auth's reference_password) carry nested multi-word wire
        // attribute names - the generic decodeValue object walk below preserves
        // nested keys verbatim (snake_case, as exported), so these need the
        // same explicit per-key mapping as tasks.ts's matching to-tf encode.
        if (attr === "basic_auth" && factory === "apiRequest") {
            if (!isObject(value))
                throw new EntityTfError(`${block.type}.${block.label}: basic_auth must be an object`);
            args.basicAuth = {
                username: decodeValue(value.username, ctx),
                ...(value.reference_password !== undefined ? { referencePassword: decodeValue(value.reference_password, ctx) } : {}),
            };
            continue;
        }
        if (attr === "bearer_auth" && factory === "apiRequest") {
            if (!isObject(value))
                throw new EntityTfError(`${block.type}.${block.label}: bearer_auth must be an object`);
            args.bearerAuth = {
                ...(value.token !== undefined ? { token: value.token } : {}),
                ...(value.reference_token !== undefined ? { referenceToken: decodeValue(value.reference_token, ctx) } : {}),
            };
            continue;
        }
        if (attr === "oauth" && factory === "apiRequest") {
            if (!isObject(value))
                throw new EntityTfError(`${block.type}.${block.label}: oauth must be an object`);
            const headers = Array.isArray(value.headers)
                ? value.headers.map((h) => {
                    if (!isObject(h))
                        throw new EntityTfError(`${block.type}.${block.label}: invalid oauth header`);
                    return { name: h.name, value: decodeValue(h.value, ctx) };
                })
                : undefined;
            args.oauth = {
                requestType: value.request_type,
                ...(value.url !== undefined ? { url: decodeValue(value.url, ctx) } : {}),
                ...(value.client_id !== undefined ? { clientId: value.client_id } : {}),
                ...(value.client_secret !== undefined ? { clientSecret: value.client_secret } : {}),
                ...(headers !== undefined ? { headers } : {}),
            };
            continue;
        }
        if (attr === "form_body" && factory === "apiRequest") {
            if (!Array.isArray(value))
                throw new EntityTfError(`${block.type}.${block.label}: form_body must be an array`);
            args.formBody = value.map((item) => {
                if (!isObject(item))
                    throw new EntityTfError(`${block.type}.${block.label}: invalid form_body entry`);
                return { key: item.key, value: decodeValue(item.value, ctx) };
            });
            continue;
        }
        if (attr === "custom_body" && factory === "apiRequest") {
            if (!isObject(value))
                throw new EntityTfError(`${block.type}.${block.label}: custom_body must be an object`);
            args.customBody = {
                contentType: value.content_type,
                content: decodeValue(value.content, ctx),
            };
            continue;
        }
        if (attr === "multipart_body" && factory === "apiRequest") {
            if (!isObject(value) || !Array.isArray(value.parts))
                throw new EntityTfError(`${block.type}.${block.label}: multipart_body.parts must be an array`);
            args.multipartBody = {
                parts: value.parts.map((part) => {
                    if (!isObject(part))
                        throw new EntityTfError(`${block.type}.${block.label}: invalid multipart_body part`);
                    return {
                        name: part.name,
                        ...(part.value !== undefined ? { value: decodeValue(part.value, ctx) } : {}),
                        ...(part.attachment !== undefined ? { attachment: decodeValue(part.attachment, ctx) } : {}),
                        ...(part.attachment_id !== undefined ? { attachmentId: decodeValue(part.attachment_id, ctx) } : {}),
                        ...(part.file !== undefined ? { file: decodeValue(part.file, ctx) } : {}),
                    };
                }),
            };
            continue;
        }
        // elementum_notification_task's `message` wire attribute means
        // notification()'s `message` option, not postComment()'s `contents` -
        // ATTR_TO_ARG's message → "contents" entry is correct for
        // elementum_message_task (which shares the same wire attribute name)
        // but wrong here. See edk#112.
        if (attr === "message" && factory === "notification") {
            args.message = decodeValue(value, ctx);
            continue;
        }
        // Notification's dynamic-recipient HCL attributes share the names
        // `users`/`groups` with sendEmail, but their public options are
        // userRefs/groupRefs (not sendEmail's toUsers/toGroups).
        if (factory === "notification" && (attr === "users" || attr === "groups")) {
            args[attr === "users" ? "userRefs" : "groupRefs"] = decodeValue(value, ctx);
            continue;
        }
        const arg = ATTR_TO_ARG[attr] ?? attr.replace(/_([a-z])/g, (_, char) => char.toUpperCase());
        args[arg] = decodeValue(value, ctx);
    }
    if (["recordSearch", "createRecord", "updateField"].includes(factory))
        args.app ??= appRef(ctx.appRefName);
    if (factory === "findRelatedRecords") {
        args.relatedApp = args.app ?? appRef(ctx.appRefName);
        delete args.app;
    }
    if (factory === "procedure") {
        const procedure = args.procedureId;
        if (isObject(procedure) && procedure.kind === "procedure") {
            const cloudLink = args.cloudLinkId;
            if (!isObject(cloudLink) || cloudLink.kind !== "cloudLink" || cloudLink.handle !== procedure.cloudLinkKey) {
                throw new EntityTfError(`${block.type}.${block.label}: stored function and task cloudlink_id refer to different CloudLinks`);
            }
            args.procedure = procedure;
            delete args.procedureId;
            delete args.cloudLinkId;
        }
    }
    if (factory === "setVariable") {
        if (block.type === "elementum_variable_task") {
            if (typeof block.attrs.variable_name !== "string" || typeof block.attrs.variable_type !== "string")
                throw new EntityTfError("variable task is incomplete");
            return { create: [{ name: block.attrs.variable_name, type: block.attrs.variable_type, ...(block.attrs.value !== undefined ? { value: decodeValue(block.attrs.value, ctx) } : {}) }] };
        }
        // The provider's real attribute is variable_reference (renamed from a
        // prior "variable" - confirmed against update_variable_task_resource.go's
        // current schema); the AST/authoring key stays "variable" (SetVariableOptions.update[].variable).
        return { update: [{ variable: decodeValue(block.attrs.variable_reference, ctx), value: decodeValue(block.attrs.value, ctx) }] };
    }
    return args;
}
/**
 * Resolve an `elementum_unmanaged_task_reference` data block's OWN real
 * position in the task tree from its `operator_child_id`/`operator_task_id`/
 * `parent_is_trigger` attributes (edk#821) - the platform already records
 * exactly which switch case / fork-join branch / forEach body (or the
 * trigger) the unsupported task sits in; this was previously discarded
 * entirely in favor of always anchoring to the trigger. Returns `undefined`
 * when none of those attributes resolve to an address this automation
 * actually owns, so the caller can fall back to the trigger-anchor + diagnostic
 * safety net rather than pointing at an address nothing will ever visit.
 */
function resolveUnmanagedTaskPosition(dataBlock, triggerAddress, ownedAddresses) {
    if (dataBlock.attrs.parent_is_trigger === true)
        return triggerAddress;
    const operatorRef = resourceRef(dataBlock.attrs.operator_task_id);
    if (operatorRef?.type === "elementum_for_each_task") {
        const inner = `${operatorRef.type}.${operatorRef.label}.inner`;
        return ownedAddresses.has(`${operatorRef.type}.${operatorRef.label}`) ? inner : undefined;
    }
    const childRef = resourceRef(dataBlock.attrs.operator_child_id);
    if (childRef !== undefined) {
        const address = `${childRef.type}.${childRef.label}`;
        return ownedAddresses.has(address) ? address : undefined;
    }
    if (operatorRef !== undefined) {
        const address = `${operatorRef.type}.${operatorRef.label}`;
        return ownedAddresses.has(address) ? address : undefined;
    }
    return undefined;
}
/**
 * `triggerAddress`/`onUnmanagedParent` are the fallback of last resort: a
 * task whose real predecessor is an unsupported task type, when that
 * predecessor's own position couldn't be resolved either (see
 * `resolveUnmanagedTaskPosition`), is anchored to the trigger instead - not
 * its true live position, but keeps it (and anything chained after it) from
 * being silently dropped from the authored source. `unmanagedTaskPositions`
 * maps each such data block's OWN address to its resolved position (edk#821)
 * - when present, the downstream task anchors onto the placeholder's own
 * address (its position in the tree), not the placeholder's resolved
 * position directly, so `buildTasks`'s normal flat-chain recursion picks up
 * the whole downstream chain from there.
 */
function parentAddress(block, triggerAddress, unmanagedTaskPositions, onUnmanagedParent) {
    const parent = resourceRef(block.attrs.parent);
    if (parent === undefined) {
        const unmanagedRef = unmanagedTaskRef(block.attrs.parent);
        if (unmanagedRef !== undefined) {
            const address = `elementum_unmanaged_task_reference.${unmanagedRef.label}`;
            if (unmanagedTaskPositions.has(address))
                return address;
            onUnmanagedParent?.(block);
            return triggerAddress;
        }
        throw new EntityTfError(`${block.type}.${block.label}: missing or invalid parent`);
    }
    const suffix = parent.path.length > 0 ? `.${pathNames(parent.path).join(".")}` : "";
    return `${parent.type}.${parent.label}${suffix}`;
}
function taskRefKey(block, automationLabel, appTfLabel) {
    const autoChild = childPartFromScopedLabel(appTfLabel, automationLabel, "app");
    const taskChild = childPartFromScopedLabel(appTfLabel, block.label ?? "", "app");
    const tail = taskChild.startsWith(`${autoChild}_`) ? taskChild.slice(autoChild.length + 1) : taskChild;
    const taskName = typeof block.attrs.name === "string" ? block.attrs.name : undefined;
    if (taskName !== undefined) {
        const sanitized = sanitizeName(taskName);
        // The platform can export created-variable tasks under generated positional
        // names such as "lookup_result_state 1" even though current-API authoring
        // keys them by the variable they create. Restrict this reconciliation to
        // that positional export shape: named tasks such as "set_variable_task"
        // remain valid action identities used by workflow outputs.
        if (block.type === "elementum_variable_task" &&
            typeof block.attrs.variable_name === "string" &&
            block.attrs.variable_name !== "" &&
            /(?:\s|-)\d+$/.test(taskName.trim()) &&
            (sanitized === tail || sanitized === `_${tail}`)) {
            return block.attrs.variable_name;
        }
        // ei sanitizes `<automation>_<task>` as ONE combined string
        // (imports.go), so the digit-leading guard only ever sees the
        // automation's own first character - a task name that itself starts
        // with a digit loses its own underscore fix once the automation's
        // child part is stripped back off above, so `sanitized` (computed on
        // the task name alone) still carries the underscore `tail` lacks
        // (issue #79).
        const matches = sanitized === tail || sanitized === `_${tail}`;
        if (matches && (taskName === tail || /[_\s-]/.test(taskName)))
            return taskName;
    }
    return tfLabelToTsLabel(/^[0-9]/.test(tail) ? `_${tail}` : tail);
}
/**
 * Best-effort resource footprint of one automation, used ONLY when whole-
 * automation extraction throws (module.ts's `extractAutomations` catch
 * branch) so adoption can exclude the FULL graph - not just the automation
 * root - from its import scope. Without this, a trigger/task/branch/publish
 * address is individually still an "adoptable" resource TYPE (the kind
 * works fine for OTHER automations), so it gets imported into state anyway;
 * build then can't reproduce what it never wrote to source, and `plan`
 * shows those addresses as destroy-on-apply (confirmed live: several
 * automations that failed extraction were imported into state anyway,
 * driving spurious destroys). Deliberately lenient - tolerant of the exact
 * conditions that make real extraction fail (zero/multiple triggers, an
 * unrecognized task kind) - so it over-collects rather than under-collects:
 * a false positive here just leaves one more resource unmanaged, a false
 * negative would silently destroy a live automation on apply.
 */
export function automationOwnedAddresses(doc, automationLabel) {
    const owned = new Set([`${AUTOMATION_RESOURCE_TYPE}.${automationLabel}`]);
    for (const block of doc.blocks) {
        if (block.kind === "resource" && block.type !== undefined && block.label !== undefined &&
            references(block.attrs.automation, AUTOMATION_RESOURCE_TYPE, automationLabel)) {
            owned.add(`${block.type}.${block.label}`);
        }
    }
    let changed = true;
    while (changed) {
        changed = false;
        for (const block of doc.blocks) {
            if (block.kind !== "resource" || block.type === undefined || block.label === undefined)
                continue;
            const address = `${block.type}.${block.label}`;
            if (owned.has(address))
                continue;
            const parent = resourceRef(block.attrs.parent) ?? resourceRef(block.attrs.switch_task) ?? resourceRef(block.attrs.fork_join_task);
            if (parent !== undefined && owned.has(`${parent.type}.${parent.label}`)) {
                owned.add(address);
                changed = true;
            }
        }
    }
    return [...owned];
}
/** Bridge metadata for required values that GraphQL could not reconstruct. */
function unresolvedAuthoringValues(doc) {
    const values = [];
    for (const block of doc.blocks) {
        if (block.kind !== "locals")
            continue;
        for (const [key, payload] of Object.entries(block.attrs)) {
            if (!key.startsWith("edk_unresolved_authoring_") || typeof payload !== "string")
                continue;
            try {
                const parsed = JSON.parse(payload);
                if (typeof parsed.resource === "string" && typeof parsed.field === "string" && typeof parsed.reason === "string") {
                    values.push({ resource: parsed.resource, field: parsed.field, reason: parsed.reason });
                }
            }
            catch {
                // Malformed bridge metadata is ignored here. The ordinary extractor
                // validation still handles the underlying resource shape.
            }
        }
    }
    return values;
}
/** Extract one app-owned automation graph from an ei app export. */
function automationFromTfDocument(doc, automationLabel, appRefName, foreignFieldRefsById, foreignFieldRefsByDataAddress, foreignTableRefsById, picklistOptionLabelById, fieldNamesByOwnerRefName, foreignAppRefNames, foreignElementRefNames, procedureKeysByDiscriminator) {
    const automation = doc.blocks.find((block) => block.kind === "resource" && block.type === AUTOMATION_RESOURCE_TYPE && block.label === automationLabel);
    if (automation === undefined)
        throw new EntityTfError(`automation resource "${automationLabel}" not found`);
    assertAllowed(automation, new Set(["app_id", "name", "description", "status", "triggers_other_automations", "service_account_id", "depends_on"]));
    const app = resourceRef(automation.attrs.app_id);
    if (app === undefined || app.type !== "elementum_app" || app.path[0] !== "id")
        throw new EntityTfError(`${AUTOMATION_RESOURCE_TYPE}.${automationLabel}: app_id is not an app reference`);
    const triggers = doc.blocks.filter((block) => block.kind === "resource" && AUTOMATION_TRIGGER_TYPE_SET.has(block.type ?? "") && references(block.attrs.automation, AUTOMATION_RESOURCE_TYPE, automationLabel));
    const publish = doc.blocks.find((block) => block.kind === "resource" && block.type === WORKFLOW_PUBLISH_RESOURCE_TYPE && references(block.attrs.automation, AUTOMATION_RESOURCE_TYPE, automationLabel));
    const unknownDirectChild = doc.blocks.find((block) => block.kind === "resource" &&
        references(block.attrs.automation, AUTOMATION_RESOURCE_TYPE, automationLabel) &&
        block.type !== WORKFLOW_PUBLISH_RESOURCE_TYPE &&
        !AUTOMATION_TRIGGER_TYPE_SET.has(block.type ?? ""));
    if (unknownDirectChild) {
        throw new EntityTfError(`unknown automation resource type "${unknownDirectChild.type}"`);
    }
    if (triggers.length !== 1)
        throw new EntityTfError(`automation "${automationLabel}" must have exactly one trigger; found ${triggers.length}`);
    const trigger = triggers[0];
    assertAllowed(trigger, TRIGGER_ATTRIBUTES);
    if (publish !== undefined)
        assertAllowed(publish, PUBLISH_ATTRIBUTES);
    const branches = doc.blocks.filter((block) => block.kind === "resource" && AUTOMATION_BRANCH_TYPE_SET.has(block.type ?? ""));
    const taskCandidates = doc.blocks.filter((block) => block.kind === "resource" && ALL_AUTOMATION_TASK_TYPE_SET.has(block.type ?? ""));
    const resourceBlocks = new Map(doc.blocks.filter((block) => block.kind === "resource" && block.type && block.label).map((block) => [`${block.type}.${block.label}`, block]));
    // edk#785: workflow_publish.depends_on is this automation's own manifest of
    // everything it owns (Go's build emits exactly one entry per task/trigger/
    // branch belonging to it) - the only reliable way to scope a task back to
    // THIS automation, since taskCandidates spans the whole app (doc.blocks is
    // every automation's blocks combined) and task labels carry no ownership
    // guarantee of their own.
    const publishDependencyAddresses = new Set(Array.isArray(publish?.attrs.depends_on)
        ? publish.attrs.depends_on.map((value) => resourceRef(value)).filter((ref) => ref !== undefined).map((ref) => `${ref.type}.${ref.label}`)
        : []);
    const owned = new Set([`${trigger.type}.${trigger.label}`]);
    let changed = true;
    while (changed) {
        changed = false;
        for (const branch of branches) {
            const owner = resourceRef(branch.attrs.switch_task ?? branch.attrs.fork_join_task);
            if (owner && owned.has(`${owner.type}.${owner.label}`) && !owned.has(`${branch.type}.${branch.label}`)) {
                owned.add(`${branch.type}.${branch.label}`);
                changed = true;
            }
        }
        for (const task of taskCandidates) {
            const parent = resourceRef(task.attrs.parent);
            if (parent && owned.has(`${parent.type}.${parent.label}`) && !owned.has(`${task.type}.${task.label}`)) {
                owned.add(`${task.type}.${task.label}`);
                changed = true;
            }
            // edk#785: a data-source parent is always "resolved" (see unmanagedTaskRef) -
            // doesn't need its target to already be owned the way a resource-attr parent does.
            else if (!parent && unmanagedTaskRef(task.attrs.parent) && publishDependencyAddresses.has(`${task.type}.${task.label}`) && !owned.has(`${task.type}.${task.label}`)) {
                owned.add(`${task.type}.${task.label}`);
                changed = true;
            }
        }
    }
    const tasks = taskCandidates.filter((block) => owned.has(`${block.type}.${block.label}`));
    const ownedBranches = branches.filter((block) => owned.has(`${block.type}.${block.label}`));
    // A required value that the bridge could not reconstruct is not a literal
    // empty string. Exclude the whole automation graph from authoring/adoption,
    // just as we already do for an unresolvable field_id. Otherwise resolving
    // the foreign field through a direct catalog accidentally turns the bridge's
    // `value = ""` placeholder into managed configuration and adoption plans a
    // live null -> "" update.
    const unresolved = unresolvedAuthoringValues(doc).find(({ resource }) => owned.has(resource));
    if (unresolved !== undefined) {
        throw new EntityTfError(`${unresolved.resource}.${unresolved.field}: ${unresolved.reason}; ` +
            "the automation is excluded because no safe authored value or UUID fallback is available");
    }
    tasks.forEach((block) => assertAllowed(block, TASK_ATTRIBUTES));
    ownedBranches.forEach((block) => assertAllowed(block, BRANCH_ATTRIBUTES));
    const publishDependencies = Array.isArray(publish?.attrs.depends_on)
        ? publish.attrs.depends_on.map((value) => resourceRef(value)).filter((value) => value !== undefined)
        : [];
    for (const dependency of publishDependencies) {
        if (dependency.type.startsWith("elementum_") &&
            dependency.type !== trigger.type &&
            !ALL_AUTOMATION_TASK_TYPE_SET.has(dependency.type) &&
            !AUTOMATION_BRANCH_TYPE_SET.has(dependency.type) &&
            dependency.type !== WORKFLOW_PUBLISH_RESOURCE_TYPE) {
            throw new EntityTfError(`unknown automation resource type "${dependency.type}"`);
        }
        const address = `${dependency.type}.${dependency.label}`;
        if (!ALL_AUTOMATION_TASK_TYPE_SET.has(dependency.type) || owned.has(address))
            continue;
        const seen = new Set();
        let cursor = address;
        while (cursor !== undefined && !owned.has(cursor)) {
            if (seen.has(cursor))
                throw new EntityTfError(`cycle detected at ${cursor}`);
            seen.add(cursor);
            const block = resourceBlocks.get(cursor);
            if (block === undefined)
                throw new EntityTfError(`${cursor}: unresolved parent`);
            const parent = resourceRef(block.attrs.parent);
            if (parent === undefined)
                throw new EntityTfError(`${cursor}: missing parent`);
            cursor = `${parent.type}.${parent.label}`;
        }
        throw new EntityTfError(`automation "${automationLabel}" contains an orphaned task ${address}`);
    }
    for (const task of tasks) {
        const parent = resourceRef(task.attrs.parent);
        if (parent === undefined) {
            if (unmanagedTaskRef(task.attrs.parent) !== undefined)
                continue; // edk#785: always resolved, see unmanagedTaskRef
            throw new EntityTfError(`${task.type}.${task.label}: unresolved parent`);
        }
        if (!owned.has(`${parent.type}.${parent.label}`))
            throw new EntityTfError(`${task.type}.${task.label}: unresolved parent`);
        if (parent.path.length > 0 && !(parent.type === "elementum_for_each_task" && parent.path.length === 1 && parent.path[0] === "inner")) {
            throw new EntityTfError(`${task.type}.${task.label}: unsupported parent path`);
        }
    }
    const appTfLabel = app.label;
    const dataBlocks = new Map(doc.blocks.filter((block) => block.kind === "data" && block.type && block.label).map((block) => [`${block.type}.${block.label}`, block]));
    const taskByAddress = new Map(tasks.map((block) => [`${block.type}.${block.label}`, { block, refKey: taskRefKey(block, automationLabel, appTfLabel) }]));
    const adoptionAddressAliases = [...taskByAddress.entries()].flatMap(([from, { block, refKey }]) => {
        if (block.type !== "elementum_variable_task" ||
            typeof block.attrs.variable_name !== "string" ||
            block.attrs.variable_name === "")
            return [];
        const to = `${block.type}.${taskTfLabel(automationLabel, refKey)}`;
        return to === from ? [] : [{ from, to }];
    });
    const taskTargetOwnerRefNameByAddress = new Map();
    for (const [address, { block }] of taskByAddress) {
        const ownerRefName = aspectOwnerRefNameFromObjectId(block.attrs.object_id, appRefName, appTfLabel, dataBlocks, foreignAppRefNames, foreignElementRefNames);
        if (ownerRefName !== undefined)
            taskTargetOwnerRefNameByAddress.set(address, ownerRefName);
    }
    const resourceImportIds = new Map();
    for (const block of doc.blocks) {
        if (block.kind !== "import" || typeof block.attrs.id !== "string")
            continue;
        const target = resourceRef(block.attrs.to);
        if (target === undefined)
            continue;
        const unresolvedId = block.attrs.id.split(/[:/]/).at(-1);
        if (unresolvedId !== undefined && unresolvedId !== "")
            resourceImportIds.set(`${target.type}.${target.label}`, unresolvedId);
    }
    const fieldById = new Map();
    for (const [address, id] of resourceImportIds) {
        const block = resourceBlocks.get(address);
        if (block?.type?.endsWith("_field") !== true || block.label === undefined)
            continue;
        fieldById.set(id, {
            key: scopedLabelToBagKey(appTfLabel, block.label ?? ""),
            name: typeof block.attrs.name === "string" ? block.attrs.name : block.label,
            resourceType: block.type,
        });
    }
    const stagesByKey = new Map();
    const stagesAttr = resourceBlocks.get(`elementum_app.${app.label}`)?.attrs["stages"];
    if (Array.isArray(stagesAttr)) {
        for (const item of stagesAttr) {
            if (isObject(item) && !isTfExpr(item) && typeof item.key === "string" && typeof item.name === "string")
                stagesByKey.set(item.key, item.name);
        }
    }
    const ctx = {
        appRefName,
        appTfLabel: app.label,
        trigger,
        taskByAddress,
        resourceBlocks,
        dataBlocks,
        resourceImportIds,
        fieldById,
        stagesByKey,
        ...(foreignFieldRefsById !== undefined && { foreignFieldRefsById }),
        ...(foreignFieldRefsByDataAddress !== undefined && { foreignFieldRefsByDataAddress }),
        ...(foreignTableRefsById !== undefined && { foreignTableRefsById }),
        ...(picklistOptionLabelById !== undefined && { picklistOptionLabelById }),
        ...(fieldNamesByOwnerRefName !== undefined && { fieldNamesByOwnerRefName }),
        ...(taskTargetOwnerRefNameByAddress.size > 0 && { taskTargetOwnerRefNameByAddress }),
        ...(foreignAppRefNames !== undefined && { foreignAppRefNames }),
        ...(foreignElementRefNames !== undefined && { foreignElementRefNames }),
        ...(procedureKeysByDiscriminator !== undefined && { procedureKeysByDiscriminator }),
    };
    let serviceAccountName;
    const serviceAccountValue = automation.attrs.service_account_id;
    if (serviceAccountValue !== undefined &&
        isTfExpr(serviceAccountValue) &&
        "type" in serviceAccountValue &&
        serviceAccountValue.type === "elementum_service_account" &&
        (serviceAccountValue.$expr === "resource-attr" || serviceAccountValue.$expr === "data-attr")) {
        const reference = serviceAccountValue;
        const block = reference.$expr === "resource-attr"
            ? resourceBlocks.get(`${reference.type}.${reference.label}`)
            : dataBlocks.get(`${reference.type}.${reference.label}`);
        if (block !== undefined) {
            if (typeof block.attrs.name === "string")
                serviceAccountName = block.attrs.name;
            else if (typeof block.attrs.first_name === "string" && typeof block.attrs.last_name === "string") {
                serviceAccountName = `${block.attrs.first_name} ${block.attrs.last_name}`;
            }
        }
    }
    const dependencyOrder = new Map();
    if (publish && Array.isArray(publish.attrs.depends_on))
        publish.attrs.depends_on.forEach((dep, index) => { const ref = resourceRef(dep); if (ref)
            dependencyOrder.set(`${ref.type}.${ref.label}`, index); });
    const triggerAddress = `${trigger.type}.${trigger.label}`;
    const unrebuildableTasks = [];
    // edk#821: resolve each elementum_unmanaged_task_reference this automation's
    // OWN tasks actually parent onto (never the whole doc's data blocks - a
    // sibling automation sharing this export can have its own, edk#785) to its
    // real position, and merge it into the same address space real tasks live
    // in so its downstream chain lands in the right switch case / forkJoin
    // branch / forEach body instead of being hoisted to the automation's top level.
    const ownedAddresses = new Set([
        triggerAddress,
        ...tasks.map((t) => `${t.type}.${t.label}`),
        ...ownedBranches.map((b) => `${b.type}.${b.label}`),
    ]);
    const unmanagedTaskPositions = new Map();
    const placeholderTasks = [];
    for (const task of tasks) {
        const unmanagedRef = unmanagedTaskRef(task.attrs.parent);
        if (unmanagedRef === undefined)
            continue;
        const address = `elementum_unmanaged_task_reference.${unmanagedRef.label}`;
        if (unmanagedTaskPositions.has(address))
            continue;
        const dataBlock = dataBlocks.get(address);
        const position = dataBlock !== undefined ? resolveUnmanagedTaskPosition(dataBlock, triggerAddress, ownedAddresses) : undefined;
        if (dataBlock === undefined || position === undefined)
            continue; // falls back to parentAddress's trigger-anchor + diagnostic
        unmanagedTaskPositions.set(address, position);
        const refKey = typeof dataBlock.attrs.name === "string" && dataBlock.attrs.name !== "" ? dataBlock.attrs.name : tfLabelToTsLabel(dataBlock.label ?? "task");
        taskByAddress.set(address, { block: dataBlock, refKey });
        placeholderTasks.push({
            name: refKey,
            taskType: typeof dataBlock.attrs.task_type === "string" ? dataBlock.attrs.task_type : "unknown",
        });
    }
    const childrenByParent = new Map();
    for (const task of tasks) {
        const parent = parentAddress(task, triggerAddress, unmanagedTaskPositions, (b) => unrebuildableTasks.push(`${b.type}.${b.label}`));
        childrenByParent.set(parent, [...(childrenByParent.get(parent) ?? []), task]);
    }
    for (const [address, position] of unmanagedTaskPositions) {
        const dataBlock = taskByAddress.get(address).block;
        childrenByParent.set(position, [...(childrenByParent.get(position) ?? []), dataBlock]);
    }
    for (const list of childrenByParent.values())
        list.sort((a, b) => (dependencyOrder.get(`${a.type}.${a.label}`) ?? Number.MAX_SAFE_INTEGER) - (dependencyOrder.get(`${b.type}.${b.label}`) ?? Number.MAX_SAFE_INTEGER) || a.label.localeCompare(b.label));
    const visiting = new Set();
    const visited = new Set();
    const buildTasks = (parent) => (childrenByParent.get(parent) ?? []).flatMap((block, siblingIndex) => {
        const address = `${block.type}.${block.label}`;
        if (visiting.has(address))
            throw new EntityTfError(`cycle detected at ${address}`);
        visiting.add(address);
        visited.add(address);
        const factory = TASK_FACTORIES[block.type];
        if (factory === undefined)
            throw new EntityTfError(`unknown task resource type "${block.type}"`);
        const refKey = taskByAddress.get(address).refKey;
        let children;
        if (factory === "forEach")
            children = [{ label: null, tasks: buildTasks(`${address}.inner`) }];
        if (factory === "switchTask" || factory === "forkJoin") {
            const branchAttr = factory === "switchTask" ? "switch_task" : "fork_join_task";
            const previousAttr = factory === "switchTask" ? "previous_case_id" : "previous_branch_id";
            const matching = ownedBranches.filter((branch) => references(branch.attrs[branchAttr], block.type, block.label));
            const ordered = orderBranchesByPreviousLink(matching, previousAttr, dependencyOrder);
            children = ordered.map((branch) => {
                const shape = factory === "switchTask" ? defaultSwitchShape(branch) : undefined;
                const isDefault = shape !== undefined;
                const rawLabel = typeof branch.attrs.label === "string" ? branch.attrs.label : undefined;
                return {
                    ...(isDefault ? { label: null, isDefault: true } : rawLabel !== undefined ? { label: rawLabel } : {}),
                    // ei always writes SOME display label on the default case, but it's
                    // not always the literal string "Default" (e.g. a manual rename to
                    // "Default Path") - carry the real text so the printer/build round
                    // trip can reproduce it instead of hardcoding "Default".
                    ...(isDefault && rawLabel !== undefined ? { defaultLabel: rawLabel } : {}),
                    // Carry the observed wire shape only when it's the older
                    // filter-based one - the newer is_default shape is to-tf.ts's
                    // unmarked default, so leaving this unset there keeps printed
                    // output terse and matches a freshly-authored default branch.
                    ...(shape === "explicitTrueFilter" ? { defaultFilterMode: shape } : {}),
                    ...(!isDefault && branch.attrs.filter !== undefined ? { filter: decodeFilter(branch.attrs.filter, ctx) } : {}),
                    tasks: buildTasks(`${branch.type}.${branch.label}`),
                };
            });
        }
        const parent = resourceRef(block.attrs.parent);
        const unmanagedParentRef = parent === undefined ? unmanagedTaskRef(block.attrs.parent) : undefined;
        const parentTask = parent !== undefined
            ? taskByAddress.get(`${parent.type}.${parent.label}`)
            : unmanagedParentRef !== undefined
                ? taskByAddress.get(`elementum_unmanaged_task_reference.${unmanagedParentRef.label}`)
                : undefined;
        // A sole/first child of its real parent is exactly what buildTasks's own
        // recursion already prints immediately after that parent, so an explicit
        // anchor is redundant with implicit sequential chaining - only a 2nd+
        // sibling sharing the same real parent is actually ambiguous without one.
        const hclParent = siblingIndex === 0
            ? undefined
            : parent?.type === trigger.type && parent?.label === trigger.label ? "trigger" : parentTask?.refKey;
        const args = factory === "unimplementedTask"
            ? {
                id: typeof block.attrs.id === "string" ? block.attrs.id : "",
                taskType: typeof block.attrs.task_type === "string" ? block.attrs.task_type : "",
            }
            : taskArgs(block, factory, ctx);
        const call = { factory, refKey, args, ...(children ? { children } : {}), ...(typeof block.attrs.name === "string" && block.attrs.name !== refKey ? { displayName: block.attrs.name } : {}), ...(hclParent !== undefined ? { hclParent } : {}) };
        visiting.delete(address);
        return [call, ...buildTasks(address)];
    });
    const topTasks = buildTasks(`${trigger.type}.${trigger.label}`);
    if (visited.size !== tasks.length + unmanagedTaskPositions.size)
        throw new EntityTfError(`automation "${automationLabel}" contains an orphaned task graph`);
    const outputBlocks = publish?.attrs.outputs;
    const outputs = Array.isArray(outputBlocks) ? outputBlocks.map((output) => {
        if (!isObject(output) || typeof output.name !== "string" || output.value === undefined)
            throw new EntityTfError("invalid workflow output");
        // Export/provider Read emit jsonencode({fieldType, taskReference, ...});
        // collapse that envelope to a RefExpr the same way filter references do.
        const value = decodeFilterReferenceEnvelope(output.value, ctx, output.name)
            ?? decodeValue(output.value, ctx);
        return { name: output.name, value };
    }) : undefined;
    const exportedRevision = publish?.attrs.workflow_revision;
    if (exportedRevision !== undefined && typeof exportedRevision !== "string") {
        throw new EntityTfError(`${WORKFLOW_PUBLISH_RESOURCE_TYPE}.${automationLabel}: workflow_revision must be a string`);
    }
    const ast = {
        ownerHandle: appRefName,
        name: typeof automation.attrs.name === "string" ? automation.attrs.name : "",
        workflowRevision: exportedRevision ?? "1.0.0",
        ...(typeof automation.attrs.description === "string" ? { description: automation.attrs.description } : {}),
        ...(typeof automation.attrs.triggers_other_automations === "boolean" ? { triggersOtherAutomations: automation.attrs.triggers_other_automations } : {}),
        ...(serviceAccountName !== undefined ? { serviceAccountName } : {}),
        status: publish ? "ACTIVE" : automation.attrs.status === "INACTIVE" ? "INACTIVE" : "DRAFT",
        trigger: triggerFromBlock(trigger, ctx),
        tasks: topTasks,
        ...(outputs && outputs.length > 0 ? { outputs } : {}),
    };
    return {
        refName: scopedLabelToBagKey(appTfLabel, automationLabel),
        tfLabel: automationLabel,
        ast,
        blocks: [automation, trigger, ...tasks, ...ownedBranches, ...(publish ? [publish] : [])],
        adoptionAddressAliases,
        ...((unrebuildableTasks.length > 0 || placeholderTasks.length > 0) && {
            diagnostics: [
                ...unrebuildableTasks.map((address) => `automation "${automationLabel}": ${address}'s real parent is a task type EDK doesn't support yet, so its authored position couldn't be reconstructed (anchored to the trigger instead) - not rebuildable from this source until that task type is supported`),
                ...placeholderTasks.map(({ name, taskType }) => `automation "${automationLabel}" uses platform task type "${taskType}" ("${name}"), which EDK doesn't implement - kept as an unimplementedTask(...) placeholder; its configuration isn't authorable, but its position and downstream chain are round-trip-safe`),
            ],
        }),
    };
}
export function automationFromTf(first, second, third, fourth, fifth, sixth, seventh, eighth, ninth, tenth, eleventh) {
    if (Array.isArray(first)) {
        const ctx = second;
        const automationBlock = first.find((block) => block.kind === "resource" && block.type === AUTOMATION_RESOURCE_TYPE);
        if (automationBlock?.label === undefined) {
            return { skippedReason: "automation resource not found" };
        }
        try {
            const result = automationFromTfDocument({ blocks: [...first] }, automationBlock.label, ctx.ownerHandle, ctx.foreignFieldRefsById, ctx.foreignFieldRefsByDataAddress, ctx.foreignTableRefsById, ctx.picklistOptionLabelById, ctx.fieldNamesByOwnerRefName, ctx.foreignAppRefNames, ctx.foreignElementRefNames, ctx.procedureKeysByDiscriminator);
            if (result.diagnostics)
                ctx.diagnostics.push(...result.diagnostics);
            return { automation: result.ast };
        }
        catch (err) {
            const message = err instanceof Error ? err.message : String(err);
            ctx.diagnostics.push(message);
            return { skippedReason: message };
        }
    }
    return automationFromTfDocument(first, second, third ?? "", fourth, fifth, sixth, seventh, eighth, ninth, tenth, eleventh);
}
