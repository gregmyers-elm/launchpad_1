/**
 * v2 display-block authoring — the record view (LAYOUT) and new-record form
 * (CREATE) surfaces. Field blocks carry a `FieldRef` token at `fieldId`
 * (resolved at deploy); group blocks nest children. Mirrors v1's
 * `AspectDisplayBlockInput` shape.
 */
import type { FieldRef } from "../../catalog/index.js";
import type { UnsafePlatformId } from "../../core/platform-id.js";
export type DisplayBlock = {
    readonly kind: "field";
    readonly field: FieldRef;
} | {
    readonly kind: "group";
    readonly name: string;
    readonly blocks: readonly DisplayBlock[];
    readonly color?: string;
    readonly icon?: string;
    readonly maxColumns?: number;
} | {
    readonly kind: "widget";
    readonly widgetId: UnsafePlatformId;
} | {
    readonly kind: "comments";
    readonly channels?: readonly string[];
};
/** Serialize a display-block list for one surface; `undefined`/empty → omitted. */
export declare function serializeDisplayBlocks(blocks: readonly DisplayBlock[] | undefined, displayType: "LAYOUT" | "CREATE"): Record<string, unknown>[] | undefined;
