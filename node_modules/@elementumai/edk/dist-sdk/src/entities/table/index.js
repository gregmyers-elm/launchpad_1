/**
 * v2 table authoring — `table`. Org-owned top-level entity.
 * Standalone from v1's `src/tables`.
 */
export { table, bigQueryTableMapping, databricksTableMapping, snowflakeTableMapping, snowflakeTableView, tablePayloadToCreateRequest, tablePayloadToReadOnlyUpdateRequest, tablePayloadToUpdateRequest, } from "./build.js";
export { calculationColumn, bigQueryColumn, serializeTableField, snowflakeColumn, sourceColumn, tableAggregation, tableColumnTag, tableColumnType, tableExpansion, } from "./fields.js";
export { serializeJoin, tableJoin, tableJoinType, } from "./joins.js";
export * as filter from "../shared/filters/index.js";
// `table.fromJSON` has no callable to attach to (the builder returns a
// configured object), so the table pull orchestrator
// ships as a standalone named export — mirroring the other materialize-backed
// entities (agents, skills, aspects, automations).
