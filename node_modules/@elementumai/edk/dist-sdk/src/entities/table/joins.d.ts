/**
 * v2 table joins — `tableJoin`. Parallel to v1 `src/tables/joins`. The join's
 * `source` (the table/aspect joined in) is a catalog token resolved at deploy;
 * the `(left, right)` field-id pairs may be raw ids, app/element field tokens,
 * or table-column tokens resolved at deploy.
 */
import type { TableJoinType as WireTableJoinType } from "../../../elementum-entity-client/clients/tables/models/index.js";
import type { AppOrElementRef, FieldRef, TableColumnRef, TableRef } from "../../catalog/index.js";
import type { Filter } from "../shared/filters/types.js";
type TableJoinFieldRef = FieldRef | TableColumnRef;
declare const tableJoinTypeBrand: unique symbol;
declare const tableJoinBrand: unique symbol;
export type TableJoinType = WireTableJoinType & {
    readonly [tableJoinTypeBrand]: "TableJoinType";
};
export declare const tableJoinType: {
    readonly full: TableJoinType;
    readonly inner: TableJoinType;
    readonly left: TableJoinType;
    readonly right: TableJoinType;
};
export interface TableJoinFieldPair {
    readonly left: TableJoinFieldRef;
    readonly right: TableJoinFieldRef;
}
export interface TableJoinOptions {
    /** The table/aspect joined into this one (token → id at deploy). */
    readonly source: AppOrElementRef | TableRef;
    readonly type: TableJoinType;
    readonly on: readonly TableJoinFieldPair[];
    readonly filter?: Filter;
}
export type TableJoin = TableJoinOptions & {
    readonly [tableJoinBrand]: "TableJoin";
};
export declare function tableJoin(opts: TableJoinOptions): TableJoin;
/** Serialize a join to the wire shape (source token left in place for deploy). */
export declare function serializeJoin(def: TableJoin): Record<string, unknown>;
export {};
