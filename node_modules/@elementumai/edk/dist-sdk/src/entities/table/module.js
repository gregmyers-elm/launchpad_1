/**
 * Org-owned table graph — authoring root `tables/`, Terraform build and pull.
 */
import { tfLabelToTsLabel } from "../../engine/tf/index.js";
import { renderTableCatalogEntry } from "./catalog-render.js";
import { tableFromTf } from "./from-tf.js";
import { tableToTf } from "./to-tf.js";
export const TABLE_RESOURCE_TYPE = "elementum_table";
function extract(stream, ctx) { const blocks = stream.byKind.get("table") ?? []; if (blocks.length !== 1)
    throw new Error(`expected exactly one ${TABLE_RESOURCE_TYPE}, found ${blocks.length}`); const table = tableFromTf(blocks[0], ctx, stream.byKind.get("datamine") ?? []); return { instance: { ...table, refName: tfLabelToTsLabel(blocks[0].label ?? table.handle) } }; }
export const tableModule = {
    kind: "table",
    stream: { role: "streamRoot", exportVerb: "tables" },
    authoring: { unit: "ownFile", bag: "tables" },
    parents: [{ parent: "org", child: "table" }],
    resource: { shape: "single", resourceType: TABLE_RESOURCE_TYPE, adoptable: true },
    // Cross-table relationships already emit name-keyed data lookups through
    // OrgRefLookup.rawLookup; register that real build surface in the graph.
    data: { participates: true, resourceType: "elementum_table", lookupAttr: "name", referencedByBuild: true, referenceLookups: [{ strategy: "rootName" }] },
    lifecycle: { participates: true, extractStreamRoot: extract, toTf: (entity, ctx) => tableToTf(entity, { orgRefs: ctx.orgRefs, diagnostics: ctx.diagnostics, workspaceAspects: ctx.workspaceAspects }) },
    canvas: { participates: true, label: "Tables", glyph: "▤", component: "generic" },
    catalog: {
        participates: true,
        refKind: "table",
        catalogBag: "tables",
        renderOrder: 40,
        catalogRoot: "org",
        renderEntry: renderTableCatalogEntry,
        stubRef: (refName) => ({ __kind: "table", refName, name: refName }),
        refFromAuthoring: () => undefined,
    },
};
