/**
 * Element extraction: `elementum_element` + owned field resources → the
 * `NormalizedApp` shape (`type: "ELEMENT"`) that `printElementV2` consumes.
 * Mirrors `app/from-tf.ts` without app-only status/stages dialect.
 */
import { type FromTfContext } from "../../engine/pull/context.js";
import { type TfBlock } from "../../engine/tf/index.js";
import type { NormalizedApp } from "../app/deserializer/normalize.js";
export interface ElementFromTfResult {
    readonly element: NormalizedApp;
    /** org.ts `cloudLinks` bag key when the element pins one. */
    readonly cloudLinkHandle?: string;
}
export interface ElementFromTfViewBlocks {
    readonly layoutBlocks: readonly TfBlock[];
    readonly listViewBlocks: readonly TfBlock[];
    readonly calendarViewBlocks: readonly TfBlock[];
    readonly kanbanViewBlocks: readonly TfBlock[];
    readonly dashboardViewBlocks: readonly TfBlock[];
    readonly managedViewOrderBlocks: readonly TfBlock[];
    readonly recordCreateFormBlocks: readonly TfBlock[];
    readonly relationshipBlocks: readonly TfBlock[];
    readonly dashboardBlocks: readonly TfBlock[];
    readonly dashboardWidgetBlocks: readonly TfBlock[];
    readonly widgetBlocks: readonly TfBlock[];
}
export declare function elementFromTf(elementBlock: TfBlock, fieldBlocks: readonly TfBlock[], ctx: FromTfContext, viewBlocks?: ElementFromTfViewBlocks, accessPolicyBlocks?: readonly TfBlock[], roleBlocks?: readonly TfBlock[]): ElementFromTfResult;
