/**
 * Element emission: `element` DeployableEntity → `elementum_element` +
 * field resources in ei's dialect. No status_options/stages (apps always
 * emit those; element captures do not).
 */
import { type ToTfContext } from "../../engine/build/context.js";
import { type DeployableEntity } from "../../engine/entity.js";
import { type TfBlock } from "../../engine/tf/index.js";
export interface ElementToTfResult {
    readonly blocks: readonly TfBlock[];
    readonly fieldAddresses: ReadonlyMap<string, string>;
    readonly fieldNames: ReadonlyMap<string, string>;
}
/** `elementum_element` + one field resource per custom field. */
export declare function elementToTf(entity: DeployableEntity, ctx: ToTfContext): ElementToTfResult;
