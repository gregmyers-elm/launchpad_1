/** Caller-access modes documented by the phone-service HCL resource. */
export declare const callerAccessMode: {
    readonly allowAll: "ALLOW_ALL";
    readonly knownCallersOnly: "KNOWN_CALLERS_ONLY";
};
export type CallerAccessMode = (typeof callerAccessMode)[keyof typeof callerAccessMode];
/** A syntactically explicit international phone number. */
export type E164PhoneNumber = `+${number}`;
export interface PhoneServiceRegistration {
    readonly generateFirstMessage?: boolean;
    readonly expandLanguages?: boolean;
    /** The provider accepts a language string (for example, `en-US`). */
    readonly defaultLanguage?: string;
    readonly enableLanguageRouting?: boolean;
    readonly preferredLanguages?: readonly string[];
}
interface PhoneServiceNumberBase {
    readonly callerAccessMode?: CallerAccessMode;
    readonly registration?: PhoneServiceRegistration;
}
/** Exactly one HCL number configuration is required. */
export type PhoneServiceNumber = PhoneServiceNumberBase & ({
    readonly twilioExistingNumber: {
        readonly phoneNumber: E164PhoneNumber;
    };
    readonly twilioNewNumber?: never;
} | {
    readonly twilioExistingNumber?: never;
    readonly twilioNewNumber: {
        readonly areaCode: number;
    };
});
/** Runtime backstop for JavaScript callers and dynamically constructed input. */
export declare function validatePhoneService(name: string, service: PhoneServiceNumber): void;
export {};
