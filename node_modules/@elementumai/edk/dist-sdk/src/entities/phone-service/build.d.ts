import { type AgentRef, type CatalogRef, type PhoneProviderRef, type PhoneServiceRef } from "../../catalog/index.js";
import type { DeployableEntity, EntityDeployOptions } from "../../engine/entity.js";
import { type PhoneServiceNumber } from "./types.js";
/**
 * An app-owned phone line. Apps may own any number of phone services, and
 * each service independently chooses the agent that handles its calls.
 */
export type PhoneServiceOptions<AppRefName extends string = string> = EntityDeployOptions & PhoneServiceNumber & {
    readonly app: CatalogRef<"app", AppRefName>;
    readonly agent: AgentRef<NoInfer<AppRefName>>;
    readonly provider: PhoneProviderRef;
};
export interface PhoneServiceConfigured {
    ref(refName: string): PhoneServiceRef;
    def(refName: string): DeployableEntity;
}
export declare function phoneService<const AppRefName extends string>(opts: PhoneServiceOptions<AppRefName>): PhoneServiceConfigured;
