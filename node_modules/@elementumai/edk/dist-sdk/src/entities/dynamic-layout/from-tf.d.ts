import { type FromTfContext } from "../../engine/pull/context.js";
import { type TfBlock } from "../../engine/tf/index.js";
import type { NormalizedDynamicLayout } from "../app/deserializer/normalize.js";
export interface DynamicLayoutOwner {
    readonly type: string;
    readonly label: string;
    readonly ownerRefName: string;
    readonly ownerTfLabel: string;
}
export declare function dynamicLayoutsFromTf(blocks: readonly TfBlock[], owner: DynamicLayoutOwner, ctx: FromTfContext): NormalizedDynamicLayout[];
/**
 * Widget resource labels referenced by dynamic-layout automation buttons.
 * The label is the companion widget's stable Terraform identity; its display
 * name is mutable and cannot safely distinguish generated from authored
 * widgets during pull.
 */
export declare function dynamicLayoutCompanionWidgetLabels(blocks: readonly TfBlock[]): ReadonlySet<string>;
