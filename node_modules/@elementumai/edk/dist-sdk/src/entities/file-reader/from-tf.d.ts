import type { FromTfContext } from "../../engine/pull/context.js";
import { type TfBlock } from "../../engine/tf/index.js";
import { type FileReaderPayload } from "./build.js";
export interface NormalizedFileReader {
    readonly refName: string;
    readonly payload: FileReaderPayload;
}
export interface FileReaderFromTfResult {
    readonly fileReader?: NormalizedFileReader;
    /** Set when this block can't be authored on the terraform path — reported, not fatal (plan §6). */
    readonly skippedReason?: string;
}
export declare function fileReaderFromTf(block: TfBlock, appTfLabel: string, _ctx: FromTfContext): FileReaderFromTfResult;
