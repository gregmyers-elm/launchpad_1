/**
 * `DeployableEntity` (kind "documentModel", built by a file-reader factory) →
 * one `elementum_<excel|json|text>_file_reader` resource block. Mirrors
 * `skill/to-tf.ts`'s shape for a single-resource own-file kind.
 */
import { type DeployableEntity } from "../../engine/entity.js";
import { type TfBlock } from "../../engine/tf/index.js";
export interface FileReaderToTfContext {
    readonly appTfLabel: string;
}
export interface FileReaderToTfResult {
    readonly blocks: readonly TfBlock[];
}
export declare function fileReaderToTf(entity: DeployableEntity, ctx: FileReaderToTfContext): FileReaderToTfResult;
