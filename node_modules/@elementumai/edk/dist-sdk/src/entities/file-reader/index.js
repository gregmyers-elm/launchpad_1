/**
 * File-reader authoring — create/edit AI, Excel, JSON, text, and XML file
 * readers, referenced by automation file-reading tasks.
 *
 * The `Legacy*`/`@deprecated` exports below are the pre-terraform,
 * GraphQL-mutation-based API this file replaced — kept for published
 * `@elementumai/edk/fileReaders` API back-compat only, not used by
 * `elementum build`/`pull`. See `build.ts`'s "Legacy" section doc comment.
 */
export { aiFileReader, excelFileReader, jsonFileReader, textFileReader, xmlFileReader, fileReaderStructure, fileReaderFieldType, } from "./build.js";
