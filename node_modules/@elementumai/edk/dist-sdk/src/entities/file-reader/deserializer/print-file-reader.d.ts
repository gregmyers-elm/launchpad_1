import type { NormalizedFileReader } from "../from-tf.js";
export interface PrintFileReaderOptions {
    /** The owning app's refName — the file's `parent` option. */
    readonly parent: string;
    readonly parentScope: "app" | "element";
}
export declare function printFileReader(model: NormalizedFileReader, opts: PrintFileReaderOptions): string;
