/** The skill-tool entity's registry entry (see src/graph/). */
import { NO_CANVAS, NO_CATALOG, NO_DATA, NO_LIFECYCLE } from "../../graph/types.js";
import { FIELD_DATA_RESOURCE_TYPE } from "../field/module.js";
import { SKILL_TOOL_SUB_TYPES } from "./types.js";
export const skillToolModule = {
    kind: "skill-tool",
    stream: { role: "streamMember" },
    authoring: { unit: "inline", bag: "tools" },
    parents: [{ parent: "skill", child: "skill-tool" }],
    resource: {
        shape: "subTypes",
        rows: SKILL_TOOL_SUB_TYPES.map((t) => ({
            key: t.typeKey,
            resourceType: t.resourceType,
            implemented: t.implemented === true,
        })),
    },
    data: NO_DATA,
    lifecycle: NO_LIFECYCLE,
    canvas: NO_CANVAS,
    catalog: NO_CATALOG,
    roundtrip: {
        referenceAliases: [],
        resolvedDataReferences: [{
                attribute: "fields",
                nestedAttribute: "field_id",
                dataType: FIELD_DATA_RESOURCE_TYPE,
            }],
    },
};
