import { type ChildResourceScope, type TfBlock, type TfValue } from "../../engine/tf/index.js";
import { type OrgRefLookup, type WorkspaceAspect } from "../../engine/build/context.js";
import type { Filter } from "../shared/filters/types.js";
export interface AppDashboardWire {
    readonly name?: string;
    readonly favorite?: boolean;
    readonly tags?: readonly string[];
    readonly widgets?: Readonly<Record<string, DashboardWidgetWire>>;
}
interface RefWire {
    readonly __kind?: string;
    readonly refName?: string;
    readonly name?: string;
    readonly parent?: string;
    readonly namespace?: string;
}
interface DashboardWidgetWireBase {
    readonly name: string;
    readonly size?: string;
    readonly color?: string;
    readonly icon?: string;
}
type DashboardWidgetWire = (DashboardWidgetWireBase & {
    readonly type: "records";
    readonly target?: RefWire | string;
    readonly filter?: Filter;
}) | (DashboardWidgetWireBase & {
    readonly type: "chart";
    readonly chart?: RefWire | string;
});
export interface DashboardToTfOptions {
    readonly ownerTfLabel: string;
    readonly scope: ChildResourceScope;
    readonly ownerType: "APP_ASPECT" | "ELEMENT_ASPECT";
    readonly ownerId: TfValue;
    readonly ownerRefName?: string;
    readonly ownerResourceType?: "elementum_app" | "elementum_element";
    readonly orgRefs?: OrgRefLookup;
    readonly workspaceAspects?: ReadonlyMap<string, WorkspaceAspect>;
    /** Chart files actually present for this owner in the current workspace build. */
    readonly availableChartRefNames?: ReadonlySet<string>;
    readonly diagnostics: string[];
}
export declare function dashboardsToTf(dashboards: Readonly<Record<string, AppDashboardWire>> | undefined, opts: DashboardToTfOptions): TfBlock[];
export {};
