import { expr, isCanonicalTsLabel, scopedResourceLabel, tsLabelToTfLabel, } from "../../engine/tf/index.js";
import { workspaceAspectKey } from "../../engine/build/context.js";
import { filterToTf } from "../access-policy/to-tf.js";
import { filterTfToWire } from "../shared/filters/tf-codec.js";
import { DASHBOARD_WIDGET_RESOURCE_TYPE } from "../dashboard-widget/module.js";
import { resolveForeignRef } from "../shared/foreign-ref.js";
import { DASHBOARD_RESOURCE_TYPE } from "./module.js";
import { CHART_RESOURCE_TYPE } from "../chart/types.js";
export function dashboardsToTf(dashboards, opts) {
    const blocks = [];
    for (const [key, dashboard] of Object.entries(dashboards ?? {})) {
        if (!isCanonicalTsLabel(key)) {
            opts.diagnostics.push(`dashboards key "${key}" is not a canonical tsLabel (see src/engine/tf/labels.ts) — rename the key`);
            continue;
        }
        const attrs = {
            name: dashboard.name ?? "",
            owner_id: opts.ownerId,
            owner_type: opts.ownerType,
            favorite: dashboard.favorite ?? false,
        };
        if (dashboard.tags !== undefined)
            attrs["tags"] = [...dashboard.tags];
        const dashboardLabel = scopedResourceLabel(opts.ownerTfLabel, tsLabelToTfLabel(key), opts.scope);
        blocks.push({
            kind: "resource",
            type: DASHBOARD_RESOURCE_TYPE,
            label: dashboardLabel,
            attrs,
        });
        for (const [widgetKey, widget] of Object.entries(dashboard.widgets ?? {})) {
            const where = `dashboards.${key}.widgets.${widgetKey}`;
            if (!isCanonicalTsLabel(widgetKey)) {
                opts.diagnostics.push(`${where}: key is not a canonical tsLabel; dropped`);
                continue;
            }
            const widgetAttrs = {
                owner_id: opts.ownerId,
                dashboard_id: expr.resourceAttr(DASHBOARD_RESOURCE_TYPE, dashboardLabel, ["id"]),
                name: widget.name,
                size: widget.size ?? "MEDIUM",
            };
            if (widget.color !== undefined)
                widgetAttrs["color"] = widget.color;
            if (widget.icon !== undefined)
                widgetAttrs["icon"] = widget.icon;
            if (widget.type === "records") {
                const aspectId = resolveAspect(widget.target, `${where}.target`, opts);
                if (aspectId === undefined)
                    continue;
                const body = { aspect_id: aspectId };
                if (widget.filter !== undefined) {
                    const hclFilter = filterToTf(widget.filter, `${where}.filter`, {
                        ownerTfLabel: opts.ownerTfLabel,
                        scope: opts.scope,
                        objectId: opts.ownerId,
                        diagnostics: opts.diagnostics,
                        orgRefs: opts.orgRefs,
                    });
                    body["filter"] = expr.jsonencode(filterTfToWire(hclFilter));
                }
                widgetAttrs["aspect_widget"] = body;
            }
            else if (widget.type === "chart") {
                const chart = resolveChart(widget.chart, `${where}.chart`, opts);
                if (chart === undefined)
                    continue;
                widgetAttrs["chart_widget"] = { chart_id: chart };
            }
            else {
                opts.diagnostics.push(`${where}: unsupported widget type; dropped`);
                continue;
            }
            blocks.push({ kind: "resource", type: DASHBOARD_WIDGET_RESOURCE_TYPE, label: scopedResourceLabel(opts.ownerTfLabel, tsLabelToTfLabel(`${key}_${widgetKey}`), opts.scope), attrs: widgetAttrs });
        }
    }
    return blocks;
}
function resolveChart(value, where, opts) {
    if (typeof value === "string") {
        opts.diagnostics.push(`${where}: raw chart ids are not supported in public authoring; dropped`);
        return undefined;
    }
    const refName = value?.refName ?? "";
    if (refName === "") {
        opts.diagnostics.push(`${where}: chart has no refName; dropped`);
        return undefined;
    }
    if (value?.__kind !== "chart") {
        opts.diagnostics.push(`${where}: expected a chart ref; dropped`);
        return undefined;
    }
    if (value.parent === undefined || value.parent === "") {
        opts.diagnostics.push(`${where}: chart ref has no app owner; dropped`);
        return undefined;
    }
    if (value.parent !== opts.ownerRefName) {
        opts.diagnostics.push(`${where}: chart belongs to ${JSON.stringify(value.parent)}, not dashboard owner ${JSON.stringify(opts.ownerRefName)}; dropped`);
        return undefined;
    }
    if (opts.availableChartRefNames !== undefined && !opts.availableChartRefNames.has(refName)) {
        opts.diagnostics.push(`${where}: chart ref ${JSON.stringify(refName)} has no available chart resource; dropped`);
        return undefined;
    }
    return expr.resourceAttr(CHART_RESOURCE_TYPE, scopedResourceLabel(opts.ownerTfLabel, tsLabelToTfLabel(refName), opts.scope), ["id"]);
}
function resolveAspect(value, where, opts) {
    if (typeof value === "string") {
        opts.diagnostics.push(`${where}: raw aspect ids are not supported in public authoring; dropped`);
        return undefined;
    }
    const kind = value?.__kind === "element" ? "element" : "app";
    const refName = value?.refName ?? "";
    if (kind === opts.scope && refName === (opts.ownerRefName ?? opts.ownerTfLabel))
        return opts.ownerId;
    const sibling = opts.workspaceAspects?.get(workspaceAspectKey(kind, refName));
    if (sibling !== undefined)
        return expr.resourceAttr(kind === "app" ? "elementum_app" : "elementum_element", sibling.tfLabel, ["id"]);
    const name = value?.name ?? refName;
    if (name === "" || opts.orgRefs === undefined) {
        opts.diagnostics.push(`${where}: target has no resolvable name; dropped`);
        return undefined;
    }
    return resolveForeignRef(kind, { name, namespace: value?.namespace }, opts.orgRefs, opts.diagnostics, where).value;
}
