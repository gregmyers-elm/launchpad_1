import { dataRefLabel, optionalBoolean, optionalString, plainObject } from "../../engine/pull/context.js";
import { tfLabelToTsLabel } from "../../engine/tf/index.js";
import { TASK_RESOURCE_TYPE } from "./module.js";
const KNOWN = new Set(["id", "name", "handle", "namespace", "category_id", "cloud_link_id", "description", "icon", "color", "title_field_name", "title_field_description", "id_field_name", "id_field_description", "status_field_name", "status_field_description", "status_options", "status_option_ids_by_label", "title_field_id", "id_field_id", "status_field_id", "comments_enabled", "comments_display_block_id"]);
export function taskFromTf(block, ctx) {
    if (block.type !== TASK_RESOURCE_TYPE || !block.label)
        throw new Error(`expected ${TASK_RESOURCE_TYPE}`);
    for (const key of Object.keys(block.attrs))
        if (!KNOWN.has(key))
            ctx.diagnostics.push(`${block.type}.${block.label}: unhandled attribute ${key}`);
    const name = optionalString(block.attrs, "name", `task ${block.label}`, ctx) ?? "";
    const handle = optionalString(block.attrs, "handle", `task ${block.label}`, ctx) ?? block.label.toUpperCase();
    const namespace = optionalString(block.attrs, "namespace", `task ${block.label}`, ctx) ?? block.label;
    const categoryLabel = dataRefLabel(block.attrs["category_id"], "elementum_category");
    const cloudLabel = dataRefLabel(block.attrs["cloud_link_id"], "elementum_cloudlink");
    const statusOptions = Array.isArray(block.attrs["status_options"])
        ? block.attrs["status_options"].flatMap((item) => {
            const option = plainObject(item);
            return option === undefined ? [] : [option];
        })
        : undefined;
    const titleName = optionalString(block.attrs, "title_field_name", `task ${block.label}`, ctx);
    const titleDescription = optionalString(block.attrs, "title_field_description", `task ${block.label}`, ctx);
    const idName = optionalString(block.attrs, "id_field_name", `task ${block.label}`, ctx);
    const idDescription = optionalString(block.attrs, "id_field_description", `task ${block.label}`, ctx);
    const statusName = optionalString(block.attrs, "status_field_name", `task ${block.label}`, ctx);
    const statusDescription = optionalString(block.attrs, "status_field_description", `task ${block.label}`, ctx);
    return { refName: tfLabelToTsLabel(block.label), name, handle, namespace,
        ...(categoryLabel !== undefined && { categoryRef: tfLabelToTsLabel(categoryLabel) }),
        ...(cloudLabel !== undefined && { cloudLinkRef: tfLabelToTsLabel(cloudLabel) }),
        ...(optionalString(block.attrs, "description", `task ${block.label}`, ctx) !== undefined && { description: optionalString(block.attrs, "description", `task ${block.label}`, ctx) }),
        ...(optionalString(block.attrs, "icon", `task ${block.label}`, ctx) !== undefined && { icon: optionalString(block.attrs, "icon", `task ${block.label}`, ctx) }),
        ...(optionalString(block.attrs, "color", `task ${block.label}`, ctx) !== undefined && { color: optionalString(block.attrs, "color", `task ${block.label}`, ctx) }),
        ...(optionalBoolean(block.attrs, "comments_enabled", `task ${block.label}`, ctx) !== undefined && { commentsEnabled: optionalBoolean(block.attrs, "comments_enabled", `task ${block.label}`, ctx) }),
        ...((titleName !== undefined || titleDescription !== undefined) && { titleField: { ...(titleName !== undefined && { name: titleName }), ...(titleDescription !== undefined && { description: titleDescription }) } }),
        ...((idName !== undefined || idDescription !== undefined) && { idField: { ...(idName !== undefined && { name: idName }), ...(idDescription !== undefined && { description: idDescription }) } }),
        ...((statusName !== undefined || statusDescription !== undefined || statusOptions !== undefined) && { statusField: { ...(statusName !== undefined && { name: statusName }), ...(statusDescription !== undefined && { description: statusDescription }), ...(statusOptions !== undefined && { options: statusOptions }) } }), };
}
