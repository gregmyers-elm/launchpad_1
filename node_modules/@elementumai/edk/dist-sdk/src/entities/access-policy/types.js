/** The access-policy entity's resource-type constant (see src/graph/). */
export const ACCESS_POLICY_RESOURCE_TYPE = "elementum_access_policy";
/** Attributes `accessPolicyFromTf` understands — anything else is dialect growth, reported loudly. */
export const KNOWN_ACCESS_POLICY_ATTRS = new Set([
    "object_id",
    "filter",
    "user_ids",
    "group_ids",
    "service_account_ids",
]);
