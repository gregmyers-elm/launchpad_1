/**
 * Access-policy authoring options — the `accessPolicies: [...]` array shared
 * by `app`/`element` (`entities/app/build.ts` /
 * `entities/element/build.ts`). Kept here, not duplicated per parent, since
 * both parents accept the identical shape.
 */
import type { Filter } from "../shared/filters/types.js";
import type { AccessPolicyGroupRef, AccessPolicyServiceAccountRef, AccessPolicyUserRef } from "./principal.js";
export { groupId, userId, type AccessPolicyGroupRef, type AccessPolicyPrincipal, type AccessPolicyPrincipalRef, type AccessPolicyServiceAccountRef, type AccessPolicyUserRef, type GroupId, type UserId, } from "./principal.js";
export interface AccessPolicyOptions {
    /** Row-level-security predicate. Omit for the platform default (matches every record). */
    readonly filter?: Filter;
    readonly userIds?: readonly AccessPolicyUserRef[];
    readonly groupIds?: readonly AccessPolicyGroupRef[];
    readonly serviceAccountIds?: readonly AccessPolicyServiceAccountRef[];
}
