import { EntityTfError } from "../../engine/entity.js";
import { plainObject, resourceRefLabel } from "../../engine/pull/context.js";
import { isTfExpr, scopedLabelToBagKey, tfLabelToTsLabel } from "../../engine/tf/index.js";
import { dataAspectDisplayName, dataAspectRefName } from "../shared/aspect-target-from-tf.js";
import { VISUAL_FLOW_RESOURCE_TYPE } from "./module.js";
const KNOWN = new Set(["id", "object_id", "stages"]);
const symbols = new Set(["SQUARE", "DIAMOND", "CIRCLE"]);
const positions = new Set(["TOP", "RIGHT", "BOTTOM", "LEFT"]);
function requiredObject(value, where) {
    const result = value === undefined ? undefined : plainObject(value);
    if (result === undefined)
        throw new EntityTfError(`${where}: expected an object`);
    return result;
}
function optionalName(value) {
    return typeof value === "string" && value !== "" ? value : undefined;
}
function ownedRef(value, type, ownerLabel, scope, where) {
    if (value === undefined || !isTfExpr(value) || value.$expr !== "resource-attr" || value.type !== type || value.path.length !== 1 || value.path[0] !== "id") {
        throw new EntityTfError(`${where}: expected a typed ${type} resource reference`);
    }
    return scopedLabelToBagKey(ownerLabel, value.label, scope);
}
function objectRef(value, ctx, where) {
    if (value === undefined || !isTfExpr(value) || (value.$expr !== "resource-attr" && value.$expr !== "data-attr") || value.path.length !== 1 || value.path[0] !== "id") {
        throw new EntityTfError(`${where}: expected a typed app or element reference`);
    }
    if (value.type !== "elementum_app" && value.type !== "elementum_element") {
        throw new EntityTfError(`${where}: expected an elementum_app or elementum_element reference`);
    }
    const kind = value.type === "elementum_element" ? "element" : "app";
    const displayName = value.$expr === "data-attr" ? dataAspectDisplayName(ctx, kind, value.label) : undefined;
    const refName = value.$expr === "data-attr"
        ? dataAspectRefName(ctx, kind, value.label, displayName) ?? tfLabelToTsLabel(value.label)
        : tfLabelToTsLabel(value.label);
    return { kind, refName, name: displayName ?? refName };
}
function actionFromTf(value, ownerLabel, scope, ctx, where) {
    const action = requiredObject(value, where);
    const variants = ["agent", "automation", "user", "api", "object"].filter((key) => action[key] !== undefined);
    if (variants.length !== 1)
        throw new EntityTfError(`${where}: expected exactly one action variant`);
    const kind = variants[0];
    const body = requiredObject(action[kind], `${where}.${kind}`);
    const name = optionalName(body["name"]);
    if (kind === "agent")
        return { kind, refName: ownedRef(body["agent_id"], "elementum_agent", ownerLabel, scope, where), ...(name !== undefined ? { name } : {}) };
    if (kind === "automation")
        return { kind, refName: ownedRef(body["automation_id"], "elementum_automation", ownerLabel, scope, where), ...(name !== undefined ? { name } : {}) };
    if (kind === "user" || kind === "api") {
        if (name === undefined)
            throw new EntityTfError(`${where}.${kind}: name is required`);
        return { kind, name };
    }
    return { kind: "object", object: objectRef(body["object_id"], ctx, `${where}.object`), ...(name !== undefined ? { name } : {}) };
}
function nodeFromTf(value, ownerLabel, scope, ctx, where) {
    const node = requiredObject(value, where);
    const key = node["key"];
    const symbol = node["symbol"];
    const x = node["x"];
    const y = node["y"];
    if (typeof key !== "string" || key === "")
        throw new EntityTfError(`${where}: key is required`);
    if (typeof symbol !== "string" || !symbols.has(symbol))
        throw new EntityTfError(`${where}: unsupported symbol ${JSON.stringify(symbol)}`);
    if (typeof x !== "number" || typeof y !== "number")
        throw new EntityTfError(`${where}: x and y are required numbers`);
    const sourcePosition = node["source_position"];
    const targetPosition = node["target_position"];
    if (sourcePosition !== undefined && (typeof sourcePosition !== "string" || !positions.has(sourcePosition)))
        throw new EntityTfError(`${where}: unsupported source_position`);
    if (targetPosition !== undefined && (typeof targetPosition !== "string" || !positions.has(targetPosition)))
        throw new EntityTfError(`${where}: unsupported target_position`);
    const rawConnections = node["connects_to"];
    if (rawConnections !== undefined && !Array.isArray(rawConnections))
        throw new EntityTfError(`${where}: connects_to must be a list`);
    const connectsTo = (rawConnections ?? []).map((connection, index) => {
        const item = requiredObject(connection, `${where}.connects_to[${index}]`);
        if (typeof item["target"] !== "string" || item["target"] === "")
            throw new EntityTfError(`${where}.connects_to[${index}]: target is required`);
        return { target: item["target"], ...(typeof item["label"] === "string" ? { label: item["label"] } : {}) };
    });
    const rawTarget = node["target"];
    let target;
    if (rawTarget !== undefined) {
        const targetBody = requiredObject(rawTarget, `${where}.target`);
        const objectBody = requiredObject(targetBody["object"], `${where}.target.object`);
        const name = optionalName(objectBody["name"]);
        target = { object: objectRef(objectBody["object_id"], ctx, `${where}.target.object`), ...(name !== undefined ? { name } : {}) };
    }
    return [key, {
            ...(typeof node["description"] === "string" ? { description: node["description"] } : {}),
            symbol: symbol, x, y,
            ...(typeof sourcePosition === "string" ? { sourcePosition: sourcePosition } : {}),
            ...(typeof targetPosition === "string" ? { targetPosition: targetPosition } : {}),
            ...(connectsTo.length > 0 ? { connectsTo } : {}),
            ...(node["action"] !== undefined ? { action: actionFromTf(node["action"], ownerLabel, scope, ctx, `${where}.action`) } : {}),
            ...(target !== undefined ? { target } : {}),
        }];
}
export function visualFlowFromTf(block, ownerType, ownerLabel, scope, ctx) {
    if (block.type !== VISUAL_FLOW_RESOURCE_TYPE || !block.label)
        throw new EntityTfError(`expected ${VISUAL_FLOW_RESOURCE_TYPE}`);
    for (const key of Object.keys(block.attrs))
        if (!KNOWN.has(key))
            throw new EntityTfError(`${block.type}.${block.label}: unhandled attribute ${key}`);
    if (resourceRefLabel(block.attrs["object_id"], ownerType) !== ownerLabel)
        throw new EntityTfError(`${block.type}.${block.label}: object_id does not reference ${ownerType}.${ownerLabel}`);
    const rawStages = block.attrs["stages"];
    if (!Array.isArray(rawStages))
        throw new EntityTfError(`${block.type}.${block.label}: stages must be a list`);
    const stages = rawStages.map((value, index) => {
        const stage = requiredObject(value, `${block.type}.${block.label}.stages[${index}]`);
        const stageId = stage["stage_id"];
        if (!isTfExpr(stageId) || stageId.$expr !== "resource-attr" || stageId.type !== ownerType || stageId.label !== ownerLabel) {
            throw new EntityTfError(`${block.type}.${block.label}.stages[${index}]: stage_id must reference the owning ${scope} stage map`);
        }
        const entry = stageId.path.find((part) => typeof part === "object" && "index" in part);
        if (entry === undefined || typeof entry !== "object")
            throw new EntityTfError(`${block.type}.${block.label}.stages[${index}]: stage_id has no stable key`);
        const rawNodes = stage["nodes"];
        if (!Array.isArray(rawNodes))
            throw new EntityTfError(`${block.type}.${block.label}.stages[${index}]: nodes must be a list`);
        return { stage: tfLabelToTsLabel(entry.index), nodes: Object.fromEntries(rawNodes.map((node, nodeIndex) => nodeFromTf(node, ownerLabel, scope, ctx, `${block.type}.${block.label}.stages[${index}].nodes[${nodeIndex}]`))) };
    });
    return { refName: scopedLabelToBagKey(ownerLabel, block.label, scope), stages };
}
