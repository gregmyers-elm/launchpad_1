import { NO_CATALOG, NO_DATA } from "../../graph/types.js";
import { APP_RESOURCE_TYPE } from "../app/module.js";
import { ELEMENT_RESOURCE_TYPE } from "../element/module.js";
import { visualFlowFromTf } from "./from-tf.js";
import { printVisualFlow } from "./printer.js";
import { visualFlowToTf } from "./to-tf.js";
export const VISUAL_FLOW_RESOURCE_TYPE = "elementum_flow";
function extract(blocks, _stream, ctx) {
    const items = [];
    const skipped = [];
    const diagnostics = [];
    if (ctx.parentScope === "table")
        return { items, skipped: blocks.map((block) => `${block.type}.${block.label ?? "?"}`), diagnostics: ["visual flows cannot belong to tables"] };
    const ownerType = ctx.parentScope === "element" ? ELEMENT_RESOURCE_TYPE : APP_RESOURCE_TYPE;
    for (const block of blocks)
        try {
            items.push(visualFlowFromTf(block, ownerType, ctx.appTfLabel, ctx.parentScope, ctx));
        }
        catch (error) {
            skipped.push(`${block.type}.${block.label ?? "?"}`);
            diagnostics.push(error instanceof Error ? error.message : String(error));
        }
    return { items, skipped, diagnostics };
}
export const visualFlowModule = {
    kind: "visual-flow", stream: { role: "streamMember" }, authoring: { unit: "ownFile", bag: "visual-flows" },
    parents: [{ parent: "app", child: "visual-flow" }, { parent: "element", child: "visual-flow" }],
    resource: { shape: "single", resourceType: VISUAL_FLOW_RESOURCE_TYPE, adoptable: true }, data: NO_DATA,
    lifecycle: { participates: true, extractOwnFile: extract, printOwnFile: (item, opts) => {
            if (opts.parentScope === "table")
                throw new Error("visual flows cannot belong to tables");
            return {
                refName: item.refName,
                source: printVisualFlow(item, opts.appRefName, opts.parentScope, {
                    ...(opts.foreignAppRefNames !== undefined && { foreignAppRefNames: opts.foreignAppRefNames }),
                    ...(opts.foreignElementRefNames !== undefined && { foreignElementRefNames: opts.foreignElementRefNames }),
                }),
            };
        }, toTf: (entity, ctx) => visualFlowToTf(entity, { appRefName: ctx.appRefName, appTfLabel: ctx.appTfLabel, parentScope: ctx.parentScope, orgRefs: ctx.orgRefs, workspaceAspects: ctx.workspaceAspects, diagnostics: ctx.diagnostics }) },
    canvas: { participates: true, label: "Visual flow", glyph: "⇢", component: "generic" }, catalog: NO_CATALOG,
};
