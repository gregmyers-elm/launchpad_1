import type { OrgRefLookup, WorkspaceAspect } from "../../engine/build/context.js";
import { type DeployableEntity } from "../../engine/entity.js";
import { type ChildResourceScope, type TfBlock } from "../../engine/tf/index.js";
export interface VisualFlowToTfContext {
    readonly appRefName: string;
    readonly appTfLabel: string;
    readonly parentScope?: ChildResourceScope | "table";
    readonly orgRefs: OrgRefLookup;
    readonly workspaceAspects?: ReadonlyMap<string, WorkspaceAspect>;
    readonly diagnostics?: string[];
}
export declare function visualFlowToTf(entity: DeployableEntity, ctx: VisualFlowToTfContext): {
    blocks: readonly TfBlock[];
    address: string;
};
