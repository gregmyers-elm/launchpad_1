/** Provider data sources that are reference surfaces rather than authored resources. */
import { NO_CANVAS, NO_CATALOG, NO_LIFECYCLE } from "../../graph/types.js";
function lookup(kind, resourceType, lookupAttr, referencedByBuild = false) {
    return {
        kind,
        stream: { role: "streamReported" },
        authoring: { unit: "none" },
        parents: [],
        resource: { shape: "none" },
        data: { participates: true, resourceType, lookupAttr, referencedByBuild },
        lifecycle: NO_LIFECYCLE,
        canvas: NO_CANVAS,
        catalog: NO_CATALOG,
    };
}
export const dataLookupModules = [
    lookup("automation-value-references", "elementum_automation_value_references", "automation_id"),
    // Empty lookupAttr means the singleton has no selector; pull keys it by block label.
    lookup("organization", "elementum_organization", ""),
    lookup("permission-template", "elementum_permission_template", "template"),
    lookup("stored-function", "elementum_stored_function", "name"),
];
