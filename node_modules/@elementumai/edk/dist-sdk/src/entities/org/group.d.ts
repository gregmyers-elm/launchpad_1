/** Groups: org-owned reference identities, name-keyed data lookups. */
import { type EntityKindModule } from "../../graph/types.js";
/** org.ts / OrgRefLookup bag — keep in lockstep with `data.orgBag`. */
export declare const GROUP_ORG_BAG: "groups";
export declare const groupModule: EntityKindModule;
