import type { OrgRefResolution } from "../../engine/build/context.js";
import { type TfExpr } from "../../engine/tf/index.js";
/** Resource ID when the category is created in this apply; otherwise a live data lookup. */
export declare function categoryIdExpr(resolved: OrgRefResolution): TfExpr;
export declare function missingCategoryDiagnostic(name: string): string;
