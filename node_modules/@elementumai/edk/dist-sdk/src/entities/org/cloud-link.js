/** Cloud links: org-owned reference identities, name-keyed data lookups. */
import { namedOnlyFactoryEntry } from "../../catalog/render-helpers.js";
/** org.ts / OrgRefLookup bag — keep in lockstep with `data.orgBag`. */
export const CLOUD_LINK_ORG_BAG = "cloudLinks";
export const cloudLinkModule = {
    kind: "cloud-link",
    stream: { role: "streamMember" },
    data: {
        participates: true,
        lookupAttr: "name",
        catalogKind: "cloudLink",
        orgBag: CLOUD_LINK_ORG_BAG,
        // Apps pin cloud links (`cloud_link_id`), so build emits this data source
        // and the build-vs-export must scope it.
        referencedByBuild: true,
    },
    authoring: { unit: "inline", bag: "cloudLinks" },
    parents: [{ parent: "org", child: "cloud-link" }],
    resource: { shape: "single", resourceType: "elementum_cloudlink", adoptable: true },
    lifecycle: { participates: true },
    canvas: { participates: true, label: "Cloud Links", glyph: "☁", component: "orgIdentity" },
    catalog: {
        participates: true,
        refKind: "cloudLink",
        catalogBag: "cloudLinks",
        renderOrder: 80,
        catalogRoot: "org",
        renderEntry: namedOnlyFactoryEntry("cloudLinkRef"),
        stubRef: (refName) => ({ __kind: "cloudLink", refName, name: refName }),
        refFromAuthoring: () => undefined,
    },
};
