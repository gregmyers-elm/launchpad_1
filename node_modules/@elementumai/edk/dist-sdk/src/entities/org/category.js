/** Categories: org-owned reference identities, name-keyed data lookups. */
import { namedOnlyFactoryEntry } from "../../catalog/render-helpers.js";
/** org.ts / OrgRefLookup bag — keep in lockstep with `data.orgBag`. */
export const CATEGORY_ORG_BAG = "categories";
export const categoryModule = {
    kind: "category",
    stream: { role: "streamMember" },
    data: {
        participates: true,
        lookupAttr: "name",
        catalogKind: "category",
        orgBag: CATEGORY_ORG_BAG,
        // Apps reference categories (`category_id`), so build emits this data
        // source and the build-vs-export must scope it.
        referencedByBuild: true,
    },
    authoring: { unit: "inline", bag: "categories" },
    parents: [{ parent: "org", child: "category" }],
    resource: { shape: "single", resourceType: "elementum_category", adoptable: true },
    lifecycle: { participates: true },
    canvas: { participates: true, label: "Categories", glyph: "▣", component: "orgIdentity" },
    catalog: {
        participates: true,
        refKind: "category",
        catalogBag: "categories",
        renderOrder: 100,
        catalogRoot: "org",
        renderEntry: namedOnlyFactoryEntry("categoryRef"),
        stubRef: (refName) => ({ __kind: "category", refName, name: refName }),
        refFromAuthoring: () => undefined,
    },
};
