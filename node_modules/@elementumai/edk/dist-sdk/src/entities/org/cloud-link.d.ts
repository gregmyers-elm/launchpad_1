/** Cloud links: org-owned reference identities, name-keyed data lookups. */
import { type EntityKindModule } from "../../graph/types.js";
/** org.ts / OrgRefLookup bag — keep in lockstep with `data.orgBag`. */
export declare const CLOUD_LINK_ORG_BAG: "cloudLinks";
export declare const cloudLinkModule: EntityKindModule;
