/** Categories: org-owned reference identities, name-keyed data lookups. */
import { type EntityKindModule } from "../../graph/types.js";
/** org.ts / OrgRefLookup bag — keep in lockstep with `data.orgBag`. */
export declare const CATEGORY_ORG_BAG: "categories";
export declare const categoryModule: EntityKindModule;
