import { type FromTfContext } from "../../engine/pull/context.js";
import { type TfBlock } from "../../engine/tf/index.js";
import type { Filter } from "../shared/filters/types.js";
import type { NormalizedTableFilterField } from "../table/deserializer/normalize.js";
import type { DatamineSchedule } from "./build.js";
export interface NormalizedTableDatamine {
    readonly key: string;
    readonly name: string;
    readonly primaryColumns: readonly string[];
    readonly description?: string;
    readonly filter?: Filter<NormalizedTableFilterField, never>;
    readonly schedule?: DatamineSchedule;
    readonly limit?: number;
}
/** Decode datamines exported with a table stream. Primary ids must be
 * resource references to that table's name-keyed `field_ids` map. */
export declare function tableDataminesFromTf(blocks: readonly TfBlock[], tableTfLabel: string, columnKeyByName: ReadonlyMap<string, string>, ctx: FromTfContext, columnKeyById?: ReadonlyMap<string, string>): NormalizedTableDatamine[];
