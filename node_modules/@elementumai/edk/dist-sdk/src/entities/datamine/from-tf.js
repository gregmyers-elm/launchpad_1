import { dataRefLabel, optionalString, plainObject, resourceRefLabel } from "../../engine/pull/context.js";
import { reportUnknownAttrs } from "../../engine/pull/attrs.js";
import { EntityTfError } from "../../engine/entity.js";
import { isTfExpr, tfLabelToTsLabel } from "../../engine/tf/index.js";
import { filterFromTf } from "../shared/filters/tf-codec.js";
const KNOWN_DATAMINE_ATTRS = new Set([
    "id",
    "table_id",
    "name",
    "description",
    "filter",
    "primary_column_names",
    "primary_column_ids",
    "schedule_fixed",
    "schedule_cron",
    "limit",
    "active",
]);
function isTimeUnit(value) {
    return typeof value === "string" && ["DAY", "HOUR", "MINUTE", "MONTH", "WEEK"].includes(value);
}
/** Decode datamines exported with a table stream. Primary ids must be
 * resource references to that table's name-keyed `field_ids` map. */
export function tableDataminesFromTf(blocks, tableTfLabel, columnKeyByName, ctx, columnKeyById = new Map()) {
    return blocks.flatMap((block) => {
        const label = block.label ?? "";
        const where = `elementum_datamine.${label || "?"}`;
        if (label === "") {
            ctx.diagnostics.push(`${where}: missing label; skipped`);
            return [];
        }
        reportUnknownAttrs(block.attrs, KNOWN_DATAMINE_ATTRS, where, "tableDataminesFromTf", ctx);
        if (resourceRefLabel(block.attrs["table_id"], "elementum_table") !== tableTfLabel) {
            ctx.diagnostics.push(`${where}.table_id does not reference elementum_table.${tableTfLabel}; skipped`);
            return [];
        }
        const rawNames = block.attrs["primary_column_names"];
        const rawIDs = block.attrs["primary_column_ids"];
        if (!Array.isArray(rawNames) && !Array.isArray(rawIDs)) {
            ctx.diagnostics.push(`${where}: expected primary_column_names or primary_column_ids; skipped`);
            return [];
        }
        const rawColumns = Array.isArray(rawNames) ? rawNames : rawIDs;
        const lookup = Array.isArray(rawNames) ? columnKeyByName : columnKeyById;
        const primaryColumns = rawColumns.flatMap((value) => {
            const key = typeof value === "string" ? lookup.get(value) : undefined;
            return key === undefined ? [] : [key];
        });
        if (primaryColumns.length !== rawColumns.length) {
            ctx.diagnostics.push(`${where}.${Array.isArray(rawNames) ? "primary_column_names" : "primary_column_ids"}: one or more columns could not be resolved to this table; skipped`);
            return [];
        }
        const fixed = plainObject(block.attrs["schedule_fixed"]);
        const cron = plainObject(block.attrs["schedule_cron"]);
        let schedule;
        if (fixed !== undefined && isTimeUnit(fixed["time_unit"]) && typeof fixed["value"] === "number") {
            schedule = { kind: "fixed", timeUnit: fixed["time_unit"], value: fixed["value"], ...(typeof fixed["description"] === "string" && { description: fixed["description"] }) };
        }
        else if (cron !== undefined && typeof cron["cron_expression"] === "string") {
            schedule = { kind: "cron", cronExpression: cron["cron_expression"], ...(typeof cron["description"] === "string" && { description: cron["description"] }) };
        }
        const prefix = `${tableTfLabel}_`;
        const description = optionalString(block.attrs, "description", where, ctx);
        return [{
                key: tfLabelToTsLabel(label.startsWith(prefix) ? label.slice(prefix.length) : label),
                name: optionalString(block.attrs, "name", where, ctx) ?? "",
                primaryColumns,
                ...(description !== undefined && { description }),
                ...(block.attrs["filter"] !== undefined && {
                    filter: filterFromTf(block.attrs["filter"], {
                        field: (value) => ({
                            kind: "tableColumn",
                            tableRefName: tfLabelToTsLabel(tableTfLabel),
                            columnRefName: tableColumnKey(value, tableTfLabel, columnKeyByName, columnKeyById, ctx, where),
                        }),
                        reference: () => { throw new EntityTfError(`${where}.filter: datamine filters do not support reference values`); },
                    }),
                }),
                ...(schedule !== undefined && { schedule }),
                ...(typeof block.attrs["limit"] === "number" && { limit: block.attrs["limit"] }),
            }];
    });
}
function tableColumnKey(value, tableTfLabel, columnKeyByName, columnKeyById, ctx, where) {
    if (typeof value === "string") {
        return columnKeyById.get(value) ?? columnKeyByName.get(value) ?? value;
    }
    if (isTfExpr(value) && value.$expr === "resource-attr" && value.type === "elementum_table" && value.label === tableTfLabel && value.path[0] === "field_ids") {
        const index = value.path[1];
        const name = typeof index === "object" && index !== null && "index" in index && typeof index.index === "string"
            ? index.index
            : typeof index === "string" ? index : undefined;
        const key = name === undefined ? undefined : columnKeyByName.get(name) ?? tfLabelToTsLabel(name);
        if (key !== undefined)
            return key;
    }
    const fieldLabel = dataRefLabel(value, "elementum_field");
    if (fieldLabel !== undefined) {
        const attrs = ctx.dataAttrs?.("elementum_field", fieldLabel);
        if (attrs === undefined) {
            throw new EntityTfError(`${where}.filter: data.elementum_field.${fieldLabel} has no matching data block`);
        }
        const ownerLabel = resourceRefLabel(attrs["object_id"], "elementum_table")
            ?? dataRefLabel(attrs["object_id"], "elementum_table");
        if (ownerLabel !== tableTfLabel) {
            throw new EntityTfError(`${where}.filter: data.elementum_field.${fieldLabel}.object_id does not reference elementum_table.${tableTfLabel}`);
        }
        const name = attrs["name"];
        if (typeof name !== "string" || name === "") {
            throw new EntityTfError(`${where}.filter: data.elementum_field.${fieldLabel}.name has no string name`);
        }
        const key = columnKeyByName.get(name);
        if (key !== undefined)
            return key;
        throw new EntityTfError(`${where}.filter: data.elementum_field.${fieldLabel}.name does not resolve to a column on elementum_table.${tableTfLabel}`);
    }
    throw new EntityTfError(`${where}.filter: field does not resolve to a column on this table`);
}
