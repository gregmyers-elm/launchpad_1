import { type TfBlock, type TfValue } from "../../engine/tf/index.js";
import type { Filter } from "../shared/filters/types.js";
import type { TableDatamine } from "./build.js";
export type TableDatamineWire = TableDatamine<string>;
/** Emit table-owned datamines using the web client's V2 ownership model. */
export declare function tableDataminesToTf(datamines: Readonly<Record<string, TableDatamineWire>> | undefined, opts: {
    readonly tableTfLabel: string;
    readonly columnNames: Readonly<Record<string, string>>;
    readonly diagnostics: string[];
    readonly filterToTf: (filter: Filter) => TfValue;
}): TfBlock[];
