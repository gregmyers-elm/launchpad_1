/**
 * Typed field factories — the authoring surface for `app`'s (and
 * `element`'s) `fields:` bag. One factory per field sub-type row
 * (types.ts), each taking only the options valid for its kind:
 *
 *   fields: {
 *     glamourShot: attachmentField({ name: "Glamour Shot" }),
 *     hasPetLlama: booleanField({ name: "Has Pet Llama", required: true }),
 *     club: dropdownField({
 *       name: "Club",
 *       config: { static: { values: ["FFA", "Rex Kwon Do"] } },
 *     }),
 *   }
 *
 * Each factory returns a plain {@link AppField} with the `type` discriminator
 * baked in — the factories are sugar over the same authoring shape, so the
 * literal form (`{ name, type: "TEXT" }`) remains valid where ever an
 * AppField is accepted.
 */
import { assertCalculatedFieldRoot, coerce } from "./calc/runtime.js";
/** Product-visible rendering modes for dynamically sourced values. */
export const dropdownDisplay = {
    dropdown: "DROPDOWN",
    table: "TABLE",
};
/**
 * Reference a static value on a parent selection field without exposing its
 * platform UUID. The provider resolves the label through the parent's
 * options-by-label local.
 */
export function dropdownValue(field, label) {
    return { __kind: "dropdownValue", field, label };
}
export function dropdownOption(option) {
    return option;
}
// Generic over the literal type discriminator so a field factory's return
// type carries e.g. `type: "PICKLIST"`, not the widened `WorkflowFieldType`
// union — required for the live catalog path (`app`'s `.ref()`) to
// preserve per-field literal typing all the way to `RecordShape`
// (`entities/field/record-shape.ts`), the same way the old baked
// `fieldRef({ fieldType: "PICKLIST" })` literal did.
function field(type, opts) {
    return { ...opts, type };
}
export function textField(opts) {
    return field("TEXT", opts);
}
export function richTextField(opts) {
    return field("RICH_TEXT", opts);
}
export function numberField(opts) {
    return field("NUMBER", opts);
}
export function decimalField(opts) {
    return field("DECIMAL", opts);
}
export function booleanField(opts) {
    return field("BOOLEAN", opts);
}
export function dateField(opts) {
    return field("DATE", opts);
}
export function dateTimeField(opts) {
    return field("DATETIME", opts);
}
export function userField(opts) {
    return field("USER", opts);
}
export function groupField(opts) {
    return field("GROUP", opts);
}
export function attachmentField(opts) {
    return field("ATTACHMENT", opts);
}
export function jsonField(opts) {
    return field("JSON", opts);
}
function selectionFieldOptions(opts) {
    const { config, ...base } = opts;
    if (config.static !== undefined) {
        const options = config.static.values.map((value) => {
            if (typeof value === "string")
                return value;
            return {
                label: value.label,
                ...(value.active !== undefined && { active: value.active }),
                ...(value.icon !== undefined && { icon: value.icon }),
                ...(value.color !== undefined && { color: value.color }),
                ...(value.tags !== undefined && { tags: value.tags }),
                ...(value.availableWhen !== undefined && { parentValues: value.availableWhen }),
            };
        });
        return {
            ...base,
            options,
            ...(config.static.parent !== undefined && { parentField: config.static.parent }),
        };
    }
    const dynamic = config.dynamic;
    return {
        ...base,
        config: {
            dynamic: {
                relatedAspectId: dynamic.source,
                relatedLabelFieldId: dynamic.label,
                ...(dynamic.parent !== undefined && {
                    parentFieldId: dynamic.parent.field,
                    parentMappings: dynamic.parent.mappings.map((mapping) => ({
                        parentAspectFieldId: mapping.parentSourceField,
                        relatedAspectFieldId: mapping.sourceField,
                    })),
                }),
                ...(dynamic.autoRelate !== undefined && { autoRelate: dynamic.autoRelate }),
                ...(dynamic.displayAs !== undefined && { valuesRenderType: dynamic.displayAs }),
                ...(dynamic.filter !== undefined && { filter: dynamic.filter }),
                ...(dynamic.sort !== undefined && { sort: { orders: dynamic.sort } }),
            },
        },
    };
}
export function dropdownField(opts) {
    const built = field("PICKLIST", selectionFieldOptions(opts));
    return {
        ...built,
        ...("dynamic" in opts.config ? { dynamicSource: true } : {}),
    };
}
/** A multiple-value Multi-Select field, using the product's Static/Dynamic configuration model. */
export function multiSelectField(opts) {
    const built = field("MULTI_PICKLIST", selectionFieldOptions(opts));
    return {
        ...built,
        ...("dynamic" in opts.config ? { dynamicSource: true } : {}),
    };
}
/** @deprecated Use {@link dropdownField}. */
export function picklistField(opts) {
    return field("PICKLIST", opts);
}
/** @deprecated Use {@link multiSelectField}. */
export function multiPicklistField(opts) {
    return field("MULTI_PICKLIST", opts);
}
export function relationField(opts) {
    return field("RELATION", opts);
}
/** Create a calculated field whose platform field kind is inferred from its expression. */
export function calculatedField(opts) {
    const expression = coerce(opts.expression);
    assertCalculatedFieldRoot(expression, opts.name);
    const type = (() => {
        switch (expression.__exprType) {
            case "number": return "NUMBER";
            case "decimal": return "DECIMAL";
            case "bool": return "BOOLEAN";
            case "date": return "DATE";
            case "datetime": return "DATETIME";
            case "handle": return "HANDLE";
            case "html": return "HTML";
            case "json": return "JSON";
            case "picklist": return "PICKLIST";
            case "multiPicklist": return "MULTI_PICKLIST";
            case "user": return "USER";
            case "group": return "GROUP";
            case "attachment": return "ATTACHMENT";
            case "text":
            case "uuid": return "TEXT";
        }
    })();
    return { ...opts, expression, type };
}
/** Authoring field type → its factory's exported name (the printer's emission table). */
export const FIELD_FACTORY_BY_TYPE = new Map([
    ["TEXT", "textField"],
    ["RICH_TEXT", "richTextField"],
    ["NUMBER", "numberField"],
    ["DECIMAL", "decimalField"],
    ["BOOLEAN", "booleanField"],
    ["DATE", "dateField"],
    ["DATETIME", "dateTimeField"],
    ["USER", "userField"],
    ["GROUP", "groupField"],
    ["ATTACHMENT", "attachmentField"],
    ["JSON", "jsonField"],
    ["PICKLIST", "dropdownField"],
    ["MULTI_PICKLIST", "multiSelectField"],
    ["RELATION", "relationField"],
]);
