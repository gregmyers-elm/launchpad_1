import type { AppCatalogRef } from "../../catalog/index.js";
/** A portable reference to a service account by display name within its app. */
export interface ServiceAccountRef {
    readonly __kind: "serviceAccount";
    readonly appRefName: string;
    readonly name: string;
}
export declare function serviceAccountRef(opts: {
    readonly app: AppCatalogRef;
    readonly name: string;
}): ServiceAccountRef;
