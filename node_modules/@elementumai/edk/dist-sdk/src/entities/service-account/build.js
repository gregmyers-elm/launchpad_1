export const serviceAccountStatus = {
    active: "ACTIVE",
    inactive: "INACTIVE",
};
export function serviceAccountRoleRef(name) {
    if (name.trim() === "")
        throw new Error("service account role name must not be empty");
    return { __kind: "serviceAccountRole", name };
}
