export function serviceAccountRef(opts) {
    if (opts.name.trim() === "")
        throw new Error("service account name must not be empty");
    return { __kind: "serviceAccount", appRefName: opts.app.refName, name: opts.name };
}
