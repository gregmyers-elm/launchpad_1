import type { OrgRefLookup } from "../../engine/build/context.js";
import { type TfValue } from "../../engine/tf/index.js";
import type { KindId } from "../../graph/types.js";
export interface ForeignRootToken {
    readonly name: string;
    readonly namespace?: string;
    readonly tfLabel?: string;
}
export interface ForeignRootDataRef {
    readonly value: TfValue;
    readonly tfLabel: string;
}
/** Resolve a foreign root through its graph-declared reference lookup facet. */
export declare function resolveForeignRef(kind: KindId, token: ForeignRootToken, orgRefs: OrgRefLookup, diagnostics?: string[], where?: string): ForeignRootDataRef;
