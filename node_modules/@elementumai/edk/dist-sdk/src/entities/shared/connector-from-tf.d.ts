export interface ConnectorDataLookup {
    dataName(type: string, label: string): string | undefined;
    dataRef(type: string, label: string): {
        readonly resourceType: string;
        readonly tfLabel: string;
    } | undefined;
}
export interface ConnectorIdentity {
    readonly handle: string;
    readonly providerKey?: string;
    readonly catalogKey?: string;
}
/** Derive connector identity from its own model and provider data blocks. */
export declare function connectorIdentityFromTf(connectorType: string, connectorLabel: string, lookup: ConnectorDataLookup): ConnectorIdentity;
