import type { Filter } from "./types.js";
export interface FilterPrintCodec<Field, ReferenceValue> {
    readonly field: (field: Field) => string;
    readonly aspect?: (aspect: Field) => string;
    readonly reference: (reference: ReferenceValue) => string;
    readonly quote?: (value: string) => string;
    readonly namespace?: string;
    readonly used?: (helper: string) => void;
}
/** Print every canonical filter through the same public `filter.*` authoring surface. */
export declare function printFilter<Field, ReferenceValue>(filter: Filter<Field, ReferenceValue>, codec: FilterPrintCodec<Field, ReferenceValue>): string;
