/** Canonical Elementum filter model and authoring helpers. */
import { picklistStaticValueToApiId } from "../../../core/types/app.js";
import { serializeRefData } from "../../../core/wire/ref-serializer.js";
import { EdkValidationError } from "../../../../elementum-entity-client/errors.js";
import { isRef } from "../ref.js";
export function fieldTypeToValueType(type) {
    switch (type) {
        case "NUMBER":
        case "DECIMAL": return "NUMBER";
        case "BOOLEAN": return "BOOLEAN";
        case "DATE": return "DATE";
        case "DATETIME": return "DATETIME";
        case "USER":
        case "USER_ID":
        case "GROUP":
        case "GROUP_ID":
        case "ASPECT_ID":
        case "ASPECT_RECORD":
        case "ASPECT_RECORD_ID":
        case "ASPECT_FIELD_ID":
            return "UUID";
        default: return "TEXT";
    }
}
const FILTER_REFERENCE_BRAND = Symbol("FilterReferenceInput");
function referenceInput(ref, valueType, options) {
    return { [FILTER_REFERENCE_BRAND]: true, ref, valueType, ...(options?.multiple === true && { multiple: true }) };
}
export function boolean(ref, options) {
    return referenceInput(ref, "BOOLEAN", options);
}
export function date(ref, options) {
    return referenceInput(ref, "DATE", options);
}
export function dateTime(ref, options) {
    return referenceInput(ref, "DATETIME", options);
}
export function number(ref, options) {
    return referenceInput(ref, "NUMBER", options);
}
export function text(ref, options) {
    return referenceInput(ref, "TEXT", options);
}
export function uuid(ref, options) {
    return referenceInput(ref, "UUID", options);
}
export function isFilterReferenceInput(value) {
    return typeof value === "object" && value !== null && FILTER_REFERENCE_BRAND in value;
}
export function currentUser(valueType = "ID") {
    return { __kind: "currentUser", valueType };
}
export function fieldValue(value, options) {
    return {
        type: "FIELD",
        value: fieldWireValue(value),
        ...(options?.aspect !== undefined && { aspect: options.aspect }),
        ...(options?.treatNullAsEquals !== undefined && { treatNullAsEquals: options.treatNullAsEquals }),
    };
}
export const UNRESOLVED_REFERENCE_MARKER = "__edkUnresolvedReference::";
export function refDataToFilterReference(data) {
    switch (data.kind) {
        case "trigger": return { type: "TRIGGER", name: data.name };
        case "task": return { type: "TASK", taskRefKey: data.actionRefKey, name: data.name };
        case "forEach": return { type: "FOR_EACH", forEachTaskRefKey: data.forEachActionName, name: data.name };
        case "static": return { type: "VALUE", value: data.value };
        case "template": return { type: "VALUE", value: serializeRefData(data) };
        case "variableParam": throw new EdkValidationError("Variable param refs cannot be used directly in filter values");
        case "secret": throw new EdkValidationError("Secret refs cannot be used directly in filter values");
    }
}
function hasKind(value, kind) {
    return typeof value === "object" && value !== null && value.__kind === kind;
}
function isCurrentUser(value) { return hasKind(value, "currentUser"); }
const FILTER_VALUE_TYPES = new Set([
    "BOOLEAN", "CURRENT_USER", "DATE", "DATETIME", "FIELD", "NULL", "NUMBER", "RECORD_FIELD", "REFERENCE", "TEXT", "UUID",
]);
function isCanonicalFilterValue(value) {
    return typeof value === "object" && value !== null && typeof value.type === "string" &&
        FILTER_VALUE_TYPES.has(value.type);
}
function isCatalogField(value) {
    return hasKind(value, "field") || hasKind(value, "userField") || hasKind(value, "tableColumn");
}
function isAppField(value) {
    return typeof value === "object" && value !== null && "id" in value && "type" in value && !isCatalogField(value);
}
function isUnsafePlatformId(value) {
    return typeof value === "object" && value !== null && value.__unsafe === "platformId";
}
function fieldWireValue(field) {
    if (isUnsafePlatformId(field))
        return field.value;
    return isAppField(field) ? field.id : field;
}
function leftType(left, fallback, explicit) {
    if (explicit !== undefined)
        return explicit;
    if (isFilterReferenceInput(left))
        return left.valueType;
    if (isRef(left) || typeof left === "string" || isUnsafePlatformId(left))
        return fallback;
    return fieldTypeToValueType(isAppField(left) ? left.type : "fieldType" in left ? left.fieldType : undefined);
}
function leftDescriptor(left, fallback, explicit) {
    if (isFilterReferenceInput(left))
        return { leftValue: toFilterValue(left) };
    if (isRef(left))
        return { leftValue: { type: "REFERENCE", realType: explicit ?? fallback, value: refDataToFilterReference(left.__data) } };
    if (typeof left === "string" && left.startsWith(UNRESOLVED_REFERENCE_MARKER)) {
        return { leftValue: { type: "REFERENCE", realType: explicit ?? fallback, value: { __rawFilterReference: left.slice(UNRESOLVED_REFERENCE_MARKER.length) } } };
    }
    return { field: fieldWireValue(left) };
}
function resolveStaticFieldValue(field, input) {
    if (isRef(input) || isFilterReferenceInput(input) || !isAppField(field))
        return input;
    return picklistStaticValueToApiId(field, input);
}
export function toFilterValue(input, valueType = "TEXT") {
    if (isCanonicalFilterValue(input))
        return input;
    if (isCurrentUser(input))
        return { type: "CURRENT_USER", valueType: input.valueType };
    if (isFilterReferenceInput(input)) {
        const value = typeof input.ref === "string" && input.ref.startsWith(UNRESOLVED_REFERENCE_MARKER)
            ? { __rawFilterReference: input.ref.slice(UNRESOLVED_REFERENCE_MARKER.length) }
            : typeof input.ref === "string"
                ? { __rawFilterReference: input.ref }
                : refDataToFilterReference(input.ref.__data);
        return { type: "REFERENCE", realType: input.valueType, value, ...(input.multiple === true && { multiple: true }) };
    }
    if (isRef(input)) {
        if (input.__data.kind === "static")
            return toFilterValue(input.__data.value, valueType);
        return { type: "REFERENCE", realType: valueType, value: refDataToFilterReference(input.__data) };
    }
    if (isCatalogField(input) || isAppField(input))
        return { type: "FIELD", value: fieldWireValue(input) };
    if (input === null)
        return { type: "NULL" };
    if (typeof input === "number")
        return { type: "NUMBER", value: input };
    if (typeof input === "boolean")
        return { type: "BOOLEAN", value: input };
    if (typeof input === "string") {
        if (valueType === "UUID")
            return { type: "UUID", value: input };
        if (valueType === "DATE")
            return { type: "DATE", value: input };
        if (valueType === "DATETIME")
            return { type: "DATETIME", value: input };
        return { type: "TEXT", value: input };
    }
    throw new EdkValidationError("Unsupported filter value");
}
function values(left, inputs, fallback, explicit) {
    const type = leftType(left, fallback, explicit);
    return inputs.map((input) => toFilterValue(resolveStaticFieldValue(left, input), type));
}
export function eq(left, input, explicit) {
    const type = leftType(left, "TEXT", explicit);
    return { type: "EQUALS", ...leftDescriptor(left, "TEXT", explicit), value: toFilterValue(resolveStaticFieldValue(left, input), type) };
}
export function neq(left, input, explicit) { return { ...eq(left, input, explicit), not: true }; }
export function gt(left, input, explicit) {
    const type = leftType(left, "NUMBER", explicit);
    return { type: "GREATER_THAN", ...leftDescriptor(left, "NUMBER", explicit), value: toFilterValue(resolveStaticFieldValue(left, input), type) };
}
export function gte(left, input, explicit) {
    const type = leftType(left, "NUMBER", explicit);
    return { type: "GREATER_THAN_EQUALS", ...leftDescriptor(left, "NUMBER", explicit), value: toFilterValue(resolveStaticFieldValue(left, input), type) };
}
export function lt(left, input, explicit) {
    const type = leftType(left, "NUMBER", explicit);
    return { type: "LESS_THAN", ...leftDescriptor(left, "NUMBER", explicit), value: toFilterValue(resolveStaticFieldValue(left, input), type) };
}
export function lte(left, input, explicit) {
    const type = leftType(left, "NUMBER", explicit);
    return { type: "LESS_THAN_EQUALS", ...leftDescriptor(left, "NUMBER", explicit), value: toFilterValue(resolveStaticFieldValue(left, input), type) };
}
export function like(left, input, options, explicit) {
    const type = leftType(left, "TEXT", explicit);
    return { type: "LIKE", ...leftDescriptor(left, "TEXT", explicit), value: toFilterValue(resolveStaticFieldValue(left, input), type), ...(options?.not === true && { not: true }), ...(options?.caseInsensitive !== undefined && { caseInsensitive: options.caseInsensitive }), ...(options?.mode !== undefined && { mode: options.mode.toUpperCase() }) };
}
export function isNull(left, options, explicit) {
    return { type: "IS_NULL", ...leftDescriptor(left, "TEXT", explicit), ...(options?.not === true && { not: true }) };
}
export function isNotNull(left, explicit) { return isNull(left, { not: true }, explicit); }
export function isIn(left, input, explicit) {
    return { type: "IN", ...leftDescriptor(left, "TEXT", explicit), values: values(left, input, "TEXT", explicit) };
}
export function isNotIn(left, input, explicit) { return { ...isIn(left, input, explicit), not: true }; }
export function between(left, min, max, options, explicit) {
    const [minimum, maximum] = values(left, [min, max], "NUMBER", explicit);
    return { type: "BETWEEN", ...leftDescriptor(left, "NUMBER", explicit), min: minimum, max: maximum, ...(options?.not === true && { not: true }) };
}
export function currentRange(left, options, explicit) {
    return { type: "CURRENT_RANGE", ...leftDescriptor(left, "DATETIME", explicit), unit: options.unit, semantic: options.semantic ?? false, ...(options.isoWeek !== undefined && { isoWeek: options.isoWeek }) };
}
export function consecutiveRange(left, options, explicit) {
    return { type: "CONSECUTIVE_RANGE", ...leftDescriptor(left, "DATETIME", explicit), unit: options.unit, count: options.count, offset: options.offset, semantic: options.semantic ?? false, ...(options.isoWeek !== undefined && { isoWeek: options.isoWeek }) };
}
export function contains(left, input, options, explicit) {
    const items = Array.isArray(input) ? input : [input];
    return { type: "CONTAINS", ...leftDescriptor(left, "TEXT", explicit), values: values(left, items, "TEXT", explicit), ...(options?.not === true && { not: true }) };
}
export function overlaps(left, input, options, explicit) {
    const items = Array.isArray(input) ? input : [input];
    return { type: "OVERLAPS", ...leftDescriptor(left, "TEXT", explicit), values: values(left, items, "TEXT", explicit), ...(options?.not === true && { not: true }) };
}
export function and(...children) { return { type: "AND", children }; }
export function or(...children) { return { type: "OR", children }; }
export function whereTrue() { return { type: "TRUE" }; }
export function whereFalse() { return { type: "FALSE" }; }
export function withOptions(value, options) {
    if (value.type === "AND" || value.type === "OR" || value.type === "TRUE" || value.type === "FALSE") {
        throw new EdkValidationError("Filter field options require a field-backed filter");
    }
    return {
        ...value,
        ...(options.aspect !== undefined && { aspect: fieldWireValue(options.aspect) }),
        ...(options.semantic !== undefined && { semantic: options.semantic }),
    };
}
