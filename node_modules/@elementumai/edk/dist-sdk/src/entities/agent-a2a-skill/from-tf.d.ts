import { type FromTfContext } from "../../engine/pull/context.js";
import type { TfBlock } from "../../engine/tf/index.js";
export interface NormalizedA2ASkill {
    readonly name: string;
    readonly description: string;
    readonly tags?: readonly string[];
    readonly examples?: readonly string[];
    readonly inputModes?: readonly string[];
    readonly outputModes?: readonly string[];
}
export interface A2ASkillOwner {
    /** The owning agent's resource type + tfLabel. */
    readonly agentType: string;
    readonly agentLabel: string;
}
/** One a2a-skill block → its normalized shape; undefined = skipped with a diagnostic (wrong owner, missing required attr). */
export declare function a2aSkillFromTf(block: TfBlock, owner: A2ASkillOwner, ctx: FromTfContext): NormalizedA2ASkill | undefined;
