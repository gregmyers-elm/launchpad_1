import { dataRefLabel, resourceRefLabel, } from "../../engine/pull/context.js";
import { scopedLabelToBagKey } from "../../engine/tf/index.js";
import { APP_RESOURCE_TYPE } from "../app/module.js";
import { connectorIdentityFromTf } from "../shared/connector-from-tf.js";
const KNOWN_ATTRS = new Set([
    "app_id",
    "name",
    "prompt",
    "ai_provider_connector_id",
    "passing_threshold",
]);
export function agentEvaluationFromTf(block, opts, ctx) {
    const label = block.label ?? "";
    const where = `agent evaluation "${label}"`;
    for (const key of Object.keys(block.attrs)) {
        if (!KNOWN_ATTRS.has(key)) {
            ctx.diagnostics.push(`${where}: unhandled attribute "${key}" — extend agentEvaluationFromTf deliberately`);
        }
    }
    if (resourceRefLabel(block.attrs["app_id"], APP_RESOURCE_TYPE) !== opts.appLabel) {
        ctx.diagnostics.push(`${where}: app_id does not reference ${APP_RESOURCE_TYPE}.${opts.appLabel}; skipped`);
        return undefined;
    }
    const connectorLabel = dataRefLabel(block.attrs["ai_provider_connector_id"], "elementum_ai_provider_connector");
    const connectorKey = connectorLabel === undefined
        ? undefined
        : connectorIdentityFromTf("elementum_ai_provider_connector", connectorLabel, ctx).catalogKey;
    if (connectorLabel === undefined || connectorKey === undefined) {
        ctx.diagnostics.push(`${where}: ai_provider_connector_id is not a resolvable AI-service data reference; skipped`);
        return undefined;
    }
    const name = block.attrs["name"];
    const prompt = block.attrs["prompt"];
    const passingThreshold = block.attrs["passing_threshold"];
    if (typeof name !== "string" || typeof prompt !== "string" || typeof passingThreshold !== "number") {
        ctx.diagnostics.push(`${where}: name, prompt, and passing_threshold must be literal values; skipped`);
        return undefined;
    }
    return {
        refName: scopedLabelToBagKey(opts.appLabel, label, "app"),
        name,
        prompt,
        connectorKey,
        passingThreshold,
    };
}
