import { type AgentEvaluationRef, type AiServiceRef, type AppCatalogRef } from "../../catalog/index.js";
import type { DeployableEntity, EntityDeployOptions } from "../../engine/entity.js";
export interface AgentEvaluationOptions<AppRefName extends string = string> extends EntityDeployOptions {
    /** The app whose scenarios may use this evaluation rubric. */
    readonly app: AppCatalogRef<AppRefName>;
    readonly name: string;
    /** Instructions given to the evaluator model when scoring a scenario result. */
    readonly prompt: string;
    /** A pulled organization AI-service token. */
    readonly connector: AiServiceRef;
    /** Minimum score required to pass. The product supports values from 0 through 4. */
    readonly passingThreshold: number;
}
export interface AgentEvaluationConfigured {
    ref(refName: string): AgentEvaluationRef;
    def(refName: string): DeployableEntity;
}
/** Define an app-owned rubric used to evaluate agent scenario responses. */
export declare function agentEvaluation<const AppRefName extends string>(opts: AgentEvaluationOptions<AppRefName>): AgentEvaluationConfigured;
