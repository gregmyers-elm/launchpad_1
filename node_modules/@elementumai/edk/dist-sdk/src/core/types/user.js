export function user(opts) {
    return {
        id: opts.id,
        email: opts.email,
        ...(opts.firstName != null && { firstName: opts.firstName }),
        ...(opts.lastName != null && { lastName: opts.lastName }),
    };
}
