import { EdkValidationError } from "../../elementum-entity-client/errors.js";
/** Mark a non-portable platform id intentionally. Prefer catalog refs whenever possible. */
export function unsafePlatformId(value) {
    if (value.trim() === "") {
        throw new EdkValidationError("unsafePlatformId requires a non-empty platform id.");
    }
    return { __unsafe: "platformId", value };
}
/** Check whether `value` is an `UnsafePlatformId` wrapper. */
export function isUnsafePlatformId(value) {
    return typeof value === "object" && value !== null && value.__unsafe === "platformId";
}
