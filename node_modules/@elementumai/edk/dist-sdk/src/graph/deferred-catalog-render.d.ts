/**
 * Catalog renderEntry for kinds not yet registered as EntityKindModules.
 * Moves onto each kind's CatalogFacet when its module lands.
 */
import type { CatalogRenderArgs, CatalogRenderResult } from "./types.js";
/** Simple `{ name }` / optional parent for deferred catalog stubs. */
export declare function renderDeferredNamedEntry(factoryName: string, opts?: {
    readonly omitParent?: boolean;
}): (args: CatalogRenderArgs) => CatalogRenderResult;
