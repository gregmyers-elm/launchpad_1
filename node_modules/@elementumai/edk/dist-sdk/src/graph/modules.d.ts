/**
 * KIND_MODULES assembly — the only place that imports concrete entity
 * modules. Engine code imports derived views from {@link ./index.js}.
 */
import type { EntityKindModule } from "./types.js";
/** Full kind graph: managed resources + data-facet / org-identity kinds. */
export declare const KIND_MODULES: readonly EntityKindModule[];
